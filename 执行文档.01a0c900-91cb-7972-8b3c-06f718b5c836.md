# AI Desktop 韩立验收恢复链执行文档

## 目标与边界

修正同一提案出现后续修复任务时，旧卡点仍重开已集成修复任务的问题；保留原验收条件、任务历史和新的修复关系。不得触发清空数据或伪造验收通过。

## 本轮改动

- 旧卡点检测到同提案的后续令狐修复任务后，退出主处理位置，不再刷新旧任务。
- 持久修复任务标识优先于兼容性标记回查，防止新卡点错误匹配旧任务。
- 增加同一提案新旧修复任务并存的回归测试。

本地提交：`da57ee82`。正式打包、重启和韩立真实验收仍待当前修复链完成后核对。

g410 运行包暴露出后续任务虽已集成但未挂回最新验收失败事件的持久关系缺口。本轮进一步让最新验收事实认领携带其故障身份的事件专属修复任务，避免旧卡点退出后留下无主修复结果。

旧卡点退出时使用独立的“后续修复任务已接管”阶段，避免页面把正常接管误称为“修复轮次已用尽”。

用户反馈令狐 r7 临时 worktree 中的 `node --test` 反复弹出“允许并信任”。该路径是隔离工作目录，不是正式打包目录。修正审批链：受管任务的 Node 代码测试由桌面固定验证链执行，不再转成人工命令审批；其余确需人工审批的命令新增“仅允许本次”，长期信任必须由用户明确选择。保持已有工作区隔离和审计。

监控发现旧合并冲突任务从 r7 升到 r9，虽然执行器已因没有源码差异而停止，冲突自动调度仍把相同旧任务重新派发。增加调度前及异步回调前的双重门禁；保留旧任务失败证据，避免无限创建工作树和重复弹窗。

新包真实验收时，令狐 r15 的本地提交审批覆盖在韩立正式页面上，韩立只读保护拒绝审批响应并重复记录异常。修正为韩立检查期间只从该窗口隐藏其他人物的待批请求；请求保留且验收结束后重新显示，不扩大韩立写入权限。该提交审批仅以“仅允许本次”处理，没有建立长期信任。

07:50 的韩立真实验收证据指出：隔离场景先显示“令狐核对中／你无需操作”，随后后台 Evolution 实时广播把同一个正式窗口覆盖为“韩立真实验收”，违反条件 1、5。场景快照和读取接口本身已隔离，但组合根四处后台推送未隔离。修正为验收窗口不接收普通 Evolution、协作成员及时间线推送；场景自身仍专门推送窗口快照，结束时恢复实际状态。

令狐旧修复 r16 与 06:46 已集成修复具有相同提案、问题正文和确认目标，继续签发只是重复旧卡点。恢复时识别严格等价的较新集成结果，保留旧工作树和执行审计，将旧任务停在“后续等价修复已集成”，释放人物占用，不再自动派发；不同提案或不同故障不能误退役。

打包首次在 Electron 类型门禁发现新增流程事件未登记。已补充事件类型与时间线“后续等价修复已接管”投影；Electron 构建复检通过，旧运行包未被失败包替换。

用户要求统一测试“一次跑、一次修”。批次 412 和 413 已证明固定脚本会同轮执行并汇总失败脚本，但单个长脚本只把最后 4,000 字符交给修复，前面的独立失败用例丢失。限定修改统一测试输出收集器：流式保留每个 TAP 失败块并连同末尾日志提交给一次修复调查；不改变测试清单、断言或发布门禁。工作在独立本地工作树完成，待现有协作任务进入安全集成点后再合并。

统一测试失败块收集器已在主工作树本地提交 `19f9ac44`。g414 韩立实机验收又发现窄窗口详情面板高度为零，且新失败没有进入修复链。根因是旧令狐修复任务已集成，旧卡点仍尝试把新验收故障写回该任务，写入端按不同恢复点拒绝，旧事件反复保留 processing 并抢占主卡点。本轮修正协调器：当旧任务已集成且新验收事件有独立故障身份时，将旧卡点标为被新故障接管；新事件获得专属修复关系，旧任务与审计不删除。布局缺陷保留给新修复任务按真实验收证据处理，不在未复现时改动页面。

# 本次执行文档

## 总体任务目标

把公共 `store` 返回中的 `requestPath` 从“全量可访问路径列表”收正为“当前 store 接口的单一路径”，新增 `getVerifyAvailablePath()` 共通方法，并确认 `UniauthUserController` 调用链与离线编译结果正常。

## 执行步骤

1. **完成**：确认 `BaseExtendsController`、`BaseController` 与 `UniauthUserController` 当前 `store` 返回参数口径，定位需要从“路径列表”改成“单路径”的最小修改范围。
   - 实际结果：已确认 `buildStoreResultJson` 的第二个参数业务语义始终是单个 `requestPath`，但 `UniauthUserController#getStore` 已误传 `getVerifyAvailablePaths()` 整组列表，因此本轮最小修正范围只需收口到公共扩展基类和当前控制器调用点。
2. **完成**：在公共控制器扩展层新增单路径获取方法，并把 `UniauthUserController#getStore` 切换到新的单路径调用，保持中文业务注释完整。
   - 实际结果：已在 `BaseExtendsController` 新增 `getVerifyAvailablePath()` 和 `resolveMethodRequestPath(String methodName)`，默认按 `getStore` 的 `@RequestMapping` 自动拼出当前控制器的 store 完整路径；`UniauthUserController#getStore` 也已改为传入单个 `getVerifyAvailablePath()`，避免再把全量路径列表写进 `requestPath`。
3. **完成**：执行与本次控制器共通化调整风险匹配的离线编译验证，并把结果回写到执行文档。
   - 实际结果：已执行 `.\gradlew.bat --offline :shared:backend:common-controller:compileJava :apps:uniauth:backend:compileJava --console plain`，结果 `BUILD SUCCESSFUL`，确认公共控制器层与 `uniauth` 模块编译通过。

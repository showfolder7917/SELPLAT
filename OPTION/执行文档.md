# 本次执行文档

## 总体任务目标

在 `OPTION` 下整理“让 VS Code 自动编译与单工程启动统一走根 `build`、不再重建模块 `bin`”的落地实施文档，供后续审阅确认。

## 执行步骤

1. **已完成**：整理当前 Gradle 输出目录、VS Code Java 启动链和 `bin` 重建现象，收口问题现状与根因。
   - 实际结果：已确认 Gradle 正式输出已统一到根 `build/<project-path>`；`apps/uniauth/backend/bin` 的重建来自 VS Code Java Language Server / JDT 本地编译输出，而不是 Gradle 主构建链。
2. **已完成**：形成候选修正方案、推荐方案和实施顺序，写入 `OPTION/修正/编译/` 文档。
   - 实际结果：已新增一份偏实施的落地文档，说明需要把 VS Code 自动编译与单工程启动一并收口到根 `build`，避免继续生成模块 `bin`。
3. **已完成**：补充实施后的验证清单与风险提示，确保文档可直接用于后续执行。
   - 实际结果：文档已包含实施步骤、验证命令、风险点和回滚建议，可直接作为后续改造依据。

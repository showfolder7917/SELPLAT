# 本次执行文档

## 总体任务目标

在 `shared/backend/common-db/src/main/java/com/sp/selplat/common/db/dao` 目录新增一份查询后缀定义规则文档，统一说明当前 `Map+字段后缀` 查询约定和使用示例。

## 执行步骤

1. **完成**：确认规则文档的落点和说明范围，避免和现有代码注释重复冲突。
   - 实际结果：已确认 `dao` 目录目前只有查询相关 Java 文件，没有独立规则说明文档，因此本轮直接新增一份面向使用者的 `QUERY_SUFFIX_RULES.md`。
2. **完成**：在 `dao` 目录新增查询后缀规则文档，统一说明等于、模糊、区间、比较和展示字段跳过规则。
   - 实际结果：已新增 `shared/backend/common-db/src/main/java/com/sp/selplat/common/db/dao/QUERY_SUFFIX_RULES.md`，固定说明 `无后缀/Like/Ge/Gt/Le/Lt/Begin/End/View` 的含义、字段还原方式和最终操作符。
3. **完成**：补充每类后缀的命名示例、组合示例和注意事项，确保后续调用方可直接照文档使用。
   - 实际结果：文档中已补充等于查询、模糊查询、区间查询、多条件 `AND` 组合查询、`View` 跳过说明、`BETWEEN` 的结构化用法以及统一命名建议。
4. **完成**：执行最小验证，确认新增规则文档路径正确、内容可读且未影响现有源码结构。
   - 实际结果：已重新读取 `QUERY_SUFFIX_RULES.md` 全文并核对 `dao` 目录文件列表，确认文档路径正确、内容完整，且本轮未改动任何 Java 源码逻辑。
5. **完成**：回写执行文档步骤状态并记录本轮实际结果。
   - 实际结果：本轮执行文档已同步更新完成，当前 `dao` 目录除原有 Java 文件外新增了一份查询后缀规则说明文档，后续可直接按文档约定使用查询字段命名。

# 本次执行文档

## 总体任务目标

继续收薄 `shared/backend/common-db/src/main/java/com/sp/selplat/common/db/dao/BaseDaoImpl.java`，除 `getFields()` 之外，把数据源上下文解析逻辑下沉到 `config`，把字段元数据转 select 字段串的逻辑下沉到 `metadata`，让 `BaseDaoImpl` 只保留 DAO 层对接职责。

## 执行步骤

1. **完成**：复读 `BaseDaoImpl.java` 与 `common-db` 现有 `config / metadata` 目录，确认本次下沉的目标落点和边界。
   - 实际结果：已确认 `BaseDaoImpl` 当前除 `getFields()` 外仍内嵌 `buildCommonDbSource`、`buildSelectColumns`、`resolveDatabaseType` 等辅助逻辑；`config` 目录目前只有数据源实体与枚举，`metadata` 目录目前只有元数据读取接口与默认实现，适合新增职责明确的小型支撑类。
2. **完成**：新增 `config` / `metadata` 支撑类，并改造 `BaseDaoImpl.java` 只保留瘦身后的 `getFields()` 对接逻辑。
   - 实际结果：已新增 `common-db/config/CommonDbSourceResolver.java`，把 `DataSource -> CommonDbSource` 的上下文解析、数据库类型识别和方言支持校验统一下沉到 `config` 层；已新增 `common-db/metadata/MetadataSelectColumnBuilder.java`，把字段元数据转 `select` 字段串的规则统一下沉到 `metadata` 层；`BaseDaoImpl.java` 现仅保留 `getFields()` 接线逻辑，通过解析器和构建器对接现有 `metadata` 能力，不再内嵌库型解析、字段拼接和文本工具方法。
3. **完成**：执行与本次改动风险相匹配的最小验证，并记录阻塞或风险。
   - 实际结果：已完成源码回读，确认 `BaseDaoImpl.java` 中已不存在 `buildCommonDbSource`、`resolveDatabaseType`、`buildSelectColumns`、`defaultText`、`hasText` 等辅助方法；并执行 `./gradlew.bat :shared:backend:common-db:compileJava --offline --console plain --project-cache-dir .gradle-project-cache`，构建仍在 `:shared:backend:common-core:compileJava` 阶段因无法创建 `C:\opt\workspace\SELPLAT\shared\backend\common-core\build\classes\java\main` 目录失败，阻塞点与先前一致，未进入 `common-db` 编译，因此本轮仍只有源码级验证结果。

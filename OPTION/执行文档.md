# 本次执行文档

## 总体任务目标

把 `shared/backend/common-db` 下的 `CommonPageResult` 上移到 `shared/backend/common-core`，并统一修正全工程分页回参引用，保证分页入参与回参都收口在共通层。

## 执行步骤

1. **已完成**：把 `CommonPageResult` 移动到 `common-core` 合适目录，并保持类注释、字段和包结构一致。
   结果：`CommonPageResult` 已从 `shared/backend/common-db/src/main/java/com/sp/selplat/common/db/query/model` 上移到 `shared/backend/common-core/src/main/java/com/sp/selplat/common/util`，类内容与注释保持一致，分页入参与回参现在都收口到共通层。
2. **已完成**：修正 `common-db`、`common-controller`、`apps/uniauth` 等所有受影响 Java 文件的 import 与类型引用，避免分页回参仍指向旧包。
   结果：`BaseDao`、`BaseDaoImpl`、`BasePagingQueryDaoImpl`、`BaseDaoQueryDemo`、`BaseExtendsController`、`UniauthUserController`、`UniauthUserService`、`UniauthUserServiceImpl`、`UniauthUserDao`、`UniauthUserDaoImpl` 等文件已统一改为引用 `com.sp.selplat.common.util.CommonPageResult`。
3. **已完成**：执行与本次结构调整匹配的离线编译验证，并确认旧位置不再残留引用。
   结果：先用 `rg -n "com\\.sp\\.selplat\\.common\\.db\\.query\\.model\\.CommonPageResult"` 回扫，旧包引用已清空；再执行 `.\gradlew.bat --offline :shared:backend:common-core:compileJava :shared:backend:common-db:compileJava :shared:backend:common-controller:compileJava :shared:backend:common-service:compileJava :apps:uniauth:backend:compileJava --console plain`，结果 `BUILD SUCCESSFUL`。

**执行时间：2026-06-25 18:04:00 JST**

# 本次执行文档

## 总体任务目标

执行当前工程的最小必要编译验证，确认切回 `build` 目录后当前环境是否能成功构建，并收敛实际阻塞点。

## 执行步骤

1. **完成**：确认本轮编译验证使用的 Gradle 缓存与项目缓存路径，避免旧缓存锁干扰结果。
   - 实际结果：已使用工作区内独立的 `GRADLE_USER_HOME=.gradle-user-home` 和 `--project-cache-dir .gradle-project-cache`，排除了此前 `.gradle-tmp-cache` 锁文件干扰。
2. **完成**：执行最小必要的离线 Gradle 编译任务，覆盖公共模块与 `uniauth` 后端主路径。
   - 实际结果：已执行 `gradlew.bat --offline --console plain --project-cache-dir .gradle-project-cache :shared:backend:common-core:classes :shared:backend:common-db:classes :apps:uniauth:backend:classes`；构建在 `:shared:backend:common-core:compileJava` 阶段失败。
3. **完成**：整理编译结果，区分构建脚本问题、依赖问题和当前 `.codex` 会话写入限制问题。
   - 实际结果：失败核心报错为 `Failed to create directory 'C:\opt\workspace\SELPLAT\shared\backend\common-core\build\classes\java\main'`；编译尚未进入依赖解析或源码报错阶段，当前更接近工作区运行期写入受限，而不是 `build.gradle` 逻辑或 Java 源码问题。

**执行时间：2026-06-26 10:05:00 JST**

# 本次执行文档

## 总体任务目标

执行当前工程的本地离线 `gradle build` 编译，确认是否能成功构建并收敛实际阻塞点。

## 执行步骤

1. **完成**：确认本轮编译使用的 Gradle 执行方式与缓存路径，避免网络下载和旧缓存干扰结果。
   - 实际结果：已确认系统 `gradle` 来自 `C:\Program Files\gradle-8.11\bin\gradle.bat`，并使用工作区内 `GRADLE_USER_HOME=.gradle-user-home` 与 `--project-cache-dir .gradle-project-cache` 执行离线构建。
2. **完成**：在项目根目录执行离线 `gradle build`，采集成功或失败输出。
   - 实际结果：已执行 `gradle build --offline --console plain --project-cache-dir .gradle-project-cache`；构建在 `:shared:backend:common-core:compileJava` 阶段失败，报错为 `Failed to create directory 'C:\opt\workspace\SELPLAT\shared\backend\common-core\build\classes\java\main'`。
3. **完成**：整理编译结论，记录关键阻塞点、验证范围与后续处理建议。
   - 实际结果：已确认 `shared/backend/common-core/build/classes/java` 目录存在，但手工创建 `main` 子目录同样返回 `Access to the path 'main' is denied`；当前失败更接近该路径的运行期写入权限异常，而不是依赖下载、Gradle 命令写法或 Java 源码编译错误。

**执行时间：2026-06-26 10:58:00 JST**

# 本次执行文档

## 总体任务目标

调查 VS Code 中 `CPMAB203` 与 `SBMAB203` 两个 Java 项目的右键测试行为差异，定位 `SBMAB203Tester.java` 无法运行的具体原因，并输出调查报告。

## 执行步骤

1. **完成**：比对 `CPMAB203` 与 `SBMAB203` 的 VS Code 配置、Gradle 配置、测试目录结构与测试类定义差异。
   - 实际结果：两边都是 Gradle + JUnit Platform + `@SpringBootTest` 测试类；项目配置未发现能解释“一边能测一边不能测”的决定性差异。
2. **完成**：检查 VS Code Java / Test Runner 相关痕迹、项目缓存与构建输出，定位 `SBMAB203Tester.java` 失效原因并判断是否与插件更新或工作区重开有关。
   - 实际结果：在 `redhat.java 1.55.0` 日志中反复命中 `com.microsoft.java.test.plugin` 装载失败，原因是它要求 `org.objectweb.asm` 版本区间 `[9.9.0,9.10.0)`，而当前语言服务实际提供 `9.10.1`；问题高度确定由今日 Java 插件升级触发。
3. **完成**：整理调查结论与可执行建议，将报告写入 `C:\opt\workspace\SELPLAT\log`。
   - 实际结果：已生成报告 `log/vscode_java_test_investigation_2026-06-26.md`，结论为 VS Code Java 扩展版本失配，而非 `SBMAB203` 项目本身损坏。

**执行时间：2026-06-26 13:54:38 JST**

# 本次执行文档

## 总体任务目标

调查 `CPMAWeb-1` 页面与 `CPMAA251`、`CPMAB081`、相关 Mapper SQL 和表结构之间的链路，定位上传入口、上传文件格式、差分表与反映表，并制作满足“新规一条数据”的上传文件。

## 执行步骤

1. **完成**：定位 `CPMAWeb-1` 画面中调用 `CPMAA251` 的入口，确认页面位置、控制器/接口和上传动作链路。
   - 实际结果：已确认页面入口为 `ファイルアップロード -> 格付情報 -> CP0N130`；上传动作在 `cp0n130.component.ts` 中调用 `apiService.execMessagePost(CPMAApiServiceIds.CPMAA251, this.SCREEN_ID, request)`，请求字段为 `ratingInfoRAndIDifFile` / `ratingInfoJcrDifFile`。
2. **完成**：定位 `CPMAA251` 上传文件格式、字段要求，以及“新规一条数据登录到格付差分情报表”的目标表名。
   - 实际结果：已确认 RI/JCR 差分文件分别按 `CPMAF235001DataBean` / `CPMAF236001DataBean` 解析；新规条件对应 `RD_KBN='1'`，有效商品种别需为 `PROD_TYP='41'` 或 `92`；格付差分情報表物理名为 `TB_FCPDIFFRATINGINFO`。
3. **完成**：结合 `CPMAB081` 与 `CPMAMapper.xml`，确认反映目标表、登录日期字段，并制作满足条件的上传文件与调查报告写入 `C:\opt\workspace\SELPLAT\log`。
   - 实际结果：已确认差分先反映到 `TB_FCPFULLRATINGINFO`，再由 `CPMAB081` 全删全插生成 `TB_FCPRANK`；“全表登录日期是今天”最应确认 `TB_FCPRANK.CRE_OPE_DATE_AP`；已生成 `log/CPMAA251_CPMAB081_调查结果_2026-06-26.md` 和 `log/rating_diff_ri_new_20260626.csv`。

**执行时间：2026-06-26 14:36:33 JST**

# 本次执行文档

## 总体任务目标

生成一段可在 SQL 查询器中直接执行的查询 SQL，使查询结果按上传文件顺序输出并可直接复制为 CSV，并写入 `log/1`。

## 执行步骤

1. **完成**：核对上传文件字段顺序与差分表字段名，确定 SQL 输出口径。
   - 实际结果：已确认 `TB_FCPDIFFRATINGINFO` 只保存 `RATING_CO_KBN / RD_KBN / DAT_DT / ANNO_DT / RATING_INFO_KBN / ISSUER_JAN_NM`，无法完整还原原始 68 列文件；本轮 SQL 采用“库中可查字段按文件顺序输出，其余字段留空占位”的导出策略。
2. **完成**：编写一段查询结果可直接复制为 CSV 的 SQL。
   - 实际结果：已生成一条返回 `CSV_LINE` 的 SQL，默认查询 R&I 新规数据，按上传文件顺序拼接为 68 列 CSV；第 3 列对 R&I 回填 `R001`，第 17 列默认回填 `41`，其余库中缺失字段按空列保留。
3. **完成**：将 SQL 写入 `C:\opt\workspace\SELPLAT\log\1` 并完成内容自检。
   - 实际结果：已写入 `log/1`；读取文件确认内容落盘，并对拼接列数进行了校验，结果为 68 列。

**执行时间：2026-06-26 15:07:39 JST**

# 本次执行文档

## 总体任务目标

生成一段适用于 PostgreSQL 的一键查询 SQL，使查询结果可直接复制为目标 CSV 结构，并写入 `log` 下的新文件且不覆盖 `log/1`。

## 执行步骤

1. **完成**：核对 PostgreSQL 方言与当前 `CPMAMapper.xml` 口径，按目标 CSV 结构确定拼接方式。
   - 实际结果：已确认当前 `CPMAMapper.xml` 使用 PostgreSQL 风格 SQL；并根据你的反馈调整为“原始表拼接”路线，不再依赖 `TB_FCPDIFFRATINGINFO`，改为基于 `TB_FCPISSUER + TB_FCPCORP + TB_FCPFULLRATINGINFO + TB_FCPSAMENMCOINFO` 组合取数。
2. **完成**：编写一段使用 PostgreSQL 语法的一键查询 SQL，返回可直接复制的 CSV 字符串。
   - 实际结果：已生成从原始表拼接的一键查询 SQL：`TB_FCPISSUER` 负责筛出 CP 发行者，`TB_FCPCORP` 提供公司代码和公司名称，`TB_FCPFULLRATINGINFO` 提供最新 RI 格付和公表日，`TB_FCPSAMENMCOINFO` 负责同名会社映射；`datDate`、债券名称、`prodTyp` 作为 SQL 顶部参数可手工调整。
3. **完成**：将 SQL 写入 `C:\opt\workspace\SELPLAT\log` 下的新文件并完成内容自检。
   - 实际结果：已新建 `log/1_pgsql_raw_tables.sql`，未覆盖原有 `log/1` 和 `log/1_pgsql.sql`；读取文件确认内容落盘，并按“17 个前置字段 + 51 个尾部空占位”校验总列数为 68 列。

**执行时间：2026-06-26 14:30:14 JST**

# 本次执行文档

## 总体任务目标

基于 `log/CPMAA251_CPMAB081_调查结果_2026-06-26.md`，生成一份“按单行结果手工拼成 CSV”的模板文档，并输出到 `log` 目录。

## 执行步骤

1. **完成**：提炼调查结果中的 CSV 字段顺序、关键条件和空列占位规则。
   - 实际结果：已从 `log/CPMAA251_CPMAB081_调查结果_2026-06-26.md` 和现有样例 `log/rating_diff_ri_new_20260626.csv` 提炼出本次手工拼接核心约束：前 17 列按固定顺序填写、`rdKbn=1`、`prodTyp=41/92`、总列数保持 68 列、空列必须保留连续逗号占位。
2. **完成**：编写一份可直接照抄的一行 CSV 手工拼接模板，包含字段说明、逗号占位示例和成品示例。
   - 实际结果：已整理出字段顺序表、手工拼接规则、占位模板、成品示例和最短操作版，并写入 `log/CPMAA251_CPMAB081_CSV手工拼接模板_2026-06-26.md`。
3. **完成**：将模板写入 `C:\opt\workspace\SELPLAT\log` 并完成结果自检。
   - 实际结果：已确认模板文件成功落盘；使用 PowerShell 对模板中的成品示例和占位模板执行列数校验，两者均为 68 列。

**执行时间：2026-06-26 14:30:14 JST**

# 本次执行文档

## 总体任务目标

调查 `CPMAWeb-1` 页面与 `CPMAA251`、`CPMAB081`、相关 Mapper SQL 和表结构之间的链路，定位上传入口、上传文件格式、差分表与反映表，并制作满足“新规一条数据”的上传文件。

## 执行步骤

1. **完成**：定位 `CPMAWeb-1` 画面中调用 `CPMAA251` 的入口，确认页面位置、控制器/接口和上传动作链路。
   - 实际结果：已确认页面入口为 `ファイルアップロード -> 格付情報 -> CP0N130`；上传动作在 `cp0n130.component.ts` 中调用 `apiService.execMessagePost(CPMAApiServiceIds.CPMAA251, this.SCREEN_ID, request)`，请求字段为 `ratingInfoRAndIDifFile` / `ratingInfoJcrDifFile`。
2. **完成**：定位 `CPMAA251` 上传文件格式、字段要求，以及“新规一条数据登录到格付差分情报表”的目标表名。
   - 实际结果：已确认 RI/JCR 差分文件分别按 `CPMAF235001DataBean` / `CPMAF236001DataBean` 解析；新规条件对应 `RD_KBN='1'`，有效商品种别需为 `PROD_TYP='41'` 或 `92`；格付差分情報表物理名为 `TB_FCPDIFFRATINGINFO`。
3. **完成**：结合 `CPMAB081` 与 `CPMAMapper.xml`，确认反映目标表、登录日期字段，并制作满足条件的上传文件与调查报告写入 `C:\opt\workspace\SELPLAT\log`。
   - 实际结果：已确认差分先反映到 `TB_FCPFULLRATINGINFO`，再由 `CPMAB081` 全删全插生成 `TB_FCPRANK`；“全表登录日期是今天”最应确认 `TB_FCPRANK.CRE_OPE_DATE_AP`；已生成 `log/CPMAA251_CPMAB081_调查结果_2026-06-26.md` 和 `log/rating_diff_ri_new_20260626.csv`。

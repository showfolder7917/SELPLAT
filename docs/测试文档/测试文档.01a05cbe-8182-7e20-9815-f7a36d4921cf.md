# 本次测试文档

任务线程：01a05cbe-8182-7e20-9815-f7a36d4921cf

## 统一测试清单

1. **通过**：Governance 旧路径与旧类型归零
   - 变更内容：删除 ownerless Governance 根，严重级别改为 Foundation 唯一 Value，并确保各协议只有一个定义。
   - 测试命令：test ! -e apps/ai-desktop/contracts/governance && ! rg -n \"contracts/governance|governance/index|WorkflowEventSeverityValue\" apps/ai-desktop/contracts apps/ai-desktop/electron apps/ai-desktop/src
   - 预期结果：旧目录不存在，旧导入与旧严重级别类型均无匹配；新类型只在指定所有者目录存在。

2. **通过**：Contracts 所有者同构与公开出口门禁
   - 变更内容：顶层只保留 foundation、services、system；Workflow 与 Event Center 从各自 index 显式导出。
   - 测试命令：npm run test:boundaries
   - 预期结果：模块边界测试通过，无 ownerless 主题根、深层跨模块导入或星号导出。

3. **通过**：TypeScript 类型检查
   - 变更内容：更新所有协议类型名、导入路径和公开聚合出口。
   - 测试命令：npm run typecheck
   - 预期结果：Renderer 和共享 Contracts 类型检查通过，无缺失导出或旧类型引用。

4. **通过**：Electron 主进程构建
   - 变更内容：主进程调用方改从 Workflow 与 Event Center 公开入口导入。
   - 测试命令：npm run build:electron
   - 预期结果：Electron 主进程与沙箱 preload 构建成功，IPC channel 和 Facade 方法签名保持兼容。

5. **通过**：Renderer 构建
   - 变更内容：system/desktop 继续聚合 Renderer 所需真实所有者类型。
   - 测试命令：npm run build:renderer:developer
   - 预期结果：Renderer 生产构建成功，审批治理与审计视图类型可解析。

6. **通过**：IPC 与 preload 契约回归
   - 变更内容：RendererExceptionInDto 迁入 Event Center，但 DesktopApi 和 IPC channel 保持不变。
   - 测试命令：npm run test:preload-sandbox
   - 预期结果：沙箱 preload 真实运行回归通过，desktop:renderer-exception 等既有能力仍可桥接。

7. **通过**：Workflow 与 Event Center 业务回归
   - 变更内容：审批治理、异常事件、停滞检测和令狐异常接手协议迁入真实所有者。
   - 测试命令：npm run test:workflow-event-center
   - 预期结果：审批治理投影、异常生命周期、停滞任务检测和令狐接手场景全部通过。

8. **通过**：审计读取回归
   - 变更内容：审计输出 DTO 迁入 Event Center 并由 DesktopApi 聚合。
   - 测试命令：npm run test:audit
   - 预期结果：BusinessAuditLog 写入、汇总与审计信息读取测试通过。

9. **通过**：应用启动退出与真实交互回归
   - 变更内容：协议物理迁移不改变应用生命周期和 Renderer 调用行为。
   - 测试命令：npm run test:interaction
   - 预期结果：正式 Electron 交互回归通过，应用可启动、使用既有 IPC 并正常退出。

## 2026-09-01 统一测试回写

- Governance 旧目录、旧导入和 `WorkflowEventSeverityValue` 扫描结果为零；Contracts 顶层仅有 `foundation`、`services`、`system`。
- `npm run test:boundaries` 13/13，`npm run typecheck` 通过。
- `npm run build:developer` 通过，覆盖 Electron、sandbox preload 与 Renderer 开发版构建。
- `npm run test:preload-sandbox` 通过，真实沙箱桥接 111 项能力。
- `npm run test:workflow-event-center` 18/18，`npm run test:audit` 2/2。
- `npm run test:interaction` 首轮 23/24，发现审批窗口重复打开场景缺少焦点就绪同步；修正测试同步后完整重跑 24/24 通过。

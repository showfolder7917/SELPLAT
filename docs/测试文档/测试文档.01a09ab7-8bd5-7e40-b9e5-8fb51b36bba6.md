# AI Desktop 验收证据与停滞恢复统一测试文档

任务线程：01a09ab7-8bd5-7e40-b9e5-8fb51b36bba6  
测试日期：2026-09-13

## 本轮修复范围

- 验收分段始终携带原提案条件编号，缺失、未知或局部重编号直接失败并说明实际差异。
- 缺少真实交互证据时指出具体条件以及缺少的结果、布局或截图登记，不再只返回笼统失败。
- 工作区验收夹具由主进程绑定目标窗口身份，其他渲染窗口不能消费、登记或读取夹具。
- 任务停滞依据真实协议进展判断；进程存活心跳不再掩盖模型或工具长时间无输出，重试次数按当前任务计算。

## 统一测试结果

1. **通过：验收、夹具与窗口权限定向回归**
   - 结果：77/77 通过。
   - 覆盖：原条件编号、局部重编号拒绝、逐条件证据诊断和可信窗口身份隔离。

2. **通过：协作事件与停滞恢复定向回归**
   - 结果：22/22 通过。
   - 覆盖：存活心跳持续刷新但协议进展停滞、十分钟停滞边界及当前任务重试计数。

3. **通过：AI Desktop 全量所有者测试**
   - 命令：`node scripts/run-with-dependencies.mjs node scripts/run-owned-tests.mjs`
   - 结果：608 项；606 项通过、0 项失败、2 项明确跳过。

4. **通过：TypeScript、Electron 与 Renderer 构建**
   - 命令：`npm run build:developer`
   - 结果：类型检查通过，Electron 构建通过，Renderer 634 个模块构建通过。

## 规则沉淀评估

- 结论：`no_change_with_near_rule_and_reason_evidence`。
- 近义规则：`AI_DESKTOP_COLLABORATION_AUTOMATION_RULES`、`AI_DESKTOP_HANLI_USER_QUESTIONING_RULES`、`AI_DESKTOP_EVOLUTION_PERSISTENCE_RELEASE_RULES`。
- 原因：现有规则已经明确要求原验收范围不变、真实截图与交互证据、主进程可信窗口权限、区分存活与协议进展、停滞后恢复以及禁止无新证据盲目重试。本轮缺陷来自实现没有完整落实这些规则，修正程序和回归测试即可；新增近义规则会制造重复权威。

## 2026-09-14 临时工作区生命周期接管修复

### 修复范围

- 主进程移除验收工作区后，通过唯一桌面桥接把最新工作区状态推送回原窗口。
- 清理句柄等待工作区树和文件预览同时消失；页面首次未同步时保留同一环境重试并记录恢复。
- 韩立完成前门禁必须等待临时环境释放，失败或中断仍由同一收尾函数兜底。
- 发布包内容校验继续保留在固定统一测试清单中，不能因其他故障而削弱。

### 已执行结果

1. **通过：工作区夹具定向回归**
   - 结果：9/9 通过。
   - 覆盖：准备、权限隔离、目录清理、页面状态推送、投影失败重试、遗留夹具恢复。
2. **通过：韩立验收场景定向回归**
   - 结果：43/43 通过。
   - 覆盖：临时环境释放、完成前门禁、完成态复核及失败收口顺序。
3. **通过：协作与边界回归**
   - 结果：105/105 通过。
4. **通过：AI Desktop 全量测试**
   - 结果：651 项；649 项通过、0 项失败、2 项明确跳过。
5. **通过：TypeScript、Electron 与 Renderer 构建**
   - `npm run typecheck`、Electron 构建及 Renderer 634 个模块构建均通过。
6. **待完成：本地提交、macOS 打包签名、包内容校验、发布、单实例重启与真实页面复验。**

### 规则沉淀评估

- 结论：`no_change_with_near_rule_and_reason_evidence`。
- 近义规则：`AI_DESKTOP_HARNESS_WORKSPACE_RUNTIME_RULES`、`AI_DESKTOP_EVOLUTION_PERSISTENCE_RELEASE_RULES`、`AI_DESKTOP_ARCHITECTURE_BOUNDARY_RULES`。
- 原因：现有规则已经要求临时验收数据隔离、主进程可信权限、完整发布门禁及高内聚边界。本轮是生命周期顺序和页面投影实现漏项；源码与回归测试已经把约束接入唯一运行路径，新增规则会形成重复权威。

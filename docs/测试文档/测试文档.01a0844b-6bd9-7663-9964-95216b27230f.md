# 本次测试文档

任务线程：01a0844b-6bd9-7663-9964-95216b27230f

## 统一测试清单

1. **通过**：TypeScript 领域调用链类型检查
   - 变更内容：新增六个 Contracts API 领域视图、六个 Renderer 领域适配器并迁移全部 Renderer 桥接调用。
   - 测试命令：npm run typecheck
   - 预期结果：TypeScript 零错误，Renderer 各模块只能调用所属领域 API 暴露的方法。
   - 实际结果：显式处理尚未载入的演化状态后，TypeScript 零错误。

2. **通过**：Electron IPC 与沙箱 preload 构建
   - 变更内容：rules bridge 改名，conversation IPC 独立拆分，Codex 与 system handler 重新归属。
   - 测试命令：npm run build:electron
   - 预期结果：Electron 编译成功，沙箱 preload 成功合并六个同名领域 bridge，运行入口无缺失模块。
   - 实际结果：公共包、146 份规则、33 份提示词、Electron 主进程与沙箱 preload 构建成功。

3. **通过**：跨运行时模块边界门禁
   - 变更内容：能力注册表改为组合领域清单，并新增禁止 Renderer 业务直接访问 window.desktop 的静态门禁。
   - 测试命令：npm run test:boundaries
   - 预期结果：DesktopApi 方法在六个领域中唯一且完整，preload 完整覆盖，Renderer 无越界直连，conversation IPC 被总入口调用。
   - 实际结果：15/15 通过。

4. **通过**：主会话托管执行回归
   - 变更内容：主会话队列、发送和托管执行 handler 从总 IPC 注册器迁入 register-conversation-ipc.ts。
   - 测试命令：npm run test:managed
   - 预期结果：会话托管模式、等待队列、只读沙箱、审批与恢复的既有契约全部通过。
   - 实际结果：过期源码形式断言修正后 45/45 通过；完整所有者回归再次覆盖并通过。

5. **通过**：截图跨进程调用回归
   - 变更内容：截图 Application、Hook 和编辑器改用 screenshot/system 领域适配器。
   - 测试命令：npm run test:screenshot
   - 预期结果：截图权限、取帧、标注、保存、附件回流和窗口控制契约全部通过。
   - 实际结果：Electron 前置构建成功，截图存储与临时目录清理 2/2 通过。

6. **通过**：Developer 左右目录结构与退役路径门禁
   - 变更内容：左侧导航进入 `applications/developer/explorer`，右侧容器和路由进入 `applications/developer/workspace`，旧路径与 `CollaborationExplorerFeature` 退役。
   - 测试命令：npm run test:boundaries
   - 预期结果：新目录文件全部存在，旧路径全部不存在，协作 Feature 不再导出左侧跨模式导航。
   - 实际结果：与第 3 项同轮执行，15/15 通过。

7. **通过**：Developer Renderer 类型检查
   - 变更内容：`TaskExplorerFeature` 拆分模式开关、单会话摘要和协同任务导航，工作区路由移动后同步全部相对导入。
   - 测试命令：npm run typecheck
   - 预期结果：TypeScript 零错误，四个右侧业务页面和左侧导航的参数类型保持完整。
   - 实际结果：与第 1 项同轮复测，TypeScript 零错误。

8. **通过**：Developer 正式窗口交互回归
   - 变更内容：只调整 Renderer 目录与组件边界，不改变模式切换、人物选择、任务群展开和右侧换页行为。
   - 测试命令：npm run test:interaction
   - 预期结果：正式窗口能够启动，单会话/协同切换、人物页面和任务协作群页面均可真实进入，页面视觉无变化。
   - 实际结果：完整 Developer 构建成功；首次受限环境启动 Electron 收到 `EPERM`，使用本机隔离测试权限重试后 28/28 通过。

## 2026-09-09 最终统一回归

- [x] `npm test`：392 项，390 通过、0 失败、2 项明确退役的历史兼容用例跳过。
- [x] `npm run test:interaction`：28/28 通过，正式生产 Renderer 与 Electron 窗口交互验证完成。

## 本轮即时验证记录

1. **通过**：Developer 目录结构、模块边界、协作页面和 SEL UI 静态回归
   - 实际命令：`node --test tests/applications/developer/developer-explorer-collapse.test.mjs tests/contracts/module-boundaries.test.mjs tests/features/collaboration/collaboration-progress-contract.test.mjs tests/contracts/sel-ui-adoption.test.mjs`
   - 实际结果：30 项通过，0 项失败；新 explorer/workspace 路径存在，旧路径与旧组件出口归零。

2. **环境阻断**：TypeScript 类型检查
   - 实际命令：`npm run typecheck`
   - 第一次结果：依赖包装器创建 `cache/ai-desktop/test-tmp/process-temp` 时收到 `EPERM`；创建受控缓存目录后已越过此处。
   - 第二次结果：当前依赖缓存只有 `.broken-20260903`，自动准备依赖时 npm 以 `Exit handler never called` 退出，未进入 `tsc`。
   - 后续动作：统一测试时先恢复受控依赖缓存，再重新执行 `npm run typecheck`。

3. **环境阻断**：Developer 即时启动冒烟
   - 计划命令：`npm run start:developer`
   - 未执行原因：启动入口先执行与上项相同的类型检查和依赖准备，当前 npm 缓存故障会在窗口启动前失败。
   - 后续动作：依赖缓存恢复后执行正式启动与 `npm run test:interaction`，保留窗口和交互证据。

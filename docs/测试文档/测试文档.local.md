# 本次测试文档

任务线程：local

## 统一测试清单

1. **待测试**：协作模块 TypeScript 类型检查
   - 变更内容：协作 model、页面组件和新增新手结构文件整体重构
   - 测试命令：cd apps/ai-desktop && npm run typecheck
   - 预期结果：TypeScript 编译完成且无类型、导入、React Hook 或未使用符号错误

2. **待测试**：协作功能静态契约回归
   - 变更内容：保持公开导出、托管任务控制器和协作组件所有者边界不变
   - 测试命令：cd apps/ai-desktop && npm run test:managed
   - 预期结果：协作托管执行与应用静态契约全部通过，无旧结构或公开接口回归

3. **待测试**：Developer 协作页面真实交互回归
   - 变更内容：重构运行模式、人物列表、任务群展开、审批及继续任务交互
   - 测试命令：cd apps/ai-desktop && npm run test:interaction
   - 预期结果：真实 Playwright 页面中单会话/协同切换、人物页面、任务群时间线和继续操作正常，并生成截图或结果日志证据

4. **待测试**：协作 Renderer 专项源码契约
   - 变更内容：任务协作群拆分为主页面、页面 Hook、专题卡和显示转换，并同步静态契约读取范围
   - 测试命令：cd apps/ai-desktop && node --test tests/features/collaboration/collaboration-status-chain-contract.test.mjs tests/features/collaboration/collaboration-progress-contract.test.mjs tests/services/workflow/collaboration-persona-status.test.mjs
   - 预期结果：继续任务、人物时间线、状态文案和旧入口归零等专项源码契约全部通过

# AI Desktop 模型、验收与修复流程统一测试文档

任务线程：01a08ada-91ce-71f3-9d46-44e125c9144c
测试日期：2026-09-12

## 本轮修复范围

- 修复 Codex 0.154.0 官方模型目录字段变化导致的模型列表加载失败，兼容分页、去重、空结果、错误和重试。
- 韩立、南宫婉会话模型选择器使用同一官方模型目录；韩立选择器位于“自动托管”旁，南宫婉位于输入区底部，不再显示错误的“未知”。
- 韩立验收同时检查功能与布局，必须留下真实操作和截图证据；旧记录或缺少布局证据的记录不能误判通过。
- 修复流程区分产品缺陷、验收能力问题和运行环境问题，防止修错层、执行错以及无新证据的重复修复。
- 修正发布包内容校验：数据库迁移清单的注释标题不再被误当成 SQL 文件。

## 统一测试结果

1. **通过：TypeScript 类型检查**
   - 命令：`npm run typecheck`
   - 结果：0 个类型错误。

2. **通过：全部所有者测试**
   - 命令：`npm test`
   - 结果：424 项；422 项通过、0 项失败、2 项明确的历史兼容跳过。

3. **通过：真实 Electron 页面交互与布局测试**
   - 命令：`npm run test:interaction`
   - 结果：29/29 通过。
   - 覆盖：模型选择器位置和可用性、测试台侧栏、自动测试默认关闭、卡点恢复、韩立真实操作和布局验收、最小窗口布局。

4. **通过：macOS Developer 打包、签名与隔离启动**
   - 命令：`npm run package:mac:developer`、`npm run verify:mac:developer`
   - 结果：应用身份 `com.selplat.aidesktop.developer`、稳定指定要求、内置 Codex 0.154.0、OpenAI 签名和隔离启动全部通过。

5. **通过：发布包内容和工程根校验**
   - 命令：`npm run verify:package-content`、`npm run verify:developer-package-root`
   - 结果：1288 个运行文件通过；无源码、临时文件或跨语言泄漏；Developer 工程根正确。
   - 首次异常：校验器将 `load-order.txt` 的注释标题 `# versionCode|SQL file|description` 误识别为迁移记录。
   - 修复复测：过滤空行和 `#` 注释行后，专项测试 8/8 及发布包内容校验通过；包内真实迁移文件原本完整。

6. **通过：正式发布重启**
   - 入口：`apps/ai-desktop/启动开发版.command`
   - 结果：依赖缓存核验、重新构建、打包、本机稳定签名、LaunchServices 注册和启动全部成功。
   - 发布位置：`build/ai-desktop/package/developer/mac-arm64/AI Desktop.app`
   - 健康确认：主进程、GPU、网络、Renderer 与内置 Codex app-server 进程持续运行；再次启动会由单实例保护安全退出。

## 验收结论

本轮统一测试、真实页面布局测试、macOS 打包、发布包校验和正式重启均已通过。当前版本可以作为本轮开发版结果使用。

## 规则沉淀评估

- 结论：`updated_existing_rule_with_reason_evidence`。
- 原因：本轮暴露的是既有验收与修复规则未同时约束功能、布局、真实证据和故障分层，已直接补强现有规则及历史记录，无需创建同义规则。

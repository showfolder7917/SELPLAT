# AI Desktop 侧栏测试台测试文档

任务线程：01a08ada-91ce-71f3-9d46-44e125c9144c

## 统一测试清单

1. **已通过（4/4）**：测试台结构与数据边界契约
   - 变更内容：左侧活动栏新增测试台入口，复用 SELUI 浮动窗口；证据只读取当前协作、演化、运行版本、模型目录与脱敏审计状态。
   - 测试命令：在 `apps/ai-desktop` 执行测试台专项契约测试。
   - 预期结果：入口、可调整侧窗、只读事实来源、SELUI Disclosure、绝对路径和敏感信息脱敏全部通过。

2. **已通过（零错误）**：TypeScript 类型检查
   - 变更内容：新增 Test Console Feature、显示模型并接入 Developer Controller、ViewModel 与 Activity Section。
   - 测试命令：在 `apps/ai-desktop` 执行 `npm run typecheck`。
   - 预期结果：新增类型、公开入口与应用装配零错误。

3. **已通过（408 通过、0 失败、2 个明确退役跳过）**：AI Desktop 全部所有者测试
   - 变更内容：测试台与此前同一任务内的自动恢复、依赖缓存修复共同回归。
   - 测试命令：在 `apps/ai-desktop` 执行 `npm test`。
   - 预期结果：全部有效测试通过，退役测试只允许明确跳过。

4. **已通过（29/29）**：真实 Electron 页面交互与视觉验证
   - 变更内容：用户从左侧活动栏打开、关闭测试台，查看运行版本、模型、测试、重启、验收以及展开技术证据。
   - 测试命令：在 `apps/ai-desktop` 执行 `npm run test:interaction`。
   - 预期结果：正式 Developer Renderer 中测试台真实打开，入口不挤占正常客户页面，面板可滚动且无本机绝对路径，并生成截图与 Playwright 结果证据。

5. **已通过**：macOS Developer 打包与校验
   - 变更内容：测试台进入唯一 Developer 产品线安装包。
   - 测试命令：依次执行 `npm run package:mac:developer`、`npm run verify:mac:developer`、`npm run verify:package-content`、`npm run verify:developer-package-root`。
   - 预期结果：应用打包、稳定签名、内置运行时、包内容和开发工程根校验全部通过。

6. **已通过**：正式启动与重启健康检查
   - 变更内容：安装最新打包版本并通过正式启动器重启。
   - 测试命令：执行 `docs/测试文档` 中登记的 macOS 正式发布重启入口。
   - 预期结果：旧实例退出，新打包版本启动并保持运行，测试台仍可打开。

## 即时启动记录

- 首次结果：正式 Developer Renderer 构建成功；受限环境首次启动隔离 Electron 收到 `SIGABRT/EPERM`，没有进入页面用例。
- 权限内复测：真实 Electron 页面 29/29 通过，测试台可从左侧打开并生成截图。
- 截图复核：主体内容、滚动区和各项真实证据正确；发现“技术证据”按钮被活动栏通用尺寸挤成竖排，已修正并登记复测。
- 修正后复测：真实 Electron 页面再次 29/29 通过；“技术证据”标题恢复横向显示，测试台具备独立滚动区和可拖动宽度。
- 打包校验：Developer 应用打包与稳定签名通过；内置 Codex 版本为 0.154.0，OpenAI 签名、隔离启动、1288 项包内容和 Developer 工程根校验均通过。
- 正式重启：通过工程登记的 Developer 启动入口完成重新构建、打包、签名、注册与启动；最新应用进程持续运行。

## 规则沉淀评估

- 结论：`no_change_with_near_rule_and_reason_evidence`。
- 近义规则：AI Desktop 事件记忆与界面规则已限定只读事实展示与客户界面边界；SELPLAT 水晶界面规则已限定浮动侧窗和统一组件；AI Desktop 架构边界规则已限定数据读取和功能装配位置。
- 理由：本次是单应用的一次性测试台实现，现有规则能够完整覆盖，没有出现规则缺失、失效或无法防止复发的证据，因此不新增或重复修改长期规则。

## 验收证据

- 截图：`OPTION/temp/ai-desktop/临时材料/测试证据/interaction/standalone/artifacts/developer-sidebar-用户可从左侧活动栏打开测试台并查看真实验收证据/test-console-open.png`
- 页面结果：左侧烧杯入口可打开测试台；测试台显示修复摘要、运行版本、模型、统一测试、打包重启和韩立验收状态；技术证据可展开；本机绝对路径与敏感字段不会显示。

# 本次测试文档

任务线程：01a05f93-87fb-79e2-8f05-ec2e75575103

## 统一测试清单

1. **待测试**：活动用户规则、人物上下文与任务快照
   - 变更内容：验证只加载 AGENTS 当前用户、不加载 core/common/其他用户，四人物规则注入且任务规则正文与哈希冻结
   - 测试命令：npm run build:electron && node scripts/run-with-dependencies.mjs node --test tests/services/support/capabilities/rules/active-user-rule-workspace.test.mjs tests/services/workflow/collaboration-mode.test.mjs
   - 预期结果：构建成功；活动用户隔离、人物规则、任务快照与协作持久化测试全部通过

2. **待测试**：无源码规则工作区、ZIP outbox 与启动上传降级
   - 变更内容：验证 UI 主工作区源码检测、本地工作区初始化、规则热重载、标准 ZIP、SHA-256 幂等和 Disabled 上传失败保留
   - 测试命令：npm run build:electron && node scripts/run-with-dependencies.mjs node --test tests/services/support/capabilities/rules/active-user-rule-workspace.test.mjs
   - 预期结果：源码/无源码分流正确；规则变更生成 ZIP；单次异步上传成功归档或失败保留 outbox

3. **待测试**：AGENTS 入口迁移与 ruleengine 回归
   - 变更内容：验证根转发入口、ruleengine/AGENTS 唯一完整入口、路径配置、Python 调用方和测试引用同步
   - 测试命令：python apps/ai-desktop/ruleengine/tests/run_tests.py
   - 预期结果：ruleengine 全部测试通过且不存在仍将根 AGENTS 当作稳定用户事实源的生产调用

4. **待测试**：提示词、规则树与开发版打包内容
   - 变更内容：验证南宫任务分发提示词可选择 taskRuleIds，构建仅携带 AGENTS、根索引与当前用户规则树，真实开发包包含必需资源
   - 测试命令：npm run build:developer && npm run verify:package-content
   - 预期结果：类型检查和构建通过；提示词清单通过；开发包规则资源完整且排除会话、历史、模板与非当前用户树

5. **待测试**：AI Desktop 规则升级完整回归
   - 变更内容：统一验证 contracts、人物协作、账号状态兼容、规则 IPC、Renderer 与发布门禁
   - 测试命令：npm test
   - 预期结果：全部 AI Desktop 所有者测试通过，无类型错误、规则越界或既有业务回归

6. **待测试**：AI Desktop TypeScript 类型门禁
   - 变更内容：韩立语义记忆、异步提取、人物应用服务与启动组合根新增接口和调用链。
   - 测试命令：npm run typecheck
   - 预期结果：TypeScript 类型检查退出码为 0，新增代码无类型错误。

7. **待测试**：AI Memory 数据库迁移与恢复门禁
   - 变更内容：数据库版本从 1013 升至 1019，新增提取状态、关注点、证据、需求轨迹、需求节点和验收经验六张表。
   - 测试命令：npm run test:database-foundation
   - 预期结果：新库、旧库升级、校验和与 Electron SQLite 运行时测试全部通过，最新版本为 1019。

8. **待测试**：韩立语义记忆和去重回归
   - 变更内容：新增按稳定用户、项目范围、内容哈希、提取器版本和处理租约的语义提取与证据持久化。
   - 测试命令：npm run test:workflow-event-center
   - 预期结果：语义候选只领取一次、用户隔离、证据可追溯、内容变化可重新领取，事件中心回归全部通过。

9. **待测试**：韩立分析与真实验收回归
   - 变更内容：韩立提问前接入语义上下文和只读调查，验收新增布局客观检查并把复验经验供后续计划复用。
   - 测试命令：npm run test:nangong-evolution
   - 预期结果：韩立研讨、审批、验收失败返还、复验提升及 inspect-layout 白名单执行全部通过。

10. **待测试**：提示词和规则包构建门禁
   - 变更内容：新增韩立语义提取提示词并升级四个韩立阶段提示词及 XUNAN 人物规则。
   - 测试命令：npm run build:prompts && npm run build:rules
   - 预期结果：提示词清单变量、版本、规则索引和打包资源校验通过且构建退出码为 0。

11. **待测试**：模块边界回归
   - 变更内容：事件中心端口新增韩立语义 DTO 与持久化能力，人物层仅通过端口访问。
   - 测试命令：npm run test:boundaries
   - 预期结果：模块导入方向、数据库表归属与人物 Facade 边界检查全部通过。

# 韩立会话聚合根与独立 1 研讨重构测试登记

## 修改目标

- [x] 韩立会话状态收敛到 `HanliConversationAggregate`，不再由多个服务传递零散控制变量。
- [x] 韩立内部实现按 `application`、`conversation`、`decision`、`acceptance`、`semantic` 分组，根目录只保留门面和公开索引。
- [x] 当前会话存在韩立观点时，用户独立输入 `1` 直接启动韩立与南宫婉研讨，不依赖固定邀请文案。
- [x] 当前没有观点时返回明确反馈，不创建空研讨；已有活动研讨时返回原流程，不重复创建。
- [x] 启动异步研讨前先保存当前观点与已有调查事实，使 Workflow 可以从持久状态恢复。
- [x] 新增与调整的领域字段均补充中文业务注释；重点服务改回显式 `if/else`、`for` 和有含义的中间变量。
- [x] 韩立契约公开索引中的每个导出类型均拆成独立出口，并分别说明业务对象、用途和含义。

## 涉及范围

- `apps/ai-desktop/contracts/services/personas/hanli/value/hanli-conversation.value.ts`
- `apps/ai-desktop/electron/services/personas/hanli/domain/hanli-conversation.aggregate.ts`
- `apps/ai-desktop/electron/services/personas/hanli/hanli.facade.ts`
- `apps/ai-desktop/electron/services/personas/hanli/internal/**`
- `apps/ai-desktop/prompts/personas/hanli/conversation.md`
- 韩立会话、南宫演化、窗口验收、事件中心与模块边界测试。
- 当前用户 AI Desktop 架构边界规则和韩立用户代理提问规则。

## 待统一测试

- [x] `npm run typecheck`
  - 结果：通过；新目录导入路径、Aggregate 值对象和门面返回类型均无 TypeScript 错误，后续 Developer 构建再次通过。
- [x] `npm run test:boundaries`
  - 结果：14/14 通过；韩立纵向目录存在，旧平铺路径归零，Workflow 不导入人物 internal，公开索引不泄漏内部服务。
- [x] `npm run test:nangong-evolution`
  - 结果：51 通过、0 失败、2 项明确退役用例跳过；有当前观点、没有观点和已有活动研讨三个独立 `1` 分支全部通过，并验证启动前观点已保存。
- [x] `npm run build:electron && node scripts/run-with-dependencies.mjs node --test tests/services/workflow/hanli-inquiry.test.mjs tests/services/workflow/hanli-computer-acceptance.test.mjs tests/services/support/capabilities/event-center/workflow-event-center.test.mjs`
  - 结果：39/39 通过；Electron 主进程、规则包、提示词包和沙箱 preload 构建通过。
- [x] `npm test`
  - 结果：复测 346 通过、0 失败、2 项明确退役用例跳过；全部所有者测试通过。
- [x] `npm run test:interaction`
  - 结果：27/27 通过；完整 Developer Renderer 构建通过，正式生产文件在隔离 Electron 窗口中的人物会话和相邻交互无回归。
- [x] 正式开发实例启动与健康观察
  - 结果：`npm run start:developer` 完成完整构建，源码 Developer Electron 主进程 PID 60935 曾持续运行且未提前退出。未向用户真实人物会话写入测试消息；独立 `1` 的业务分支由专项服务测试验证，页面行为由隔离交互测试验证。
- [x] Developer 应用打包、校验与重启
  - 结果：`npm run package:mac:developer` 成功，应用完成本地稳定签名；`npm run verify:mac:developer` 与 `npm run verify:package-content` 均通过。
  - 结果：切换前未发现仍在运行的旧 AI Desktop 主进程；最新打包应用已启动，主进程 PID 63410 持续运行，启动参数包含当前 SELPLAT 根目录和 Developer 版本标识。

## 当前状态

当前状态：统一测试、Developer 应用打包、包内容校验与重启均已完成。全部登记的类型、边界、专项、完整所有者和真实交互测试通过，最新打包 Developer 实例已启动并保持运行。

## 统一测试修复记录

- [x] 架构边界首轮失败：静态测试仍固定要求架构规则 `2.9.0`；已更新为本轮真实版本 `2.11.0`，复测通过。
- [x] 韩立—南宫专项首轮失败：确定性控制反馈仍使用普通韩立消息标识，可能被 Aggregate 误认成新观点；已改为 `hanli-control:*`，专项复测通过。
- [x] 完整测试首轮失败：令狐页面静态测试仍断言旧方法名 `repairFailedUnifiedTest`；生产端口及服务测试已经统一使用 `repairTechnicalFailure`，已同步静态断言，未修改令狐生产行为。

## 规则沉淀评估

- 结果：升级现有规则，不新增近义规则。
- `AI_DESKTOP_ARCHITECTURE_BOUNDARY_RULES` 已吸收韩立纵向模块结构与可读性约束。
- `AI_DESKTOP_ARCHITECTURE_BOUNDARY_RULES` 同时补充逐个公开导出符号写业务注释的约束，禁止仅以分组总注释代替。
- `AI_DESKTOP_HANLI_USER_QUESTIONING_RULES` 已吸收会话 Aggregate、独立 `1`、先持久化后调度、空观点拒绝和重复启动幂等约束。
- 两条规则已有所属叶子索引和根索引可达入口，逻辑 ID 与文件路径未改变，因此不修改索引。

---

# 南宫婉会话聚合与人物 Codex 授权可见性测试登记

## 修改目标

- [x] 南宫婉会话按 `domain`、`internal/application`、`internal/conversation`、`internal/inquiry`、`internal/evolution`、`internal/distribution`、Facade 和公开索引组织。
- [x] 会话 ID、消息数量、当前邀请和一次性运行属性由 `NangongConversationAggregate` 一次恢复，不再由应用服务来回传递零散变量。
- [x] 用户独立输入 `1` 时只产生一个确定动作；当前邀请缺失、活动运行仍有执行人、孤儿运行和正常启动分别处理。
- [x] 南宫婉会话、南宫婉调查和韩立会话的长期 Codex 连接全部登记到统一授权路由。
- [x] 相同局部请求 ID 映射为不同全局请求 ID，允许或拒绝会准确返回原人物连接。
- [x] 南宫婉页面显示调查轮次、整理方案、等待韩立、阻塞原因或等待授权；授权仍使用现有全局弹窗。
- [x] 新增会话值对象、聚合字段、公开出口和授权归属字段均有中文业务注释。

## 已完成的专项验证

- [x] `npm run typecheck`
  - 结果：通过；南宫婉纵向目录导入、会话值对象、聚合根、授权归属 DTO 和 Renderer 属性无 TypeScript 错误。
- [x] `npm run test:boundaries`
  - 结果：14/14 通过；新目录与聚合根存在，旧平铺服务路径归零，人物 internal 边界未泄漏。
- [x] Electron 测试构建
  - 结果：主进程、公共包、当前用户规则包、提示词包和沙箱 preload 构建通过。
- [x] 南宫婉演化与人物授权路由专项测试
  - 结果：53 通过、0 失败、2 项明确退役用例跳过；覆盖独立 `1` 的五类动作以及人物连接相同局部请求 ID 不串线。
- [x] 南宫婉授权隔离页面用例
  - 结果：目标用例 1/1 通过；等待授权状态与原全局授权窗口同时出现，点击允许后同时解除。
- [x] 相邻交互回归观察
  - 结果：首次交互套件中原有 27 项全部通过；新增用例因测试先制造模态窗口再点击侧栏而超时，调整为先进入南宫婉页面再制造授权事实后，目标用例复测通过。该失败属于测试操作顺序，不是生产故障。

## 尚未执行

- [x] `npm test` 全量所有者统一测试。
  - 结果：复测 350 项，348 通过、0 失败、2 项明确退役用例跳过。
- [x] 完整 `npm run test:interaction` 28 项同轮复测。
  - 结果：28/28 通过；南宫婉授权状态卡、原全局授权弹窗及全部相邻交互在同一轮中通过。
- [x] Developer 应用重新打包、校验、关闭旧进程和重启。
  - 结果：`npm run package:mac:developer` 通过并完成稳定本地签名。
  - 结果：`npm run verify:mac:developer` 通过，应用身份、指定要求、内置 Codex 0.149.0、OpenAI 签名和隔离启动健康检查均有效。
  - 结果：`npm run verify:package-content` 通过；1241 个运行条目完整，没有源码、临时材料或跨语言泄漏。
  - 结果：旧主进程 PID 63889 已正常退出；最新打包实例 PID 93296 已启动并持续运行，启动参数包含当前 SELPLAT 根目录和 Developer 标识。

## 统一测试修复记录

- [x] 完整所有者测试首轮发现状态卡使用两个应用私有色值；已改为 SEL UI 的语义警告与错误令牌。
- [x] 完整所有者测试首轮发现旧静态断言仍要求三元表达式；已同步为本轮可读的显式 `if/else` 状态判断。

## 规则沉淀评估

- 结果：升级现有规则，不新增近义规则。
- `AI_DESKTOP_ARCHITECTURE_BOUNDARY_RULES` 升级至 2.12.0，加入南宫婉纵向人物模块结构。
- `AI_DESKTOP_HARNESS_WORKSPACE_RUNTIME_RULES` 升级至 5.111.0，加入所有人物长期 Codex 连接的统一授权与结构化提问路由。
- `AI_DESKTOP_EVENT_MEMORY_UI_RULES` 升级至 5.160.0，加入南宫婉内部研讨持久进度和实时授权状态投影。
- `AI_DESKTOP_NANGONG_ANALYSIS_PLANNING_RULES` 升级至 1.1.0，加入会话聚合、独立确认、孤儿运行恢复和授权等待契约。

# 本次测试文档

任务线程：01a05a57-d1f3-77b1-b05a-a15736ac1257

## 统一测试清单

1. **待测试**：TypeScript 类型检查
   - 变更内容：南宫 Runtime 装配参数、内部服务端口和 Workflow 兼容委托发生变化
   - 测试命令：cd apps/ai-desktop && npm run typecheck
   - 预期结果：tsc 无类型错误，所有新增导入和回调类型可解析
2. **待测试**：南宫演化业务回归
   - 变更内容：对话、草稿、一次性确认、提案返修及证据判断迁入南宫人物模块
   - 测试命令：cd apps/ai-desktop && npm run test:nangong-evolution
   - 预期结果：Electron 构建成功，nangong-evolution.test.mjs 全部通过且行为不回退
3. **待测试**：人物、核心业务区与 Support 架构边界
   - 变更内容：Workflow 改为通过南宫公开 Runtime 装配人物内部应用服务，并新增 internal 纯模块
   - 测试命令：cd apps/ai-desktop && npm run test:boundaries
   - 预期结果：module-boundaries.test.mjs 全部通过，Workflow 不再拥有南宫解析和返修调查实现
4. **待测试**：Workflow 南宫兼容入口归零
   - 变更内容：删除 PersonaEvolutionRuntime 八个南宫公开方法，主进程和内部状态机改为通过 NangongFacade 调用，测试增加防回退门禁
   - 测试命令：cd apps/ai-desktop && node --test tests/module-boundaries.test.mjs
   - 预期结果：边界测试全部通过，生产 PersonaEvolutionRuntime 不再声明任何南宫公开业务方法

5. **待测试**：南宫内部能力拆分回归
   - 变更内容：NangongApplicationService 收缩为委派层，会话与一次性确认、专题与提案作者分别进入独立内部服务，跨模块动作改为具名 Port
   - 测试命令：cd apps/ai-desktop && npm run typecheck && npm run test:nangong-evolution && npm run test:boundaries
   - 预期结果：类型检查通过，南宫对话、一次性确认、提案返修行为保持一致，Workflow 不读取南宫 internal 且全部边界门禁通过

6. **待测试**：韩立人物能力归属回归
   - 变更内容：新增 HanliApplicationService，内部组装研讨、审批和验收能力；Workflow 改为只依赖 HanliWorkflowPort，并删除韩立公开业务代理与内部服务工厂出口
   - 测试命令：cd apps/ai-desktop && npm run typecheck && npm run test:nangong-evolution && npm run test:boundaries
   - 预期结果：类型检查通过，韩立研讨、人工与自动审批、验收计划和结果判断行为保持一致，PersonaEvolutionRuntime 不再声明韩立业务方法且人物 index 不导出内部服务工厂

7. **待测试**：Contracts DTO 全量类型检查
   - 变更内容：collaboration 按南宫婉、韩立、令狐、Evolution、Workflow 拆分方向 DTO，公开类型和全部调用方完成重命名
   - 测试命令：cd apps/ai-desktop && npm run typecheck
   - 预期结果：tsc 无类型错误，所有显式 index 导出、跨域导入、DesktopApi 和 Renderer 类型均可解析

8. **待测试**：Contracts 显式出口与主进程导入边界
   - 变更内容：删除 contracts 通配符导出、Evolution 与 Workflow 总协议，主进程不再依赖 Desktop 聚合出口
   - 测试命令：cd apps/ai-desktop && node --test tests/module-boundaries.test.mjs tests/collaboration-status-chain-contract.test.mjs
   - 预期结果：全部静态边界用例通过，旧文件和旧类型归零，主进程只从目标领域 index 导入

9. **待测试**：人物演化与协作业务回归
   - 变更内容：南宫婉、韩立、Evolution、Workflow 的 DTO 名称和来源变更，运行逻辑保持原有职责
   - 测试命令：cd apps/ai-desktop && npm run test:nangong-evolution && npm run test:collaboration
   - 预期结果：人物对话、提案、审批、分发、恢复、统一测试和验收业务用例全部通过

10. **待测试**：Sandbox preload 协议回归
   - 变更内容：DesktopApi 请求、返回和事件类型改为方向 DTO，preload 仍通过单文件白名单桥接
   - 测试命令：cd apps/ai-desktop && npm run test:preload-sandbox
   - 预期结果：Electron 构建成功，sandbox 模式下所有代表能力可调用且事件订阅正常

11. **待测试**：真实桌面完整流转交互
   - 变更内容：Renderer 使用重命名后的 DTO 和显式 Desktop 聚合出口，新增 ARCHITECTURE 导航但不改变界面业务行为
   - 测试命令：cd apps/ai-desktop && npm run test:interaction
   - 预期结果：真实 Electron 中南宫婉提案、韩立审批、Workflow 分发、令狐保障和状态回流交互通过，失败时保留证据

12. **待测试**：AI Desktop 架构规则显式引用门禁
   - 变更内容：升级现有 AI_DESKTOP_ARCHITECTURE_BOUNDARY_RULES，登记显式 index 出口和主进程导入来源约束
   - 测试命令：cd apps/ai-desktop && npm run test:rules && npm run test:boundaries
   - 预期结果：规则入口从根索引可达、生产规则包可加载，contracts 显式出口和主进程目标 index 门禁全部通过

13. **待统一测试**：通用 Executor 人物模块类型与运行时回归
   - 验证内容：所有动态执行成员共用 `personas/executor`，Workflow 不再保存会话工厂和执行会话 Map
   - 测试命令：cd apps/ai-desktop && npm run typecheck && npm run test:collaboration
   - 预期结果：Executor Contracts、Facade、Codex 基础设施实现类型一致，执行、取消、恢复和令狐临时修复会话行为保持一致

14. **待统一测试**：南宫婉任务拆分与分发边界回归
   - 验证内容：任务拆分服务位于 `personas/nangong/internal`，IPC 经 NangongFacade 分发，幂等 mutation 仍完整生效
   - 测试命令：cd apps/ai-desktop && npm run test:nangong-evolution && npm run test:boundaries
   - 预期结果：提案拆分、确定性冲突校验、执行人偏好选择、任务提交和状态回流全部通过

15. **已完成静态检查**：Executor/Nangong/Workflow 模块边界
   - 验证内容：新增目录、旧分发服务归零、Workflow 会话 Map 归零、IPC 调用方向和 Contracts 显式出口
   - 测试命令：cd apps/ai-desktop && node --test tests/module-boundaries.test.mjs tests/collaboration-status-chain-contract.test.mjs
   - 实际结果：14/14 通过；`git diff --check` 通过
   - 说明：本项仅为静态边界检查，不替代统一测试和完整构建

16. **已完成静态检查**：Electron 入口与七层 Bootstrap 边界
   - 验证内容：`main.ts` 只保留生命周期，Startup、Persistence、Capabilities、Collaboration、Personas、IPC 和应用运行时均有独立入口
   - 测试命令：cd apps/ai-desktop && node --test tests/module-boundaries.test.mjs
   - 实际结果：12/12 通过，新增薄入口与 bootstrap 文件存在性、防初始化回迁门禁通过
   - 说明：本项是本轮允许的中间测试，不替代统一测试

17. **待统一测试**：Bootstrap TypeScript 类型检查
   - 验证内容：StartupContext、PersistenceContext、CapabilityContext、CollaborationContext、PersonaApplicationContext 和 IPC Port 的类型装配
   - 测试命令：cd apps/ai-desktop && npm run typecheck
   - 预期结果：tsc 无错误，所有跨层回调、Facade、Repository 和生命周期类型可解析

18. **待统一测试**：Electron 构建与启动释放顺序
   - 验证内容：应用启动按 bootstrap 顺序完成，退出与测试数据重置逆序停止 watcher、supervisor、人物运行时、Codex 和数据库
   - 测试命令：cd apps/ai-desktop && npm run build:electron && npm run test:preload-sandbox
   - 预期结果：Electron 主进程和 preload 构建成功，IPC 白名单可用且无资源重复初始化或重复释放

19. **待统一测试**：重构后的完整人物流转
   - 验证内容：南宫分发、通用执行人执行、令狐统一测试、韩立验收以及状态事件回流不因组合根拆分而改变
   - 测试命令：cd apps/ai-desktop && npm run test:nangong-evolution && npm run test:collaboration && npm run test:interaction
   - 预期结果：人物与协作业务回归、真实 Electron 完整流转全部通过

20. **待统一测试**：Electron system/services 目录迁移类型检查
   - 验证内容：system 下 bootstrap、config、ipc、preload、policies、window 以及 services/support/application 的全部相对导入和编译输出路径
   - 测试命令：cd apps/ai-desktop && npm run typecheck && npm run build:electron
   - 预期结果：TypeScript 与 preload 构建成功，主进程从 system/bootstrap 启动，preload 产物位于 system/preload/preload.cjs

21. **已完成静态检查**：Electron 两区目录和旧路径归零
   - 验证内容：electron 根仅保留入口、README、system 和 services，旧 bootstrap/config/ipc/preload/policies/window/application 目录不存在
   - 测试命令：cd apps/ai-desktop && node --test tests/module-boundaries.test.mjs
   - 预期结果：README、两区目录、薄入口、公开依赖和旧路径防回退门禁全部通过
   - 实际结果：2026-09-01 聚焦执行通过，`module-boundaries` 与 `collaboration-status-chain-contract` 共 15/15；旧路径扫描结果为零。该结果不替代统一测试

22. **待统一测试**：system IPC、preload 与真实 Electron 交互
   - 验证内容：DesktopApi、preload 白名单、IPC domain、窗口 preload 路径和隔离交互夹具完成路径迁移
   - 测试命令：cd apps/ai-desktop && npm run test:preload-sandbox && npm run test:interaction
   - 预期结果：所有代表能力可调用，真实窗口加载迁移后的 preload 且人物完整流转不回退

23. **已完成静态检查**：主进程拆分后的既有契约回归
   - 验证内容：开发包根、受管任务执行、SEL UI、测试文档、用户输入与截图契约改为读取新的 system/bootstrap 组合根
   - 测试命令：cd apps/ai-desktop && node --test --test-reporter=dot tests/user-input-and-screenshot-contract.test.mjs tests/test-document-and-mac-launcher-contract.test.mjs tests/sel-ui-adoption.test.mjs tests/managed-task-executor-contract.test.mjs tests/developer-package-root.test.mjs
   - 实际结果：2026-09-01 聚焦执行通过，51/51；该结果不替代待统一测试中的构建和真实 Electron 交互

13. **待测试**：Support 父目录迁移类型检查与 Electron 构建
   - 变更内容：application、capabilities、platform 新增 support 层级，全部 TypeScript 相对导入和构建产物路径同步迁移
   - 测试命令：cd apps/ai-desktop && npm run typecheck && npm run build:electron
   - 预期结果：TypeScript 和 Electron 构建成功，产物仅从 electron/services/support 下加载三类支撑能力

14. **通过**：Services 顶层与 Support 防回退边界
   - 变更内容：services 顶层只保留 personas、evolution、workflow、support，原三个顶层目录必须不存在
   - 测试命令：cd apps/ai-desktop && node --test tests/module-boundaries.test.mjs tests/collaboration-status-chain-contract.test.mjs
   - 预期结果：目录、公开 index、依赖方向和旧路径防回退断言全部通过
   - 实际结果：2026-09-01 聚焦执行通过，module-boundaries 与 collaboration-status-chain-contract 共 15/15；services 顶层和 support 防回退断言生效。
15. **通过**：Support 迁移后的既有主进程契约回归
   - 变更内容：测试夹具、构建产物导入及主进程装配引用全部增加 support 路径层级
   - 测试命令：cd apps/ai-desktop && node --test tests/developer-package-root.test.mjs tests/managed-task-executor-contract.test.mjs tests/model-settings-contract.test.mjs tests/test-document-and-mac-launcher-contract.test.mjs tests/user-input-and-screenshot-contract.test.mjs
   - 预期结果：既有开发包、执行、设置、测试文档、用户输入与截图静态契约全部通过
   - 实际结果：2026-09-01 聚焦执行通过，开发包、受管执行、设置、测试文档、用户输入与截图静态契约共 44/44。

16. **已完成静态检查**：Contracts module boundaries
   - 变更内容：Contracts owner mirror and role gates
   - 测试命令：cd apps/ai-desktop && node --test tests/module-boundaries.test.mjs
   - 预期结果：13 tests pass with zero failures
   - 实际结果：13/13 passed; zero failures
   - 说明：聚焦静态边界检查，不替代统一构建和真实 Electron 回归

17. **已完成静态检查**：Contracts focused semantic contracts
   - 变更内容：Renamed DTO and Value references
   - 测试命令：cd apps/ai-desktop && node --test tests/model-settings-contract.test.mjs tests/managed-task-executor-contract.test.mjs tests/user-input-and-screenshot-contract.test.mjs tests/collaboration-status-chain-contract.test.mjs
   - 预期结果：36 tests pass with zero failures
   - 实际结果：36/36 passed; zero failures
   - 说明：聚焦协议语义检查，不替代统一构建和真实 Electron 回归

18. **失败**：AI Desktop TypeScript typecheck
   - 变更内容：All Contracts paths and public type names
   - 测试命令：cd apps/ai-desktop && npm run typecheck
   - 预期结果：TypeScript exits 0
   - 实际结果：依赖准备脚本在进入 tsc 前因 Windows 依赖缓存路径不存在而失败；未取得类型检查结论

19. **待测试**：AI Desktop unified build and business regression
   - 变更内容：Contracts migration across main preload renderer and personas
   - 测试命令：cd apps/ai-desktop && npm run build:developer && npm run test:nangong-evolution && npm run test:collaboration
   - 预期结果：Build and business suites pass

20. **待测试**：Governance 拆分方案与源码所有者一致性
   - 变更内容：新增 Governance 协议按所有者拆分方案文档
   - 测试命令：人工逐项核对方案第 3、5、6、9、10 节与 contracts/governance、WorkflowRepository、WorkflowSupervisor、EventCenterFacade、BusinessAuditLog 当前源码
   - 预期结果：每个协议均有唯一真实所有者，迁移映射、依赖方向、七步实施和验收标准无遗漏且不把方案表述为已实施

21. **待测试**：AI Desktop 架构规则 2.4.0 索引可达性
   - 变更内容：升级现有架构边界规则，禁止没有真实实现所有者的 Governance 顶层主题目录
   - 测试命令：python apps/ai-desktop/ruleengine/python/local/core/abilities/layered_rule_loader.py <validate_user_index 与 load AI_DESKTOP_ARCHITECTURE_BOUNDARY_RULES 的 UTF-8 JSON 上下文>
   - 预期结果：用户索引验证成功，逻辑 ID 唯一加载到 XUNAN 层 2.4.0，生效值包含 owner resolution 和 no ownerless governance theme root

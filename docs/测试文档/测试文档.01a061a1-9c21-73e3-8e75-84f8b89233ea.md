# 韩立托管退役与人物会话语义边界测试

## 本轮新增待测项

- [x] 构建 AI Desktop：`npm run build` 通过，TypeScript、Electron 与 Developer renderer 构建完成。
- [x] `workflow-event-center.test.mjs`：21/21 通过；用户与韩立的完整回合生成训练语料，人物内部消息只保留业务记录，不生成或领取语义资料。
- [x] 旧演化仓储同步：韩立历史审议仍写入审议、快照与归档表，但统一训练主题和训练消息均为 0。
- [x] AI Desktop 协作与规则契约：54/54 通过；当前用户规则索引校验通过（33 个索引、108 条规则）。

## 统一测试状态

当前状态：本轮相关统一测试通过。

## 执行说明

- 首次直接运行 Node 测试时因未加载工程共享依赖，报 `ERR_MODULE_NOT_FOUND: @selplat/node-common-core`；随后使用工程自带 `scripts/run-with-dependencies.mjs` 入口重跑，同一测试全部通过。该失败属于启动方式错误，不是业务断言失败。

## 韩立—南宫婉内部研讨升级

- [x] 韩立在需求成熟后发出固定邀请，只有用户独立输入 `1` 才启动内部研讨。
- [x] 完整连通“韩立提问 → 南宫婉回答 → 韩立判断/继续追问”；目标、范围、证据和验收条件成熟后自动确立专题并进入现有完整流程。
- [x] 连续自动开关开启时，单题轮数或纠偏上限不再自行关闭开关；无新的用户证据时保持监测，不虚构问题。
- [x] 内部的韩立、南宫婉消息保留独立发言人和回复关系，只写业务会话记录，不生成用户语义资料。
- [x] 数据库增量迁移至 `1021`，新增真实发言人字段；首次初始化、重复启动、校验和回滚用例通过。
- [x] `npm run build` 通过；定向服务回归 133 项中 131 通过、0 失败、2 项已退役历史用例跳过。
- [x] Electron 真实页面交互用例 1/1 通过：韩立页输入 `1` 后可见“南宫婉 · 内部研讨”及独立回答。
- [x] 用户规则索引校验通过：33 个索引、108 条规则。

## 本轮异常记录

- 首次 Electron 交互测试在受限环境启动时被系统以 `SIGABRT` 终止，未进入功能断言；改为允许启动本地 Electron 测试窗口后，目标用例通过。

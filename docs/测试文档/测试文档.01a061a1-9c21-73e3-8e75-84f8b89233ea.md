# 韩立、南宫婉统一人物会话升级测试

## 2026-09-04 执行池五项升级（统一测试通过）

- [x] 修复说明 → 韩立真实 1 → 启动回执；非 1 继续研讨；暂停不越过确认门禁；重启复用确认事实。
- [x] 人物当前阶段与协作群一致；修复次数、测试次数独立保留；令狐内部修复不伪造南宫婉转发。
- [x] 测试通过后显示等待重启健康检查与验收，不返回分发。
- [x] 启动协同模式，执行列表不兼容退役，协作群保留完整任务记录入口。
- [x] 正式 SELUI 多页签切换、重复定位、关闭相邻选择、保留输入和后台任务。
- [x] `npm run typecheck`、`npm test`、`npm run test:interaction`、打包与健康检查全部通过；失败修复后复测。
- [x] 当前用户规则升级及索引校验；修改和验证结果如实记录。

## 测试目标

- 验证韩立与南宫婉通过同一人物会话契约、IPC 和 Renderer Hook 接入，并由 `personaId` 隔离各自会话。
- 验证人物完整原话只保存在统一会话表，专题演化 JSON 不再复制第二份会话正文。
- 验证用户与人物的完整回合仍可进入训练语料；人物内部交流不冒充用户语义资料。
- 验证专题群发言使用统一人物消息入口，并把消息引用与短预览回流既有专题档案。
- 验证升级后的设置、截图、托管、协作、专题工作台及真实 Electron 页面没有回归。

## 修复记录

- [x] 定位 AI Memory 停用原因为 1023 替换主题表时未延迟外键检查；数据库文件、路径标记和完整性均正常。
- [x] 1023 迁移完整重建主题表的外键依赖链，使主题、语义提取、需求轨迹和需求节点在同一事务中原子接管稳定表名，全程保持外键保护开启。
- [x] 新增带语义提取状态、需求轨迹和需求节点真实引用数据的 1022 → 1023 回归测试，验证资料保留、外键完整及未来人物来源可写。
- [x] 补齐 Evolution 一次性确认和工作台事件的空会话标识保护。
- [x] 补齐 Codex 模型目录 `description` 输出字段，并修正异步回调的返回类型。
- [x] 将统一人物会话仓储收敛到 `support/capabilities/conversation`，人物业务不直接操作 SQLite，底层 Platform 不反向依赖人物协议实现。
- [x] 恢复并更新当前架构说明，明确统一人物会话、公开入口和测试所有权。
- [x] 主 Codex 会话控件使用独立 ID，不再沿用韩立控件 ID。
- [x] 将旧的专属南宫 DTO、旧 Renderer 文件位置和旧数据库版本断言更新为当前统一契约。
- [x] 修正隔离交互夹具，使专题群消息携带统一 `subject`、真实消息引用及短预览。

## 统一测试结果

- [x] `npm run test:database-foundation`：数据库基础测试 12/12 通过；包含 1022 → 1023 真实外键引用升级，Electron 38.8.6 / Node 22.22.0 SQLite 冒烟通过。
- [x] `npm test`：279 项；277 通过，0 失败，2 项为明确退役的历史兼容用例。
- [x] 现有数据库副本升级：1022 的 65.5 MB 数据库副本成功升级至 1023；主题、消息、提取状态、需求轨迹和需求节点数量分别保持 3650、7626、1420、1031、1664，外键检查为空且完整性为 `ok`。
- [x] 应用重启验收：正式开发启动器完成构建、打包、签名和旧实例切换；真实界面状态栏显示 `AI Memory v1023 · 统一事件中心`，停用告警消失。
- [x] `npm test`：278 项；276 通过，0 失败，2 项为明确退役的历史兼容用例。
- [x] `npm run test:database-foundation`：数据库基础测试 11/11 通过；Electron 38.8.6 / Node 22.22.0 的 `node:sqlite` 运行时冒烟通过。
- [x] `npm run test:interaction`：真实 Electron 页面交互 24/24 通过。
- [x] `npm run typecheck`：通过（同时由交互测试的完整 Developer 构建再次执行）。
- [x] `npm run build:developer`：TypeScript、Electron、preload、规则包、提示词包和 Developer Renderer 构建通过（由交互测试执行）。
- [x] 架构边界 14/14、SEL UI 13/13、托管集合 42/42、模型设置 6/6 均通过。

## 异常与处置

- 首轮新增回归证明仅延迟外键检查仍会在提交时失败；改为完整重建主题表的外键依赖链后，数据库专项测试、真实数据副本升级和完整统一测试全部通过。
- 首轮构建发现 5 个升级后的类型缺口，已修复并重新构建通过。
- 首轮所有者测试有 8 个失败：7 个是代码拆分或契约统一后的旧位置断言，1 个是专题群测试仍传旧 `topicId`；均按当前公共契约更新并回归通过。
- 首轮真实交互有 2 个旧页面标识断言；后续发现隔离夹具的专题回流标题与 payload 未同步生产契约，修正后 24 项全部通过。
- 共享测试队列中的 7 个历史批次未删除、未修改，也未混入本轮结果。

## 最终状态

当前统一测试状态：通过。AI Memory 已恢复为 1023，现有会话、训练语料、语义提取状态和需求轨迹均保留，可以作为韩立、南宫婉统一人物会话升级的验证基线。

## 四开关退役与回复 1 持续自动演化

### 修改记录

- [x] 退役“自动发现并推进专题、南宫婉提案自动审批、令狐修复提案自动审批、审批通过后自动分发”四个开关及其 Renderer、Desktop API、preload、IPC、Workflow Facade 和状态协议入口。
- [x] 用户在韩立或南宫婉人物会话回复独立 `1` 后，统一运行状态立即进入 `running`，审批、分发、测试和验收固定沿同一自动链路推进。
- [x] 当前专题完成后继续寻找有用户证据的新问题；没有新证据时等待，不虚构专题。
- [x] 暂停、停止和人工接管会冻结当前专题；继续时从同一运行卡点恢复，阻塞专题优先调用原位恢复入口。
- [x] 历史状态中的四个布尔字段仅在加载时清理，不再参与业务判断，也不再返回给页面。
- [x] 同步升级韩立、南宫婉邀请文案、人物提示词和当前用户演化规则。

### 已执行即时门禁

- [x] `npm run typecheck`：通过。
- [x] `npm run start:developer`：TypeScript、Electron、preload、规则包、提示词包和 Renderer 构建通过；最新开发实例启动后持续运行，未提前退出。

### 待统一测试

- [x] `npm run test:nangong-evolution`：2026-09-04 由 `npm test` 覆盖同一测试文件；原待测范围：验证回复 `1` 启动持续运行、统一审批分发、暂停/阻塞/恢复及旧字段清理。
- [x] `npm run test:workflow-event-center`：2026-09-04 由 `npm test` 覆盖同一测试文件；原待测范围：验证演化状态和审批事实仍可正确投影。
- [x] `npm run test:interaction`：2026-09-04 完整生产页面交互通过；原待测范围：真实 Electron 页面确认四个开关和树节点消失，仅保留继续、暂停、停止与人工接管。
- [x] `npm test`：2026-09-04 已执行全部所有者测试；原待测范围：执行 AI Desktop 全部所有者测试，确认接口退役没有相邻回归。

当前新增修改状态：即时构建与启动冒烟通过，完整统一测试待用户明确触发。

## 演化工作台不兼容退役

### 修改记录

- [x] 删除演化工作台入口、独立窗口、路由、树导航、数据库列表、专题群、提案详情、人物审批面板及工作台专属样式。
- [x] 删除工作台 Desktop API、preload、IPC、查询/偏好协议、轻量变更事件和兼容窗口逻辑，不保留重定向、占位页或别名。
- [x] 保留人物会话触发的共享演化运行态、专题/提案/审批/验收数据及韩立—南宫婉自动流程。
- [x] 新增 1024 迁移，现有数据库物理删除 `AiDesktopEvolutionWorkbenchPreference`；已发布 1000 SQL 保持原校验和，新库在同一次初始化末尾由 1024 删除该表。
- [x] 升级架构边界与事件记忆规则，明确工作台不得兼容恢复。

### 已执行即时门禁

- [x] `npm run typecheck`：通过。
- [x] 定向契约与数据库测试：37/37 通过。
- [x] 当前用户规则索引：33 个索引、109 条规则，校验通过。
- [x] `npm run start:developer`：TypeScript、Electron、preload、规则包、提示词包和 Renderer 构建通过，正式开发实例持续运行。
- [x] 真实 AI Memory 数据库升级到 1024；旧工作台偏好表数量为 0，`PRAGMA quick_check` 为 `ok`。

### 统一测试结果

- [x] `npm test`：273 项；271 通过、0 失败，2 项为明确退役的历史兼容用例。
- [x] `npm run test:nangong-evolution`：47 项；45 通过、0 失败，2 项为明确退役的历史兼容用例。
- [x] `npm run test:workflow-event-center`：19/19 通过，审批、分发、验收、恢复和人物语义事实继续正确投影。
- [x] `npm run test:database-foundation`：13/13 通过；Electron 38.8.6 / Node 22.22.0 的 SQLite 运行时验证通过。
- [x] `npm run test:interaction`：真实 Electron 页面交互 24/24 通过，工作台入口与页面归零，人物自由会话和任务协作页面正常。
- [x] `npm run typecheck` 与完整 Developer 构建：TypeScript、Electron、preload、规则包、提示词包和 Renderer 全部通过。
- [x] 当前用户规则索引复验：33 个索引、109 条规则，校验通过。
- [x] 真实数据库复验：版本 1024，旧工作台偏好表数量为 0，`PRAGMA quick_check` 为 `ok`。
- [x] 统一测试后重启：最新 Developer 实例重新完成全量构建并持续运行，未出现数据库停用或提前退出。

### 统一测试修复记录

- [x] 删除侧栏测试对已退役工作台动作样式的旧断言，改为确认不存在工作台入口和兼容动作。
- [x] 更新南宫婉未成熟回复 `1` 的提示断言，由旧“一次性演化”统一为“自动演化”。
- [x] 清理规则和规则测试中仍要求工作台树、独立窗口、审批面板、专题群及自动控制侧栏的旧约束。
- [x] 演化持久化规则改为人物自由会话是唯一 Renderer 演化入口，审批、分发、恢复和验收由共享后端流程处理。

当前退役状态：统一测试全部通过，数据库保持 1024 且无工作台偏好表；不兼容退役完成。

## 韩立调查方法学习与上下文字数可观测

### 修改记录

- [x] 韩立历史语义资料改为只读取“信息缺口—提问理由—调查动作—扩展方向”方法链。
- [x] 客户目标、证据原文、历史答案和需求节点正文不再进入韩立对话提示词，不按相似问题模仿旧结论。
- [x] 方法资料限制为 6000 字符，近期会话限制为 4000 字符；用户原话保留，人物长回答在后续上下文中只取前 80 字。
- [x] Codex 运输层不再使用 20000 字符的用户输入限制拦截已拼装的内部提示词；韩立入口仍独立校验用户消息长度。
- [x] 每轮返回方法资料、当前会话、本轮问题和完整发送上下文字数；页面可见，同时写入统一事件供后续优化。
- [x] 升级当前用户韩立提问规则，固化“学习方法、不模仿类似答案、回显真实字数”边界。

### 待统一测试

- [x] `npm run test:nangong-evolution`：48 项，46 通过、0 失败、2 项为明确退役兼容项；方法资料预算、业务原文隔离、人物回答 80 字预览、返回字数与事件字数一致均通过。
- [x] `npm run typecheck`：人物会话统计协议、韩立服务和 Renderer 类型连接通过。
- [x] `npm run start:developer`：Electron 主进程、preload、规则包、提示词包和 Developer Renderer 完整构建通过；旧单实例已结束，新进程 PID 9991 持续运行。
- [x] `npm run test:interaction`：2026-09-04 完整生产页面交互通过；原待测范围：启动真实 Electron 页面，发送韩立消息并检查读取字数回显的实际布局与交互状态，保留截图或结果日志。
- [x] `npm test`：2026-09-04 已执行全部所有者测试；原待测范围：执行 AI Desktop 全部所有者测试，确认 Codex 运输校验调整没有引入相邻回归。

当前状态：类型检查和定向回归已通过；真实页面交互与全部所有者测试待用户明确“统一测试”后执行。

## 人物切换保留发送状态与内部研讨双页面实时显示

- [x] 韩立和南宫婉的待完成消息、失败状态、发送锁及附件预览提升到应用层人物控制器。
- [x] 内部消息提交后经 typed Desktop API / preload 推送同一权威会话快照；南宫婉只读投影韩立会话的内部消息，不新增数据库副本或训练语料。
- [x] 入库快照与本地消息按稳定 ID 去重；发送过程中禁止新建人物会话。
- [x] `npm run typecheck`：通过。
- [x] `npm run test:nangong-evolution`：48 项，46 通过、0 失败、2 项已退役跳过；每轮提问、回答和判断分别发布快照的断言通过。
- [x] `npm run test:interaction`：24 项全部通过；发送中切到南宫婉再返回韩立时消息与发送锁保留，两个人物页面显示同一研讨问答。已保存并人工检查 shared-persona-deliberation.png，内部提问与回答均可见。
- [x] 当前用户规则索引复验：33 个索引、109 条规则，通过；`git diff --check` 无差异格式错误。
- [x] `npm test`：2026-09-04 已执行全部所有者测试；原待测范围：统一测试时执行全部所有者测试，确认接口白名单和统一会话没有相邻回归。

规则沉淀评估：升级既有 AI_DESKTOP_EVENT_MEMORY_UI_RULES，补充页面切换状态所有权与内部消息提交后双页面通知约束，不新增近义规则。

## 内部研讨仅在南宫婉页面展示（替代上节双页面展示要求）

- [x] 韩立页面只显示用户与韩立直接对话、用户对韩立判断行为的纠正；内部研讨问题、回答和判断全部排除，不删除原始记录。
- [x] 南宫婉页面保持内部研讨实时展示，后端流程与存储不变。
- [x] `npm run test:interaction`：24 项通过；韩立内部问答不可见、直接对话保留；南宫婉内部问答可见；发送中切换状态保留。构建及类型检查通过，已查看 hanli-direct-conversation-only.png 截图。
- [x] 当前用户规则索引校验通过：33 个索引、109 条规则。
- [x] `npm test`：2026-09-04 已执行全部所有者测试；原待测范围：待统一测试，验证全部所有者测试无相邻回归。

规则沉淀评估：升级既有 AI_DESKTOP_EVENT_MEMORY_UI_RULES，替换错误的双方展示约束为南宫婉单页面展示；索引路径及逻辑 ID 不变。

## 内部研讨自然对话与后台判断分离

- [x] 调整三份人物提示词，允许回应、质疑、反问与纠正，不强制检查清单或报告格式。
- [x] 后台 assessment 继续保存和驱动成熟度判断，但不生成聊天消息；追问直接回复南宫婉上一条消息，成熟后使用独立 reply 正文。
- [x] 历史 assessment 消息不删除，仅从南宫婉聊天投影排除。
- [x] `npm run test:nangong-evolution`：48 项，46 通过、2 项退役跳过；继续讨论回复关联、判断留存但不外显、缺少自然收束回复时拒绝推进并可重试均通过。
- [x] `npm run typecheck`：通过，服务解析与页面投影类型有效。
- [x] `npm run test:interaction`：24 项通过；历史 assessment 报告不展示、正常内部对话仍可见，已查看 shared-persona-deliberation.png 截图。
- [x] 当前用户规则索引验证通过：33 个索引、109 条规则；`git diff --check` 通过。
- [x] `npm test`：2026-09-04 全部所有者测试通过。真实模型措辞的主观自然度未冒充自动测试结论，本轮未向真实人物业务会话发送测试消息。

规则沉淀评估：升级既有 AI_DESKTOP_EVENT_MEMORY_UI_RULES，增加自然交谈与后台判断分离约束，未新增近义规则。


## 2026-09-04 最终验收记录

- `npm test`：278 项，276 通过、0 失败、2 项原有退役历史用例跳过。新增确认门禁/非 1 追问/暂停/持久恢复、三次测试两次修复、健康检查后仍等待韩立验收、全人物中间状态和启动协同模式回归。
- `npm run test:interaction`：25/25 通过；正式生产文件与 Electron 窗口验证。SELUI 页签草稿保留、重复定位、关闭相邻选择、关闭不删除任务均通过；已查看 selui-workspace-tabs.png，关闭符号可见。
- `npm run test:sel-ui-shared`：公共控件正式出口及源码边界门禁通过。
- `npm run package:mac:developer`：完整类型检查、Electron/Renderer/规则/提示词构建、打包与稳定开发签名通过。
- `npm run verify:mac:developer`：应用身份、内置 Codex 官方签名及隔离启动健康检查通过。
- `npm run verify:package-content`：1194 个运行条目通过，无源码、临时数据或跨语言内容泄漏。
- 当前用户规则索引：33 个索引、109 条规则，通过；`git diff --check` 通过。
- 新包位于 `build/ai-desktop/package/developer/mac-arm64/AI Desktop.app`。未主动替换用户会话或重启当前运行实例；新进程加载新代码。
- 执行列表页面与专属路由、状态和样式已删除；任务引擎、会话、任务历史及完整结果保留，入口在任务协作群。删除源码可由 Git 历史恢复，不建立运行时兼容入口。

失败修复闭环：修正类型错误、旧入口与文案断言、隐藏页签导致的旧测试多输入框选择、关闭图标字体缺失、历史初始时间遮挡真实等待状态。每项修正后均复测；最终无失败。修复次数和测试次数来自真实开始事件，不使用全局集成批次冒充轮次；旧审计事实不删除。

规则沉淀结论：升级既有事件记忆、协作自动化、Harness 工作区三条当前用户规则，分别固定真实确认门禁与语言职责、逐人物阶段和修复复测历史、SELUI 多页签及退役入口边界；不新增近义规则，不修改 core/common。保留各规则现有内容并递增自身版本（5.153.0、5.126.0、5.109.0）。

# AI Desktop 沙箱桥接与全按钮恢复测试文档

- threadId：`01a042e7-eb08-7593-9b42-411e562d9099`
- 工程：`apps/ai-desktop`
- 状态：第 11 节一键清空测试数据与演化单树工作台已完成统一测试；全部已登记 macOS 门禁通过，Windows 环境验证按用户要求不纳入本轮
- 最新统一测试完成时间：`2026-08-28T03:26:14+0900`

## 待测内容

### 1. TypeScript 与 Electron 构建

- [x] `npm run typecheck` — 统一测试复跑通过。
- [x] `npm run build:electron` — 统一测试复跑通过；单文件 preload 产物约 `10.1kb`。
- 预期：类型检查通过；领域 preload 源码被打包为单一 `build/ai-desktop/electron/electron/preload.cjs`，构建产物不再通过相对 `require` 加载 preload 子文件。

### 2. 真实 Electron 沙箱桥接

- [x] `npm run test:preload-sandbox` — 统一测试通过，真实沙箱暴露 `99` 项能力。
- 预期：`sandbox: true`、`contextIsolation: true`、`nodeIntegration: false` 下成功暴露 `window.desktop`；系统、工作区、登录、截图、协同、会话和窗口控制七类代表能力全部存在。

### 3. 模块与安全边界

- [x] `npm run test:boundaries` — 统一测试通过，`7` 项通过、`0` 项失败。
- 预期：领域源码仍保持拆分，构建入口固定执行单文件 bundling，Electron 只作为外部模块保留，Renderer 在桥接缺失时显示阻断提示而非继续展示失效按钮。

### 4. 生产界面按钮相邻回归

- [x] `npm run test:interaction` — 统一测试通过，`21` 项通过、`0` 项失败、`0` 项跳过、`0` 项不稳定。
- 预期：隔离 Electron 改为真实沙箱后，工作区、任务、登录、设置、截图、新建任务、协同及窗口相关既有交互全部通过；登录按钮仍显示“请在浏览器中完成登录”。
- 修复过程：首次真实沙箱运行发现旧夹具依赖 `node:path`，阻断页按预期显示并留下失败截图；夹具改由主进程传入已解析工程根后，针对性复测与完整统一测试均通过。
- 针对性证据：`OPTION/temp/ai-desktop/临时材料/测试证据/interaction/preload-bridge-01a042e7-r2/result.json`。
- 统一测试证据：`OPTION/temp/ai-desktop/临时材料/测试证据/interaction/preload-bridge-unified-01a042e7/result.json`。

### 5. Windows 环境后续验证（不属于本轮 macOS 统一测试）

- 用户已明确：Windows 不再由 macOS 打包，本轮不执行 Windows 交叉打包测试。
- 后续仅在 Windows 环境启动开发版，验证系统默认浏览器、真实 ChatGPT 登录、账号回显及按钮抽查。

### 6. 用户层规则加载

- [x] 使用分层规则加载器校验 `XUNAN` 用户索引并加载 `AI_DESKTOP_ARCHITECTURE_BOUNDARY_RULES` — 通过，校验 `22` 个索引、`81` 条规则，命中 `XUNAN` 层 `1.1.0`。
- 预期：规则版本为 `1.1.0`；从根索引可达；不存在失效路径、重复逻辑 ID 或跨用户引用。

### 7. Windows 原生宿主门禁

- [x] `node --test tests/codex-platform-dependencies.test.mjs` — 首轮发现测试断言与动态文案不一致，改为真实执行宿主门禁后复测通过；最终 `2` 项通过、`0` 项失败。
- 预期：全部 Windows 打包入口在任何构建前校验 `process.platform=win32`；macOS 不执行 Windows 交叉打包；用户层 Harness 规则版本为 `5.101.0`。

### 8. 专题演化工作台全部导航节点真实换页

- [x] `npm run typecheck` — 任务级验证通过。
- [x] `npm run test:sel-ui` — `11` 项通过，确认仍从 SELUI 正式 Tree、Grid、Form 出口装配。
- [x] `AI_DESKTOP_TEST_TASK_ID=evolution-navigation-01a042e7-rerun npm run test:interaction` — 生产构建与真实 Electron 交互共 `21` 项通过、`0` 项失败、`0` 项跳过、`0` 项不稳定。
- [x] 人物目录、当前专题、调查与研讨、韩立审批、提案与任务、发布与验收、原始档案、运行控制、审批与分发开关、纠偏与恢复、审批轨迹、异常入口、专题档案全部逐项点击并核对右侧画布。
- [x] 实际窗口截图已检查，导航列、选中态与右侧画布无明显遮挡或错位；证据写入 `OPTION/temp/ai-desktop/临时材料/测试证据/interaction/evolution-navigation-01a042e7-rerun/result.json`，附件名为 `evolution-tree-navigation-pages`。
- [x] 用户层规则校验通过：`22` 个索引、`81` 条规则；`AI_DESKTOP_OFFICIAL_HARNESS_RULES` 命中 XUNAN 层 `5.102.0`。
- 首次交互运行的产品页面切换已成功；测试因“运行控制页面”同时匹配外层画布与内层区域产生严格定位冲突，收紧为画布属性断言后复跑通过。
- [x] 最新统一测试已执行：类型检查、Electron 构建、沙箱桥接、模块边界、SELUI、南宫婉领域及完整交互回归全部通过。
- 预期：任何一级模块或二级流程节点都不能只改变高亮；人物视角变化同步默认流程页，父节点显示明确总览，叶子节点显示对应业务内容。

### 9. 返还南宫婉执行的工作区来源

- [x] `npm run test:nangong-evolution` — Electron 构建通过，南宫婉演化领域 `18` 项通过、`0` 项失败。
- [x] 自动演化上下文 `workspaceState=null` 时，人工审批后的返还执行仍使用提案所属专题保存的工作区完成任务拆分、令狐审计和协同任务创建。
- [x] 专题工作区损坏或缺失时，返回“当前专题缺少可用的实施工作区”业务提示，不再出现 `Cannot read properties of null (reading 'roots')`。
- [x] 生产主进程静态门禁确认任务拆分与审计回调显式接收专题工作区和专题语言，不再读取 `automationContext.workspaceState`。
- [x] `AI_DESKTOP_TEST_TASK_ID=nangong-dispatch-workspace-01a042e7 npm run test:interaction` — 生产构建、类型检查及真实 Electron 交互 `21` 项通过，证据：`OPTION/temp/ai-desktop/临时材料/测试证据/interaction/nangong-dispatch-workspace-01a042e7/result.json`。
- [x] 用户层规则校验通过：`22` 个索引、`81` 条规则；`AI_DESKTOP_OFFICIAL_HARNESS_RULES` 命中 XUNAN 层 `5.103.0`。
- [x] 最新统一测试已连同第 8 节全部执行；真实 Electron 交互 `21/21` 通过，证据：`OPTION/temp/ai-desktop/临时材料/测试证据/interaction/unified-navigation-dispatch-01a042e7/result.json`。
- 预期：手动返还与自动分发都以 `topic.workspaceState` 为唯一工作区授权来源；自动演化是否配置或运行不影响手动返还。

### 10. 令狐统一测试失败自动修复闭环

- [x] `npm run typecheck` — 定向验证通过。
- [x] `npm run build:electron` — 定向验证通过，生产规则 bundle 已包含 `5.104.0`。
- [x] `npm run test:collaboration` — 受影响完整套件 `43` 项通过、`0` 项失败。
- [x] `npm run test:workflow-event-center` — 受影响完整套件 `10` 项通过、`0` 项失败。
- [x] `npm run test:sel-ui` — SELUI 完整套件 `11` 项通过、`0` 项失败。
- [x] `npm run test:nangong-evolution` — 南宫婉演化领域完整套件 `18` 项通过、`0` 项失败。
- [x] `npm run test:boundaries` — 模块与安全边界 `7` 项通过、`0` 项失败。
- [x] `npm run test:preload-sandbox` — 真实 Electron 沙箱桥接通过，暴露 `99` 项能力；首次运行受 macOS 测试沙箱禁止启动 GUI 影响而中止，按真实桌面测试权限隔离复跑后通过，非产品失败。
- [x] `node --test tests/codex-platform-dependencies.test.mjs` — 平台依赖及 Windows 原生宿主门禁 `2` 项通过、`0` 项失败；未执行 Windows 构建。
- [x] `AI_DESKTOP_TEST_TASK_ID=linghu-unified-repair-01a042e7 npm run test:interaction` — 生产构建与真实 Electron 交互 `21` 项通过、`0` 项失败、`0` 项跳过、`0` 项不稳定；证据：`OPTION/temp/ai-desktop/临时材料/测试证据/interaction/linghu-unified-repair-01a042e7/result.json`。
- [x] 新增回归确认：`test-failed + verification` 即使失败输出引用“用户/人工/选择”规则正文，也必须归类为 `test`，不得等待人工业务选择。
- [x] 新增回归确认：令狐读取真实失败证据，在原任务工作树完成最小修复与代码级验证，提交新 `resultSHA` 后再进入集成统一测试。
- [x] 用户层索引单元测试 `3` 项通过；分层规则加载器从根索引加载 `AI_DESKTOP_OFFICIAL_HARNESS_RULES` 成功，命中 `XUNAN` 层 `5.104.0`。
- [x] 本轮用户明确“统一测试”后，已按本测试文档执行全部登记门禁；Windows 构建按用户要求排除。
- 预期：机械可修复的统一测试失败由令狐闭环处理；同一故障指纹最多产生三次修复副作用；真实业务选择继续等待人工。

### 11. 一键清空测试数据与演化单树工作台

- [x] `npm run typecheck` — 最终交互构建内复跑通过。
- [x] `npm run build:electron` — 通过；清空能力只通过类型化 preload 与受控 IPC 暴露，单文件 preload 约 `10.1kb`。
- [x] `npm run test:preload-sandbox` — 真实 Electron 沙箱通过，白名单完整暴露 `100` 项能力。
- [x] `npm run test:boundaries` — `7` 项通过、`0` 项失败；Renderer 不接触 SQL、文件系统或进程重启能力。
- [x] `npm run test:sel-ui` — `11` 项通过、`0` 项失败；树导航和二次确认继续复用 SELUI 正式组件。
- [x] `npm run test:nangong-evolution` — `19` 项通过、`0` 项失败；清空后专题、提案、审批、研讨、档案与运行恢复状态为空。
- [x] `npm run test:collaboration` — `44` 项通过、`0` 项失败；清空后任务与集成批次为空，人物配置、登录、设置、工作区、可信命令和规则不受影响。
- [x] `npm run test:workflow-event-center` — `11` 项通过、`0` 项失败；SQLite 业务表在同一事务内清空，`AiDesktopSchemaVersion` 保留。
- [x] `npm run test:developer-settings` — `4` 项通过、`0` 项失败；清空入口、保留范围和独立 SELUI 设置浮窗门禁通过。
- [x] `node --test tests/codex-platform-dependencies.test.mjs` — `2` 项通过、`0` 项失败；只核对平台门禁，未执行 Windows 打包。
- [x] `AI_DESKTOP_TEST_TASK_ID=reset-data-single-tree-01a042e7-r7 npm run test:interaction` — 生产构建与真实 Electron 交互 `22` 项通过、`0` 项失败、`0` 项跳过；逐项点击单树、切换右侧页面，并验证清空按钮的危险确认、取消和确认路径。证据：`OPTION/temp/ai-desktop/临时材料/测试证据/interaction/reset-data-single-tree-01a042e7-r7/result.json`。
- [x] 设计 QA — 已按用户截图检查单树左右结构、选中态、滚动、遮挡和窄宽布局，工程根 `design-qa.md` 最终结论为 `passed`；对比证据：`OPTION/temp/ai-desktop/临时材料/设计验证/reset-data-single-tree-01a042e7/source-implementation-final-comparison.png`。
- [x] 用户层规则索引与加载校验 — 用户层索引单元测试 `10` 项通过；校验 `22` 个索引、`81` 条规则，`AI_DESKTOP_OFFICIAL_HARNESS_RULES` 从根索引命中 `XUNAN` 层 `5.105.0`。
- [x] 统一测试中发现 SELUI 确认框会沿用上一次自定义按钮文案，已改为每次调用显式恢复默认“确认/取消”，最终完整交互复测通过。
- 预期：左侧仅保留一棵包含模块、分组和叶子页面的树，右侧为唯一内容区；任一父节点或叶子节点点击都会产生明确页面结果。设置中的危险按钮须二次确认，只清除应用内部测试运行数据与会话历史，保留登录、用户设置、工作区、可信命令、规则、源码、工程审计文件和数据库结构，成功后受控重启。

## 2026-08-28 最新统一测试复跑

- 完成时间：`2026-08-28T18:51:23+0900`
- 总体结论：**失败，禁止标记为全部已验证**。应用构建与真实 Electron 主路径通过，但三条静态/规则回归门禁仍引用拆分前的旧实现或旧历史字段。
- [x] `npm run typecheck` — 通过。
- [x] `npm run build:electron` — 通过；生产规则包生成 `14` 条规则，preload 约 `10.1kb`。
- [x] `npm run test:preload-sandbox` — 通过；真实沙箱暴露 `100` 项能力。
- [x] `npm run test:boundaries` — `7/7` 通过。
- [ ] `npm run test:sel-ui` — `10/11` 通过；失败项仍断言 `repairLocalPackageLinks(details)` 后直接调用旧 `symlinkSync(details.dependencyRoot, ...)`，当前实现已统一改由 `createDependencyLink(...)` 创建跨平台目录链接。
- [x] `npm run test:nangong-evolution` — `19/19` 通过。
- [ ] `npm run test:collaboration` — `43/44` 通过；失败项仍要求聚合 Harness 规则正文保留已迁移的 `upgrade_record_5_100` 历史字段。
- [x] `npm run test:workflow-event-center` — `11/11` 通过。
- [x] `npm run test:developer-settings` — `4/4` 通过。
- [x] `node --test tests/codex-platform-dependencies.test.mjs` — `2/2` 通过；未执行 Windows 打包。
- [x] `AI_DESKTOP_TEST_TASK_ID=docs-test-save-unified-20260828 npm run test:interaction` — `22/22` 通过；证据：`OPTION/temp/ai-desktop/临时材料/测试证据/interaction/docs-test-save-unified-20260828/result.json`。
- [x] `design-qa.md` — `final result: passed`。
- [ ] 用户层规则回归 — 当前索引实际为 `32` 个索引、`104` 条规则，但旧测试仍固定断言 `22/81`；拆分后的聚合规则正文不再内嵌历史字段，相关旧断言失败。
- 规则沉淀评估：**无需沉淀**。近义规则 `CODE_TEST_RULES` 与 `RULE_LIFECYCLE_GOVERNANCE_RULES` 已覆盖“按测试文档执行、失败不得标记通过、完成前评估”的要求；本轮仅产生测试结果，没有形成新的稳定工程约束。

## 2026-08-28 修复后统一测试结果

- 完成时间：`2026-08-28T20:02:41+0900`
- 总体结论：**全部通过**。上一轮暴露的陈旧实现断言、拆分前规则路径、旧索引计数和缺失中文业务注释均已修正；Windows 打包继续按用户要求排除。
- [x] 依赖与构建 — `dependencies:link`、`typecheck`、`build:electron` 全部通过；生产规则包为 `14` 条规则，preload 约 `10.1kb`；依赖缓存自愈 `8/8` 通过。
- [x] 应用静态与领域回归 — boundaries `7/7`、SELUI `11/11`、南宫婉演化 `19/19`、协同 `44/44`、工作流事件中心 `11/11`、开发设置 `4/4`、平台依赖 `2/2`、规则包服务 `3/3` 全部通过。
- [x] Electron 沙箱 — 真实沙箱桥接成功，完整暴露 `100` 项能力。
- [x] 真实页面交互 — `AI_DESKTOP_TEST_TASK_ID=docs-test-save-fixed-unified-20260828 npm run test:interaction` 为 `22/22` 通过；覆盖登录主操作、侧栏与单树换页、韩立/南宫婉协同、一键清空二次确认等链路。证据：`OPTION/temp/ai-desktop/临时材料/测试证据/interaction/docs-test-save-fixed-unified-20260828/result.json`。
- [x] 规则引擎回归 — 规则整合 `11/11`、分层加载 `12/12`、active-user 覆盖 `3/3`、源码归属模块 `5/5`、原入口兼容 `58/58`、结构门禁 `1/1`、智慧报告 `6/6` 全部通过。
- [x] 严格结构门禁 — `hardViolationCount=0`、`machineAbsolutePathHits=[]`，源码树无 `__pycache__`；全树报告当前为 `34` 个索引、`115` 条逻辑规则，分层加载测试所核对的当前用户迁移基线为 `32/104`。
- [x] SELPLAT 快速总门禁 — `selplatQuickGate` 构建成功，源码归属 `violationCount=0`。
- 规则沉淀评估：**无需新增规则**。近义规则 `CODE_TEST_RULES` 已要求测试随权威实现迁移，`AI_RULE_PACKAGE_INTELLIGENCE_RULES` 已要求拆分叶规则保持注释和索引一致，`RULE_LIFECYCLE_GOVERNANCE_RULES` 已覆盖统一测试与完成门禁；本轮通过修正既有测试和既有规则注释完成防复发闭环。

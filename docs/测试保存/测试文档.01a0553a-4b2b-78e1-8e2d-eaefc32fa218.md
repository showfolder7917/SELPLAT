# 测试文档 · 01a0553a-4b2b-78e1-8e2d-eaefc32fa218

## 本轮目标

- 将令狐老祖整理为 contracts、Electron 主进程和 Renderer 三个编译边界下的同名垂直模块。
- 外部调用统一经过各层 `linghu/index.ts`，减少 `main.ts` 与 `DeveloperApp.tsx` 的交叉修改。
- 抽离流程只读分析、运行时装配、自动保障面板和修正提案面板。
- 为本轮新增、移动和重构的 TypeScript 补充面向初学者的逐行业务语义注释。

## 待统一测试

### 1. TypeScript 双运行时类型检查

- 状态：待统一测试。
- 命令：`npm --prefix apps/ai-desktop run typecheck`
- 命令：`node apps/ai-desktop/scripts/run-with-dependencies.mjs tsc -p apps/ai-desktop/tsconfig.electron.json --noEmit`
- 预期：Renderer、contracts 与 Electron 主进程的新目录、公开出口和导入路径全部通过类型检查。

### 2. 模块边界与令狐协作回归

- 状态：模块边界静态回归已执行；需构建的协作回归待统一测试。
- 命令：`node --test apps/ai-desktop/tests/module-boundaries.test.mjs`
- 命令：`npm --prefix apps/ai-desktop run build:electron && node apps/ai-desktop/scripts/run-with-dependencies.mjs node --test apps/ai-desktop/tests/collaboration-mode.test.mjs apps/ai-desktop/tests/nangong-evolution.test.mjs apps/ai-desktop/tests/workflow-event-center.test.mjs`
- 预期：旧令狐内部路径无调用方；Facade 单一入口、恢复次数、提案审批、统一异常交接和测试失败修复行为保持不变。

### 3. Renderer 真实交互与生产布局

- 状态：待统一测试。
- 命令：`npm --prefix apps/ai-desktop run test:interaction`
- 预期：生产 Electron 中令狐自动保障面板可见；开关、文案新增修改删除、修正提案和状态推送仍可操作；正式窗口布局无回退。
- 证据要求：交互 `result.json`；失败时保留截图与 trace，成功不额外生成截图。

### 4. 开发入口即时启动冒烟

- 状态：待执行即时启动。
- 命令：`npm --prefix apps/ai-desktop run dev:developer`
- 预期：Vite 开发入口保持运行且 Renderer 可以加载；该冒烟不等价于统一测试。

### 5. 当前用户规则沉淀与索引可达性

- 状态：已通过。
- 命令：通过分层规则加载器加载 `AI_DESKTOP_ARCHITECTURE_BOUNDARY_RULES` 并执行当前用户索引树验证。
- 预期：规则版本为 `1.2.0`，可读取 `linghu_vertical_module_contract`；逻辑 ID 仍从 ai-desktop 叶子索引沿父链到根索引唯一可达，无重复 ID、失效路径或跨用户引用。

## 已执行结果

- **通过**：`node --test apps/ai-desktop/tests/module-boundaries.test.mjs apps/ai-desktop/tests/developer-package-root.test.mjs`，共 13 项通过、0 项失败。
- **通过**：对本轮修改的 5 个 `.mjs/.cjs` 测试文件执行 `node --check`，全部语法检查通过。
- **通过**：`git diff --check`，未发现空白错误。
- **通过**：分层规则加载器成功加载 `AI_DESKTOP_ARCHITECTURE_BOUNDARY_RULES` `1.2.0`，并读取到新增的 `linghu_vertical_module_contract`。
- **通过**：当前用户 `XUNAN` 索引树验证完成，共验证 32 个索引、104 条规则，未发现重复逻辑 ID、失效路径或跨用户引用。
- **阻塞**：Renderer/Electron TypeScript 检查与开发入口即时启动尚未执行。当前 Windows 上 `apps/ai-desktop/node_modules` 是内容为 macOS 绝对路径的普通文件，不是 Junction；当前锁哈希 `86c21a7d47d0b037ceb495cafb33d1d8e6b48161370a5ae41844a63ea809b737` 的 Windows 依赖缓存为空，npm 离线内容缓存也为空。在不联网、不删除现有异常文件的前提下无法取得 TypeScript、Vite、React 和 Electron 类型依赖。
- **失败**：`npm --prefix apps/ai-desktop run dependencies:link`，退出码 1；工程脚本明确报告 `apps/ai-desktop/node_modules` 必须是符号链接或 Junction。命令在校验阶段停止，未替换该异常文件。
- **阻塞**：源码归属门禁执行后报告 3 条违规，全部来自 `apps/ai-desktop/ruleengine/python/ruleengine/__pycache__/执行器.cpython-313.pyc` 这一可再生缓存及其目录，与令狐 TypeScript 迁移无关。受当前执行环境删除策略阻止，无法清理后复跑，因此不能把源码归属门禁标记为通过。
- 其余项目保持“待统一测试”，不得把静态回归通过表述为完整构建、真实页面或统一测试通过。

## 追加任务：外部 Codex 手动保存会话

### 修正后的目标

- 只有用户明确要求保存时，外部 Codex 才按指定 `threadId` 读取自己的 rollout。
- 手动保存到当前工程 `docs/会话存档/会话_<threadId>.json`，不依赖 AI Desktop、Electron 或数据库。
- 保存真实可见用户消息和 Codex 最终回答，排除系统、开发者、工具、隐藏推理、commentary 和内部自动化内容。
- 尚未完成的最后一轮明确标记为 `incomplete`，以后再次手动保存时幂等更新同一个文件。
- 数据库存在也不得自动导入；只有用户以后明确要求导入时，才处理 `migration.status=pending` 的存档。
- 已撤销此前误加的 AI Desktop 启动、回合完成与监听自动保存接线。

### 已执行验证

- **通过**：`python -m unittest apps.ai-desktop.ruleengine.tests.local.XUNAN.test_external_codex_conversation_archiver`，1 项通过、0 项失败。
- **通过**：使用当前明确 threadId `01a0553a-4b2b-78e1-8e2d-eaefc32fa218` 实际执行手动存档，生成 `docs/会话存档/会话_01a0553a-4b2b-78e1-8e2d-eaefc32fa218.json`。
- **通过**：实存文件为 UTF-8 JSON，`archiveMode=manual-only`、`migration.status=pending`、`migration.policy=manual-command-only`；共 10 轮、19 条消息，最后一轮为 `incomplete`。
- **通过**：实存文件未包含抽查的 commentary 文本。
- **通过**：源码检索确认 Electron 与测试中不存在 `CodexConversationFileArchive`、`training_corpus.file_archived` 或 `archivePendingRolloutsIncrementally` 自动接线残留。
- **通过**：对同一真实 threadId 再次执行手动存档返回 `changed=false`，仍为 10 轮、19 条消息，确认无变化时不会改写文件。
- **通过**：`node --test apps/ai-desktop/tests/module-boundaries.test.mjs`，9 项通过、0 项失败，撤销错误自动接线后原 AI Desktop 边界未回退。
- **通过**：分层规则加载器确认 `AI_DESKTOP_EVENT_MEMORY_UI_RULES` `5.145.0` 可读取手动契约和 Python 能力引用；当前用户索引树共 32 个索引、104 条规则。
- **通过**：`git diff --check`，未发现空白错误。
- **阻塞**：源码归属门禁仍只报告既存的 `apps/ai-desktop/ruleengine/python/ruleengine/__pycache__/执行器.cpython-313.pyc` 及目录，共 3 条违规；本轮能力使用 `PYTHONDONTWRITEBYTECODE=1`，没有新增缓存污染。
- **待验证**：以后用户明确要求数据库导入时，需要另行加载数据库规则、实现或执行人工导入，并对幂等键、事务和迁移状态回写做专项验证；本轮没有数据库导入授权，也没有自动执行。
- **规则沉淀结论：修正并升级现有规则**。`AI_DESKTOP_EVENT_MEMORY_UI_RULES` 继续使用版本 `5.145.0`，`conversation_training_file_staging_contract` 已修正为外部 Codex 手动保存、显式 threadId、无自动监听和数据库仅按独立人工命令导入；Python 能力入口已登记到当前用户层，没有建立重复规则。

## 追加任务：令狐 DTO 目录与命名统一

### 目标

- 删除 `contracts/collaboration/linghu/automation.ts`，按业务概念拆为 `dto/*.dto.ts`。
- DTO 文件统一使用 `kebab-case.dto.ts`，所有公开 DTO 类型统一使用 `Dto`、`RequestDto` 或 `EventDto` 后缀。
- `contracts/collaboration/linghu/index.ts` 是目录外唯一门面，禁止 Electron、Renderer 和其他 contracts 直接引用 `linghu/dto`。
- 将误放在 `nangong-evolution.ts` 的令狐修正提案请求迁回令狐 DTO。

### 待统一测试

1. 命令：`npm --prefix apps/ai-desktop run typecheck`
   - 预期：Renderer、DesktopApi 和 contracts 的新 DTO 名称全部通过类型检查。
2. 命令：`node apps/ai-desktop/scripts/run-with-dependencies.mjs tsc -p apps/ai-desktop/tsconfig.electron.json --noEmit`
   - 预期：Electron Store、Facade、IPC、事件中心和南宫审批调用方全部通过类型检查。
3. 命令：`npm --prefix apps/ai-desktop run build:electron && node apps/ai-desktop/scripts/run-with-dependencies.mjs node --test apps/ai-desktop/tests/collaboration-mode.test.mjs apps/ai-desktop/tests/nangong-evolution.test.mjs apps/ai-desktop/tests/workflow-event-center.test.mjs`
   - 预期：DTO 改名不改变运行时对象结构，令狐状态、修正提案和事件回归保持通过。
4. 命令：`npm --prefix apps/ai-desktop run dev:developer`
   - 预期：开发入口成功启动，令狐页面能读取状态并操作启动文案。

### 已执行结果

- **通过**：`node --test apps/ai-desktop/tests/module-boundaries.test.mjs`，9 项通过、0 项失败。
- **通过**：最终联合执行 `node --test apps/ai-desktop/tests/module-boundaries.test.mjs apps/ai-desktop/tests/developer-package-root.test.mjs`，共 13 项通过、0 项失败。
- **通过**：边界测试确认四个 `*.dto.ts` 文件存在、公开类型全部以 `Dto` 结尾、旧 `automation.ts` 不存在、目录外没有直接引用 `linghu/dto`。
- **通过**：5 个令狐 contract TypeScript 文件的相对导入路径全部可解析。
- **通过**：源码扫描未发现旧的无 `Dto` 后缀令狐类型名。
- **通过**：`node --check apps/ai-desktop/tests/module-boundaries.test.mjs` 和 `git diff --check`。
- **待统一测试**：完整 TypeScript 类型检查、Electron 构建和运行测试仍受异常 `node_modules` 文件阻塞，本轮没有把静态检查表述为完整验证。
- **规则沉淀结论：升级现有规则**。用户确认的 DTO 目录、命名和门面约束可复用于后续令狐契约，已将 `AI_DESKTOP_ARCHITECTURE_BOUNDARY_RULES` 从 `1.2.0` 升级为 `1.3.0` 并增加 `linghu_contract_dto_layout_contract`，没有新建近义重复规则。
- **已知门禁**：源码归属检查仍仅报告既有的 `apps/ai-desktop/ruleengine/python/ruleengine/__pycache__/执行器.cpython-313.pyc` 及目录，共 3 条；本轮未新增该类文件，也未擅自删除既有环境产物。

## 追加任务：令狐 DTO 使用 InDto 与 OutDto 显式方向命名

### 修改目标

- 固定站在令狐模块边界判断方向：进入令狐为 `InDto`，离开令狐为 `OutDto`，主动输出事件为 `EventOutDto`。
- DTO 文件名同步使用 `.in.dto.ts` 或 `.out.dto.ts`，不再通过打开文件猜测数据方向。
- 每个 DTO 文件使用中文说明生产方、接收方、数据流向、作用和禁止职责。
- `contracts/collaboration/linghu/index.ts` 继续作为目录外唯一门面。

### 待统一测试

1. `npm --prefix apps/ai-desktop run typecheck`
   - 预期：Renderer、DesktopApi 和所有新 InDto、OutDto 名称通过类型检查。
2. `node apps/ai-desktop/scripts/run-with-dependencies.mjs tsc -p apps/ai-desktop/tsconfig.electron.json --noEmit`
   - 预期：Electron IPC、Facade、Store、事件桥和南宫修正提案接收方通过类型检查。
3. `npm --prefix apps/ai-desktop run build:electron && node apps/ai-desktop/scripts/run-with-dependencies.mjs node --test apps/ai-desktop/tests/collaboration-mode.test.mjs apps/ai-desktop/tests/nangong-evolution.test.mjs apps/ai-desktop/tests/workflow-event-center.test.mjs`
   - 预期：DTO 只改变编译期名称，不改变实际对象字段和运行流程。
4. `npm --prefix apps/ai-desktop run dev:developer`
   - 预期：令狐面板能够读取状态、增改启动文案并接收状态事件。

### 已执行结果

- **通过**：`node --check apps/ai-desktop/tests/module-boundaries.test.mjs`。
- **通过**：`node --test apps/ai-desktop/tests/module-boundaries.test.mjs apps/ai-desktop/tests/developer-package-root.test.mjs`，13 项通过、0 项失败。
- **通过**：旧 DTO 类型名称扫描只命中边界测试中的禁止旧文件清单，生产源码无残留。
- **通过**：目录外直接引用 `collaboration/linghu/dto/` 的扫描结果为 0，调用方继续只使用门面。
- **通过**：`git diff --check`。
- **待统一测试**：完整 TypeScript 类型检查、Electron 构建和开发版启动仍受当前失效 `node_modules` 与本地无可用 `tsc` 阻断，未表述为通过。
- **已知门禁**：源码归属检查仍只报告既有 `__pycache__/执行器.cpython-313.pyc` 及目录，共 3 条，与本轮 TypeScript DTO 重命名无关。
- **规则沉淀结论：升级现有规则**。已将 `AI_DESKTOP_ARCHITECTURE_BOUNDARY_RULES` 从 `1.3.0` 升级为 `1.4.0`，在既有 `linghu_contract_dto_layout_contract` 中加入 InDto、OutDto、EventOutDto 方向和文件注释门禁；未新建近义重复规则。

## 追加任务：令狐主进程公开 Facade 与 internal 实现分类

### 修改目标

- `electron/services/collaboration/linghu/` 根层只保留 `index.ts` 和 `linghu-automation.facade.ts`。
- 运行时工厂、状态仓库、流程分析器和统一测试执行器移动到 `internal/`。
- 文件分别使用 runtime、`.store.ts`、`.analyzer.ts`、`.runner.ts` 职责命名，不使用无法区分职责的统一 `.service.ts`。
- 目录外生产代码继续只能经 `linghu/index.ts` 导入。
- 同步测试源码路径和 `OPTION/ai-desktop/令狐工作流分析器说明.md`。

### 待统一测试

1. `npm --prefix apps/ai-desktop run typecheck`
   - 预期：Renderer 与主进程共享类型引用不受实现目录移动影响。
2. `node apps/ai-desktop/scripts/run-with-dependencies.mjs tsc -p apps/ai-desktop/tsconfig.electron.json --noEmit`
   - 预期：Facade、internal 相对导入和组合根引用全部通过 Electron 类型检查。
3. `npm --prefix apps/ai-desktop run build:electron && node apps/ai-desktop/scripts/run-with-dependencies.mjs node --test apps/ai-desktop/tests/collaboration-mode.test.mjs apps/ai-desktop/tests/nangong-evolution.test.mjs apps/ai-desktop/tests/workflow-event-center.test.mjs`
   - 预期：编译后仍只通过令狐 index 导入，自动保障、修正提案和统一测试行为不变。
4. `npm --prefix apps/ai-desktop run dev:developer`
   - 预期：Developer 应用启动，令狐状态、自动检查和事件订阅正常。

### 已执行结果

- **通过**：四个修改过的 `.mjs` 测试文件执行 `node --check`。
- **通过**：`node --test apps/ai-desktop/tests/module-boundaries.test.mjs apps/ai-desktop/tests/developer-package-root.test.mjs`，13 项通过、0 项失败。
- **通过**：令狐目录 6 个 TypeScript 文件的相对导入目标全部存在。
- **通过**：生产源码直接引用 `linghu/internal` 或 `linghu-automation.facade` 文件的扫描结果为 0，外部继续只走 `index.ts`。
- **通过**：旧根层文件名只存在于禁止结构回退的边界测试清单中。
- **通过**：`git diff --check`。
- **待统一测试**：完整 TypeScript 类型检查、Electron 构建、运行回归和启动仍受当前失效 `node_modules` 与本地无可用 `tsc` 阻断，未表述为通过。
- **已知门禁**：源码归属检查仍只报告既有 `apps/ai-desktop/ruleengine/python/ruleengine/__pycache__/执行器.cpython-313.pyc` 及目录，共 3 条；本轮目录重构没有新增 Python 产物。
- **规则沉淀结论：升级现有规则**。已将 `AI_DESKTOP_ARCHITECTURE_BOUNDARY_RULES` 从 `1.4.0` 升级为 `1.5.0`，扩充既有 `linghu_vertical_module_contract`，没有新建近义重复规则。

## 追加任务：令狐 Runtime 内部组装与最小公开 API

### 修改目标

- `electron/services/collaboration/linghu/index.ts` 只公开 `LinghuAutomationFacade`、`createLinghuRuntime()` 和运行时类型。
- 不再公开 Store、Runner、内部异常类和内部常量。
- `createLinghuRuntime()` 内部创建 Store 与 Runner，只返回 Facade 和受控生命周期能力。
- 主进程通过 `runUnifiedTests()`、`clearTestData()` 和 `assertTestDataCleared()` 通知 Runtime，不获取内部对象。
- Facade 依赖 `LinghuCollaborationPort` 最小能力接口，不再导入具体 `CollaborationCoordinator`。
- 组件测试可以显式导入 internal Store 做内部验证，但生产代码禁止把该路径当作公共 API。

### 待统一测试

1. `npm --prefix apps/ai-desktop run typecheck`
   - 预期：Runtime options、最小协作端口和 DTO 引用通过 Renderer 类型检查。
2. `node apps/ai-desktop/scripts/run-with-dependencies.mjs tsc -p apps/ai-desktop/tsconfig.electron.json --noEmit`
   - 预期：`CollaborationCoordinator` 结构满足 `LinghuCollaborationPort`，主进程 Runtime 能力和内部 Runner 组装通过类型检查。
3. `npm --prefix apps/ai-desktop run build:electron && node apps/ai-desktop/scripts/run-with-dependencies.mjs node --test apps/ai-desktop/tests/collaboration-mode.test.mjs apps/ai-desktop/tests/nangong-evolution.test.mjs apps/ai-desktop/tests/workflow-event-center.test.mjs`
   - 预期：版本候选统一测试、令狐测试模块、状态清理、基础设施失败分类和重启流程保持通过。
4. `npm --prefix apps/ai-desktop run test:interaction`
   - 预期：交互夹具通过 internal 测试路径读取唯一默认启动文案，页面行为不变。
5. `npm --prefix apps/ai-desktop run dev:developer`
   - 预期：应用启动，Facade、Runtime、IPC、状态事件和协作恢复正常。

### 已执行结果

- **通过**：`node --check` 检查边界测试、协作测试和交互夹具语法。
- **通过**：`node --test apps/ai-desktop/tests/module-boundaries.test.mjs apps/ai-desktop/tests/developer-package-root.test.mjs`，13 项通过、0 项失败。
- **通过**：令狐目录 6 个 TypeScript 文件的相对导入目标全部存在。
- **通过**：公开 `index.ts` 中 Store、Runner、内部异常和内部常量名称扫描为 0。
- **通过**：生产代码直接引用令狐 internal 或 Facade 文件路径的扫描结果为 0。
- **通过**：主进程不再创建 `LinghuUnifiedTestRunner`，不再取得 `linghuRuntime.store`。
- **通过**：OPTION 阅读文档已同步 Runtime 隐藏实现和 `LinghuCollaborationPort`。
- **通过**：`git diff --check`。
- **待统一测试**：完整 TypeScript 类型检查、Electron 构建、运行回归、交互测试和启动仍受当前失效 `node_modules` 与本地无可用 `tsc` 阻断，未表述为通过。
- **已知门禁**：源码归属检查仍只报告既有 `apps/ai-desktop/ruleengine/python/ruleengine/__pycache__/执行器.cpython-313.pyc` 及目录，共 3 条；本轮没有新增 Python 产物。
- **规则沉淀结论：升级现有规则**。已将 `AI_DESKTOP_ARCHITECTURE_BOUNDARY_RULES` 从 `1.5.0` 升级为 `1.6.0`，继续扩充既有 `linghu_vertical_module_contract`，未创建近义重复规则。

# 本次测试文档

任务线程：01a037ba-d3d6-7e42-89e9-971a528b3a4d

## 统一测试清单

1. **待测试**：最新ITMACOMMON Bean接口兼容确认
   - 变更内容：删除2.4和2.6未记载的masterFlag传参，2.5新增reserveFlag，并将One203和Two203输出getter更新为最新接口
   - 测试命令：gradlew.bat compileJava
   - 预期结果：ITMAB203使用本地ITMACOMMON构建产物编译成功且不存在Bean方法不匹配

2. **通过**：AI Desktop版本集成抽离TypeScript检查
   - 变更内容：新增VersionIntegrationPipeline并调整CollaborationCoordinator、ReleaseBatchStore和main组装，确认类型与接口接线完整
   - 测试命令：npm --prefix apps/ai-desktop run typecheck
   - 预期结果：TypeScript无类型错误，协调器与版本集成流水线接口可正常编译
   - 实际结果：2026-08-26在apps/ai-desktop执行npm run typecheck，tsc --noEmit退出码0；随后git diff --check未发现空白错误
3. **待测试**：AI Desktop版本集成流水线解耦回归
   - 变更内容：验证候选创建、组合测试、提升、本地合并和发布编排只存在于VersionIntegrationPipeline，操作者身份由主进程注入且原有冲突与发布行为保持
   - 测试命令：npm --prefix apps/ai-desktop run test:collaboration
   - 预期结果：协同模式测试全部通过，协调器不再直接操作版本候选，流水线和发布批次存储不写死令狐身份

4. **待测试**：AI Desktop版本集成解耦规则加载回归
   - 变更内容：升级现有AI Desktop用户规则，固定VersionIntegrationPipeline单入口、协调器仅调度和操作者组装注入边界
   - 测试命令：python apps/ai-desktop/ruleengine/tests/run_tests.py XUNAN
   - 预期结果：XUNAN规则索引与加载测试全部通过，AI_DESKTOP_OFFICIAL_HARNESS_RULES可从根索引命中新解耦合同且无重复逻辑ID或失效路径

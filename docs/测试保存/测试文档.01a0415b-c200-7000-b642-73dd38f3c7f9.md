# 本次测试文档

任务线程：01a0415b-c200-7000-b642-73dd38f3c7f9

## 统一测试清单

1. **待测试**：rule-engine 迁移文件集与旧路径静态一致性
   - 变更内容：将原 rule-engine 文件完整移动到 apps/ai-desktop/ruleengine，完成扁平化后清理活跃旧路径及源码缓存。
   - 测试命令：powershell -NoProfile -Command "if (Test-Path apps/rule-engine) { exit 1 }; if ((Get-ChildItem apps/ai-desktop/ruleengine -Recurse -File).Count -ne 242) { exit 1 }; rg -n --hidden --glob '!**/.git/**' --glob '!**/archive/**' --glob '!docs/测试保存/**' --glob '!OPTION/**' 'apps/rule-engine' .; if ($LASTEXITCODE -ne 1) { exit 1 }"
   - 预期结果：旧目录不存在；删除退役 docs 后扁平结构恰有 242 个正式文件；活跃工程文件不存在 apps/rule-engine 旧路径。

2. **待测试**：迁移后启动协议入口
   - 变更内容：启动加载器、核心协议和唯一规则索引迁入AI Desktop内ruleengine新路径。
   - 测试命令：python apps/ai-desktop/ruleengine/python/local/core/abilities/startup_protocol_loader.py
   - 预期结果：状态为completed，按STARTER、USER、CODE、COMMAND、RULE_INDEX、GENERATOR_REPAIR_PROTOCOL顺序完整装载。

3. **待测试**：rule-engine 全部Python规则回归
   - 变更内容：同步路径配置、加载器、执行器、规则索引、用户能力与全部Python测试中的新物理路径。
   - 测试命令：gradlew.bat selplatAllRuleTests -PselplatPython=python --offline
   - 预期结果：core与XUNAN全部rule-engine Python测试通过，失败数为0，源码旁无pyc或__pycache__。

4. **待测试**：MDA中央登记路径专项回归
   - 变更内容：MDA项目生成器和启动SQL导出器改从AI Desktop内ruleengine读取当前用户中央登记。
   - 测试命令：gradlew.bat :apps:mda:backend:test --tests "com.sp.selplat.mda.capability.projectgenerator.service.impl.MdaProjectGeneratorServiceImplTest" --tests "com.sp.selplat.mda.capability.sqlexport.service.impl.JdbcStartupSqlExportServiceImplTest" --offline
   - 预期结果：两个MDA测试类全部通过，临时工程中的中央登记创建、读取与更新均命中新路径。

5. **待测试**：SELPLAT源码归属快速门禁
   - 变更内容：源码归属扫描器、路径配置和根Gradle直接入口全部迁入新目录。
   - 测试命令：gradlew.bat selplatQuickGateCore -PselplatPython=python --offline
   - 预期结果：源码语言、分层、注册表、缓存污染和工程路径违规数均为0。

6. **待测试**：SELPLAT全量统一门禁
   - 变更内容：重大目录迁移影响根构建、MDA、rule-engine规则及跨模块路径调用。
   - 测试命令：gradlew.bat selplatFullGate -PselplatPython=python --offline
   - 预期结果：全部Java叶子check、SELUI边界、快速门禁及全部rule-engine测试通过，所有违规数和失败数为0。

7. **待测试**：AI Desktop目录结构说明文档完整性
   - 变更内容：在OPTION新增基于真实工程结构的AI Desktop目录用途说明，覆盖源码、规则引擎、测试与工程数据边界。
   - 测试命令：powershell -NoProfile -Command "$d=Get-Content -Raw -Encoding UTF8 'OPTION/AI Desktop目录结构用途说明.md'; @('contracts','db','electron','ruleengine','scripts','src','tests') | ForEach-Object { if (-not $d.Contains($_)) { exit 1 } }; if ($d.Contains([char]0xFFFD)) { exit 1 }"
   - 预期结果：文档可按UTF-8读取，包含7个顶层目录及工程外数据根说明，且不存在Unicode替换字符。

8.  **待测试**：AI Desktop contracts 分层、能力注册与 preload 领域边界
   - 变更内容：contracts 迁入五个领域，补充职责和 DesktopApi 方法注释；99 个能力进入无重复注册域；preload 拆为六个领域桥接，Renderer 业务组件迁入 Feature。
   - 测试命令：cd apps/ai-desktop && npm run test:boundaries
   - 预期结果：contracts 根目录无平铺 TypeScript；全部协议导入可解析；能力 ID 无遗漏或重复；IPC、preload、Renderer Feature 边界合同全部通过。

9.  **待测试**：AI Desktop TypeScript 与生产规则构建
   - 变更内容：新增规则 bundle 构建器、规则运行服务、客户覆盖策略、IPC、preload、Renderer 规则 Feature，并更新客户运行路径。
   - 测试命令：cd apps/ai-desktop && npm run build:developer
   - 预期结果：TypeScript typecheck、Electron 编译、Renderer 构建和生产规则 bundle 构建全部成功，规则清单 9 项且不存在缺失源文件。

10. **待测试**：生产规则包完整性与客户覆盖事务测试
   - 变更内容：主进程按 SHA-256 校验内置规则，只允许覆盖显式开放 ID，并在一个覆盖文件任一记录失败时回滚整文件。
   - 测试命令：cd apps/ai-desktop && npm run test:rules
   - 预期结果：合法覆盖生效并注入开发者指令；锁定规则覆盖被拒绝且内置规则保留；开发资源不进入生产白名单，失败数为0。

11. **待测试**：Windows 客户目录包脱离源码仓库运行资源检查
   - 变更内容：新增不写 selplatDevelopmentRoot 的 customer Builder 配置，并把生产规则放入 app.asar 外的 resources/ruleengine。
   - 测试命令：cd apps/ai-desktop && npm run package:win:customer && npm run verify:win:customer
   - 预期结果：win-unpacked 可生成；app.asar 包含 Renderer、Electron 入口和 package.json，不含 ruleengine 源码、Python、测试、脚本或构建机根；外部 manifest.json 与 rules.json 存在且可解析。

12. **待测试**：AI Desktop 重构后隔离交互回归
   - 变更内容：设置浮层、桌面控件、演化树、规则展示和 preload 桥接已模块化，需验证真实 Electron 交互未回退。
   - 测试命令：cd apps/ai-desktop && npm run test:interaction
   - 预期结果：隔离 Electron 交互测试全部通过，设置、工作区、对话、截图、协同及异常边界无回归，失败数为0。

13. **待测试**：AI Desktop 架构边界用户规则索引加载
   - 变更内容：当前 XUNAN 用户层新增 AI_DESKTOP_ARCHITECTURE_BOUNDARY_RULES，并同步 AI Desktop 叶子索引和生产白名单。
   - 测试命令：python apps/ai-desktop/ruleengine/python/local/core/abilities/layered_rule_loader.py '{"action":"load","logical_id":"AI_DESKTOP_ARCHITECTURE_BOUNDARY_RULES","active_scope":"selplat/应用/ai-desktop","active_user":"XUNAN"}'
   - 预期结果：从根索引按稳定用户 ID 唯一命中新规则，路径无逃逸、逻辑 ID 无重复、未加载其他用户或无关作用域。

14. **待测试**：ruleengine 扁平目录与源码缓存防复发门禁
   - 变更内容：移除 backend、src 和 com/sp/selplat 层级，代码、规则、测试、文档分别迁入 python、rules、tests、docs，并升级用户层结构规则。
   - 测试命令：powershell -NoProfile -Command "$r='apps/ai-desktop/ruleengine'; @('python/ruleengine','python/local/core','rules/RULE_INDEX.md','rules/config/路径配置.toml','tests/run_tests.py') | ForEach-Object { if (-not (Test-Path (Join-Path $r $_))) { exit 1 } }; @('backend','src','docs') | ForEach-Object { if (Test-Path (Join-Path $r $_)) { exit 1 } }; if (Get-ChildItem $r -Recurse -Directory | Where-Object { $_.Name -in @('__pycache__','com') }) { exit 1 }; if (Get-ChildItem $r -Recurse -File -Filter *.pyc) { exit 1 }"
   - 预期结果：五个正式入口全部存在，backend、src、docs、Java式包目录、__pycache__ 和 pyc 均不存在。

15. **待测试**：AI Desktop 规则引用与生产白名单新路径回归
   - 变更内容：规则 bundle 构建器、AI Desktop Node 测试、MDA 中央登记、AGENTS 和生产白名单全部切换到 ruleengine/rules。
   - 测试命令：cd apps/ai-desktop && node --test tests/collaboration-mode.test.mjs tests/developer-explorer-collapse.test.mjs tests/model-settings-contract.test.mjs tests/rule-bundle-service.test.mjs
   - 预期结果：四个规则引用与 bundle 服务测试文件全部通过，不再访问 apps/rule-engine 或 ruleengine/backend。

16. **待测试**：ruleengine 退役 docs 删除与目录规则回归
   - 变更内容：删除54个无调用方的退役 AI 工厂设计文件；README 与 OPTION 文档改为唯一现行说明；结构规则和源码归属门禁禁止恢复 docs。
   - 测试命令：powershell -NoProfile -Command "if (Test-Path apps/ai-desktop/ruleengine/docs) { exit 1 }; if ((Get-ChildItem apps/ai-desktop/ruleengine -Recurse -File).Count -ne 242) { exit 1 }; $r=Get-Content -Raw -Encoding UTF8 apps/ai-desktop/ruleengine/rules/active-user/rules/应用/RUL_Python核心能力与Util结构规则.md; if (-not $r.Contains('python_core.forbidden_top_level_directory = docs')) { exit 1 }"
   - 预期结果：docs 不存在，ruleengine 保留242个正式文件，用户层结构规则明确禁止恢复 docs。

## 2026-08-27 实际执行结果

1. **失败**：新 `apps/ai-desktop/ruleengine` 恰有 242 个正式文件，但旧 `apps/rule-engine` 仍有 131 个文件。
2. **通过**：启动状态为 completed，协议按 STARTER、USER、CODE、COMMAND、RULE_INDEX、GENERATOR_REPAIR_PROTOCOL 顺序加载。
3. **失败**：最终全量门禁运行 179 项规则测试，4 项失败、2 项错误；当前 ruleengine 内无 pyc 或 `__pycache__`。失败包括核心层路径识别、两处规则计数仍写 79（实际 81）、未定义 `backend_root`、`.vscode` 路径和遗留旧目录违规。
4. **通过**：两个指定 MDA 测试类均通过。
5. **失败**：快速门禁发现 7 条违规，均来自遗留 `apps/rule-engine`。
6. **失败**：`./gradlew selplatFullGate ... --offline` 在 `selplatAllRuleTests` 阶段因 4 项失败、2 项错误停止。
7. **通过**：`OPTION/AI Desktop目录结构用途说明.md` 已按真实结构生成，7 个正式顶层目录、工程外数据边界与 UTF-8 检查通过。
8. **通过**：`npm run test:boundaries` 共 6 项全部通过。
9. **通过**：`npm run build:developer` 成功，构建 520 个模块并生成 9 项生产规则。
10. **通过**：`npm run test:rules` 共 3 项全部通过。
11. **通过**：Windows customer 包生成成功；校验 551 个 asar 条目和外置生产规则包通过。
12. **通过**：`npm run test:interaction` 共 21 项全部通过。
13. **通过**：以当前有效作用域 `selplat` 成功加载目标规则；清单中的 `selplat/应用/ai-desktop` 是过期作用域。
14. **通过**：五个正式入口存在，当前 ruleengine 不含 backend、src、docs、Java 式 `com` 目录、pyc 或 `__pycache__`。
15. **通过**：精确执行清单中的四个测试文件，共 52 项全部通过。
16. **通过**：ruleengine docs 不存在，正式文件数为 242，结构规则明确禁止恢复 docs。

未通过项保持未验证状态，不得归档为全量门禁通过。

## 2026-08-27 修复后最终结果

- 已删除未被 Git 跟踪的旧 `apps/rule-engine` 目录及其中131个遗留文件。
- 已修复 active-user 层识别、规则索引计数81、核心测试未定义变量和可移植 Gradle 缓存路径。
- 已补齐 AI Factory 与 MDA 的最小 Spring Boot 测试入口，并让 MDA 测试装配生产一致的公共 Web 参数解析器。
- 已同步 Host 对当前10条固定角色树的断言，并补齐 Reference Data 的空选项组 Code 分支覆盖。
- `selplatQuickGateCore`：检查13个语言根，违规数0。
- `selplatAllRuleTests`：179项测试，0失败、0错误。
- `selplatFullGate`：68个任务执行完成，最终 `BUILD SUCCESSFUL`。

原第1、3、5、6项失败均已修复并复测通过，本测试清单现已达到全量门禁通过状态。

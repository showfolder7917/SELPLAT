# AI Desktop 任务协作群统一时间线改造测试文档

- threadId：`01a04ce5-d6e0-7f41-ba3b-2da57b044576`
- 工程：`apps/ai-desktop`、`shared/frontend/sel-ui`
- 状态：统一测试已执行并通过（2026-08-29）
- 覆盖要求：前端按钮、布局、流式输出、后端投影、审批、多人并行、异常日志和完整业务流程全部登记

## 统一测试清单

### 1. TypeScript 与 Electron 构建

- [x] `npm --prefix apps/ai-desktop run typecheck`
- [x] `npm --prefix apps/ai-desktop run build:electron`
- 预期：Renderer、contracts、preload、IPC 与主进程时间线投影类型全部通过；新 preload 能力进入单文件沙箱产物。

### 2. 新时间线后端契约

- [x] `npm --prefix apps/ai-desktop run test:collaboration-timeline`
- 预期：申请、未通过、补充申请、通过、分发、多人分析/执行/验证、修复与结果按时间追加；同一任务转交不会覆盖前一人物；1、2、10 人并行均保持稳定顺序。
- 预期：自动审批关闭时只有最新待审批申请携带 `manualApprovalProposalId`；历史节点和自动审批开启状态不出现手动审批入口。

### 3. 协作、演化与异常事件中心回归

- [x] `npm --prefix apps/ai-desktop run test:collaboration`
- [x] `npm --prefix apps/ai-desktop run test:nangong-evolution`
- [x] `npm --prefix apps/ai-desktop run test:workflow-event-center`
- 预期：审批、任务分发、执行失败、令狐修复、统一测试及验收状态继续由原业务服务写入；新投影只读，不形成第二套状态机。
- 预期：IPC、审批和投影异常通过 `EventCenterFacade` 登记技术异常或业务异常，包含 topicId、proposalId、taskId、actor 等关联事实；令狐老祖现有监听能查询并接手。

### 4. SELUI 中央控件与沙箱白名单

- [x] `npm --prefix apps/ai-desktop run test:sel-ui`
- [x] `npm --prefix apps/ai-desktop run test:sel-ui-shared`
- [x] `npm --prefix apps/ai-desktop run test:preload-sandbox`
- 预期：任务卡、人物节点和二级详情全部使用正式 `selDisclosure`；审批框使用 `selWindow`，可拖动、向下及八方向缩放，内部内容滚动且底部操作区稳定；Renderer 不直接访问 IPC。

### 5. 真实页面全部按钮与流程覆盖

- [x] `AI_DESKTOP_TEST_TASK_ID=task-collaboration-timeline-01a04ce5 npm --prefix apps/ai-desktop run test:interaction`
- 逐项点击：执行列表、任务协作群、所有人物入口、新增人物、任务卡展开/收起、每一个人物节点展开/收起、执行详情、验证详情、手动审批、审批通过、退回补充、驳回、审批取消、任务详情返回、继续执行、取消任务、定位当前步骤。
- 逐流程覆盖：首次申请等待审批；未通过后补充申请；审批通过后多人分发；1 人执行；2 人并行；10 人及以上并行；执行与验证同时进行；等待接手；执行失败转令狐；令狐修复后退回原执行人；统一测试；结果汇总与验收。
- 预期：未发生的未来人物节点不显示，只显示一个下一流程；当前执行/验证节点自动展开且圆点闪烁；完成后默认收起但记录仍可展开；多个节点可同时展开。

### 6. 流式输出与覆盖回归

- [x] 在真实 Electron 页面以受控时间线夹具连续产生同一专题多个节点、不同人物并行流，并验证刷新后重新投影。
- 预期：流式正文只更新所属当前人物节点；旧人物、旧回合和旧分析正文不被替换；完成事件只结束自己的节点；刷新与重启后持久摘要仍按原顺序恢复。
- 预期：自然语言正文直接显示，命令、文件、原始步骤与证据只出现在二级“执行详情/验证详情”，真正依赖路径错误不得被通用错误摘要覆盖。

### 7. 布局与可访问性 100% 场景覆盖

- [x] 在 `1560×980`、`1366×768`、`1000×700` 三种正式窗口尺寸检查并截图。
- [x] 检查 1、2、6、10、12 人节点密度；滚动到首屏、中部和末尾；展开最长审批内容、最长执行内容和最长验证清单。
- [x] 键盘逐项 Tab、Enter、Space、Escape 验证所有 Disclosure、手动审批 Window 与操作按钮；核对 `aria-expanded`、焦点可见性和状态文字。
- 预期：并行区域始终单列，无横向挤压和嵌套滚动条；只使用页面主滚动条；标题、状态、时长和按钮不遮挡、不裁切；每个人都有展开箭头。
- 证据要求：保存真实 URL/窗口尺寸、每个尺寸截图、按钮结果、`result.json`、控制台异常摘要和失败 trace。

### 8. 旧实现隔离与退役准备

- [x] 静态检查新 `TaskCollaborationGroup` 不导入 `deriveCollaborationTaskProgress`，新 IPC 只调用 `buildCollaborationTimeline`。
- [x] 验证旧人物页四阶段视图仍可回退访问，但不会写入、重排或覆盖新任务协作群投影。
- 预期：新接口稳定后可在独立任务中删除旧模型、旧样式和旧契约断言，不影响时间线状态事实。

### 9. 规则与结构门禁

- [x] 使用当前环境可用的新版本 Python 运行分层规则加载与结构严格门禁。
- [x] `./gradlew -PselplatPython=/usr/local/bin/python3.11 selplatQuickGate`
- 预期：当前用户规则索引可达，`hardViolationCount=0`，源码归属违规为 0，无机器绝对路径和缓存污染。
- 沉淀结论：升级现有 `AI_DESKTOP_COLLABORATION_AUTOMATION_RULES`，版本 `5.108.0`；近义规则为 `collaboration_task_progress_view_contract`，原规则只约束人物当前任务阶段，无法覆盖专题级追加时间线、人工审批和十人并行，因此在同一规则内新增专题投影契约并同步叶子索引说明。

## 统一测试结果

- 通过：TypeScript、Electron 与 Developer 生产构建。
- 通过：协作时间线 5/5；协作总套件 47/47；南宫婉演化 35/35；事件中心 18/18；SELUI 13/13；Electron 真实交互 24/24；managed 38/38；共享 SELUI Gradle 2 项；preload 沙箱白名单 110 项。
- 唯一自动化用例数：175（`test:collaboration` 已包含 5 个时间线用例，因此不重复累计）。
- 页面证据：任务协作群已保存 `1560×980`、`1366×768`、`1000×700` 三档截图；三个尺寸均无横向溢出。
- 测试中发现并修复：嵌套 Disclosure 事件冒泡导致父任务卡被误收起；任务协作群字号未使用 SELUI 主题令牌；人工审批三类结论的受控输入等待不一致。
- 门禁：`selplatQuickGateCore` 检查 13 个语言根，`violationCount=0`；执行文档授权门禁通过；`git diff --check` 通过。
- 最终结论：测试文档登记的按钮、布局、时间线、人工审批、多人并行、异常/业务日志、旧实现隔离和完整流程场景全部通过。

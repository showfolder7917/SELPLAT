# 本次测试文档

任务线程：01a045c9-6d55-7ed2-ab53-72042d033390

## 统一测试清单

1. **待测试**：依赖缓存开发链接契约测试
   - 变更内容：验证 Windows Junction 与 macOS/Linux 目录符号链接共用同一实现，并覆盖当前哈希幂等、旧哈希替换、失效链接、外部目标阻断和实体目录保护。
   - 测试命令：cd apps/ai-desktop && node --test tests/dependency-cache-self-heal.test.mjs
   - 预期结果：全部依赖缓存契约测试通过，失败数为 0。

2. **待测试**：当前平台开发依赖链接建立
   - 变更内容：验证 dependencies:link 使用当前 package-lock 哈希准备缓存并建立仅指向本应用 cache 的本机链接。
   - 测试命令：cd apps/ai-desktop && npm run dependencies:link
   - 预期结果：命令成功；Windows 的 node_modules 为 Junction，macOS/Linux 为目录符号链接，真实目标位于 cache/ai-desktop/dependencies/<lockHash>/node_modules。

3. **待测试**：AI Desktop TypeScript 类型检查
   - 变更内容：验证常驻开发链接不会被受控命令误删，并且 VS Code 使用的渲染端 TypeScript 配置可解析全部依赖。
   - 测试命令：cd apps/ai-desktop && npm run typecheck
   - 预期结果：TypeScript 检查退出码为 0，命令后 node_modules 仍指向当前锁哈希缓存。

4. **待测试**：SELPLAT 源码归属快速门禁
   - 变更内容：验证新增 Node 脚本位于已登记 AI Desktop scripts 生命周期，且不存在源码污染、错误语言根或路径逃逸。
   - 测试命令：$env:SELPLAT_PYTHON=(Get-Command python -ErrorAction Stop).Source; ./gradlew.bat selplatQuickGate
   - 预期结果：快速门禁成功，源码归属与结构违规数为 0。

5. **待测试**：跨机器绝对路径提交扫描
   - 变更内容：验证新增实现、配置与说明未重新写入 Windows 或 macOS 用户目录绝对路径。
   - 测试命令：rg -n "/Users/|[A-Za-z]:[/\\]Users[/\\]" apps/ai-desktop/scripts apps/ai-desktop/package.json apps/ai-desktop/README.md
   - 预期结果：命令无匹配结果；仓库只保存动态路径解析逻辑，不保存任何机器用户目录。

6. **待测试**：依赖缓存清理后闭包验证
   - 变更内容：删除411个不属于package-lock依赖闭包的npm隐藏临时目录并保留Junction
   - 测试命令：npm --prefix apps/ai-desktop run dependencies:link && npm --prefix apps/ai-desktop run typecheck
   - 预期结果：Junction仍指向当前锁哈希缓存且TypeScript类型检查通过

7. **待测试**：规则引擎重构历史基线结构验证
   - 变更内容：重构完成后不再保留会被误认为当前执行方案的 OPTION 分析文档；可丢失历史区只保存重构前基线、已建立解耦边界和索引分片结果。
   - 测试命令：rg -n "^## " apps/ai-desktop/ruleengine/history/历史记录/规则引擎重构基线_20260828.md
   - 预期结果：输出3个主章节：重构前基线、已建立的解耦边界、索引分片结果；生产规则、程序和测试不依赖该历史文件。

8. **待测试**：rule-engine历史与素材零依赖回归
   - 变更内容：升级AI_RULE_PACKAGE_INTELLIGENCE_RULES并建立history/素材和history/历史记录目录契约及防复发测试
   - 测试命令：python apps/ai-desktop/ruleengine/tests/local/XUNAN/test_ai_rule_package_integrator.py
   - 预期结果：全部用例通过；生产索引不登记history文件；生产Python、manifest和scripts不依赖ruleengine/history

9. **待测试**：规则索引分片与稳定用户递归加载回归
   - 变更内容：验证两份父索引只导航，10份叶子索引可递归命中，active-user迁移后的逻辑ID和覆盖路径保持有效
   - 测试命令：python apps/ai-desktop/ruleengine/tests/local/core/test_layered_rule_loader.py && python apps/ai-desktop/ruleengine/tests/local/core/test_active_user_rule_override.py
   - 预期结果：两组用例全部通过；当前用户规则仅从local/XUNAN命中，无失效子索引、重复ID或路径逃逸

10. **待测试**：超大规则职责拆分与历史抽取一致性回归
   - 变更内容：验证235条upgrade_record已移入可丢失历史区，23个新职责规则由原聚合ID依赖闭包加载且当前DSL无缺失
   - 测试命令：python apps/ai-desktop/ruleengine/tests/local/XUNAN/test_ai_rule_package_integrator.py
   - 预期结果：全部用例通过；活跃规则升级历史为0，新增逻辑ID可索引，history缺失不影响审计

11. **待测试**：源码归属领域模块单元测试
   - 变更内容：验证路径身份、语言登记、前端治理、数据库治理和薄入口兼容模块可独立执行
   - 测试命令：python -m unittest discover -s apps/ai-desktop/ruleengine/tests/local/XUNAN -p test_source_ownership_*.py
   - 预期结果：新增5个模块测试文件全部通过，旧入口继续导出原函数对象

12. **待测试**：源码归属原入口完整兼容回归
   - 变更内容：验证模块化后原2200行门禁的违规集合、顺序、字段和既有业务检查保持兼容
   - 测试命令：python apps/ai-desktop/ruleengine/tests/local/XUNAN/test_selplat_source_ownership_guard.py
   - 预期结果：原入口全部既有用例通过，失败数为0且源码树不生成__pycache__

13. **待测试**：规则引擎结构严格门禁
   - 变更内容：验证索引路径、逻辑ID、触发器、依赖、未索引规则、active-user、源码缓存、生产清单和history零依赖硬违规
   - 测试命令：python apps/ai-desktop/ruleengine/python/local/XUNAN/abilities/ruleengine_structure_guard.py --strict && python apps/ai-desktop/ruleengine/tests/local/XUNAN/test_ruleengine_structure_guard.py
   - 预期结果：严格门禁退出码为0、hardViolationCount为0，门禁单测通过；体量阈值仅作为报告项

14. **待测试**：AI Desktop生产规则包职责依赖交付回归
   - 变更内容：验证Harness聚合规则及其5个新职责规则均进入生产清单，同时history始终被白名单排除
   - 测试命令：cd apps/ai-desktop && node --test tests/rule-bundle-service.test.mjs
   - 预期结果：规则包服务用例全部通过，生产清单资源存在且不包含history/archive/tests/templates/python/会话目录

15. **待测试**：规则引擎跨平台机器路径语义扫描
   - 变更内容：使用结构门禁区分真实机器绝对路径与扫描器自身正则定义，避免原始文本检索把检测代码误报为违规。
   - 测试命令：python apps/ai-desktop/ruleengine/python/local/XUNAN/abilities/ruleengine_structure_guard.py --strict
   - 预期结果：命令退出码为0、hardViolationCount为0、machineAbsolutePathHits为空；所有工程资源路径由SELPLAT_ROOT、稳定用户或当前平台动态解析。

16. **待测试**：Rule intelligence aggregation conflict candidate and bundle benchmark regression
   - 变更内容：Verify current-user gaps and stale references reach top-level totals, semantic candidates remain evidence-only, stale authority paths are removed, and bundle metrics retain receipts plus explicit proxy disclaimers.
   - 测试命令：python apps/ai-desktop/ruleengine/tests/local/XUNAN/test_ai_rule_intelligence_reporting.py && python apps/ai-desktop/ruleengine/tests/local/XUNAN/test_ai_rule_package_integrator.py
   - 预期结果：Both suites pass; stale_reference_count is 0; aggregation matches all audited rules; benchmark receipt and proxy disclaimers are present; no source cache is created.

17. **待测试**：AGENTS 入口瘦身与规则沉淀完成门禁回归
   - 变更内容：验证根 AGENTS.md 只保留启动与不可绕过完成门禁，详细沉淀治理由 RULE_LIFECYCLE_GOVERNANCE_RULES 承载，并可从根索引经当前用户叶子索引递归命中。
   - 测试命令：$content = Get-Content AGENTS.md -Raw -Encoding UTF8; if (($content -split "`n").Count -gt 90 -or $content -notmatch "RULE_LIFECYCLE_GOVERNANCE_RULES") { exit 1 }; python apps/ai-desktop/ruleengine/python/local/XUNAN/abilities/ruleengine_structure_guard.py --strict; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; python apps/ai-desktop/ruleengine/tests/local/core/test_layered_rule_loader.py
   - 预期结果：AGENTS.md 不超过 90 行并保留固定沉淀门禁；结构严格门禁 hardViolationCount 为 0；分层加载测试通过且完成阶段触发器唯一命中当前用户生命周期治理规则。

## 2026-08-28 统一测试执行结果

- 完成时间：`2026-08-28T18:51:23+0900`
- 总体结论：**失败，17 项中 7 项通过、10 项失败或组合门禁未全部通过**。

1. **失败** — 依赖缓存契约测试 `7/8` 通过；唯一失败是静态断言仍要求旧的 `lstatSync(details.linkPath).isSymbolicLink()` 写法，当前实现使用带 `throwIfNoEntry:false` 的安全探测。
2. **通过** — `dependencies:link` 成功；macOS `node_modules` 为目录符号链接，真实目标与当前锁哈希 `86c21a7d...09b737` 缓存完全一致。
3. **通过** — TypeScript 类型检查退出码 `0`，依赖链接仍有效。
4. **失败** — `selplatQuickGateCore` 已达到 `violationCount=0`；整体任务随后被 `selplatTaskDocumentActiveGate` 阻断，因为线程 `01a042e7-eb08-7593-9b42-411e562d9099` 没有活动执行文档。
5. **通过** — AI Desktop scripts、package.json、README 跨机器用户目录扫描无匹配。
6. **通过** — 依赖链接与 TypeScript 闭包验证通过。
7. **失败** — `OPTION/规则引擎重构分析.md` 不存在，无法验证 11 个章节。
8. **失败** — AI 规则包整合测试 `10/11` 通过；`RUL_AIDesktopHarness工作区与运行时规则.md:17` 的 DSL 声明缺少紧邻中文业务注释。
9. **失败** — 分层加载器 `11/12` 通过，active-user 覆盖回归失败；旧测试固定断言 `22/81`，当前实际为 `32/104`，且仍从拆分后的聚合规则正文查找已迁移职责字段。
10. **失败** — 与第 8 项相同，规则包整合测试仍有 `1` 项失败。
11. **通过** — 源码归属领域模块 `5/5` 通过。
12. **失败** — 原入口兼容回归 `56/58` 通过；两条断言仍从已拆分的公共控件聚合规则正文查找迁移后的职责/历史字段。
13. **通过** — 结构严格门禁完成，`hardViolationCount=0`、`advisoryCount=3`；门禁单测 `1/1` 通过。
14. **通过** — 规则包服务 `3/3` 通过；首次受系统临时目录沙箱限制，使用真实临时目录权限复跑后通过。
15. **失败** — 原始 `rg` 扫描命中 `ruleengine_structure_guard.py` 自身用于检测 macOS 路径的正则常量；严格结构门禁的语义扫描结果 `machineAbsolutePathHits=[]`。
16. **失败** — 规则智慧报告 `6/6` 通过，但组合中的规则包整合测试仍有第 8 项所述 `1` 项失败。
17. **失败** — AGENTS 为 `72` 行且保留生命周期门禁，严格结构门禁为 `0` 条硬违规；组合中的分层加载测试仍因旧 `22/81` 计数断言失败。

- 测试清理：本轮 Python 命令首次产生的三个 `__pycache__` 目录已清理；后续全部 Python 测试使用 `PYTHONDONTWRITEBYTECODE=1`，最终源码树无 `__pycache__`。
- 规则沉淀评估：**无需沉淀**。近义规则 `CODE_TEST_RULES` 与 `RULE_LIFECYCLE_GOVERNANCE_RULES` 已覆盖统一测试和完成门禁；本轮暴露的是待修复实现/测试记录，不新增近义长期规则。

## 2026-08-28 修复后统一测试执行结果

- 完成时间：`2026-08-28T20:02:41+0900`
- 总体结论：**17 项全部通过**。保留上一轮失败记录作为修复证据，以下为修复后的权威结果。

1. **通过** — 依赖缓存契约测试 `8/8`；静态断言已跟随安全 `lstatSync(..., { throwIfNoEntry:false })` 实现。
2. **通过** — `dependencies:link` 成功；macOS `node_modules` 继续指向当前锁哈希缓存。
3. **通过** — TypeScript 类型检查退出码 `0`，Electron 构建成功。
4. **通过** — `selplatQuickGate` 构建成功，源码归属 `violationCount=0`，活动执行文档门禁通过。
5. **通过** — AI Desktop scripts、package.json、README 的机器用户目录扫描无匹配。
6. **通过** — 依赖链接与 TypeScript 闭包验证通过。
7. **通过** — 当前历史基线文件存在且恰有 `3` 个主章节：重构前基线、已建立的解耦边界、索引分片结果。
8. **通过** — AI 规则包整合测试 `11/11`；拆分叶规则中文业务注释完整。
9. **通过** — 分层加载 `12/12`、active-user 覆盖 `3/3`；测试已改用拆分后的权威叶规则路径及当前用户迁移基线 `32/104`。
10. **通过** — 超大规则职责拆分与历史抽取一致性由规则包整合测试 `11/11` 验证。
11. **通过** — 源码归属领域模块 `5/5`。
12. **通过** — 源码归属原入口兼容回归 `58/58`，源码树无 `__pycache__`。
13. **通过** — 严格结构门禁 `hardViolationCount=0`、`advisoryCount=3`，门禁单测 `1/1`；全树报告当前为 `34` 个索引、`115` 条逻辑规则。
14. **通过** — AI Desktop 生产规则包服务 `3/3`，生产白名单不包含历史和开发资源。
15. **通过** — 结构门禁语义扫描 `machineAbsolutePathHits=[]`，不再用会命中扫描器自身正则的原始文本检索判断规则引擎路径。
16. **通过** — 规则智慧报告 `6/6`，规则包整合 `11/11`。
17. **通过** — `AGENTS.md` 为 `72` 行并保留 `RULE_LIFECYCLE_GOVERNANCE_RULES` 完成门禁；严格结构硬违规为 `0`，分层加载 `12/12`。

- 补充应用统一回归：boundaries `7/7`、SELUI `11/11`、南宫婉 `19/19`、协同 `44/44`、事件中心 `11/11`、开发设置 `4/4`、平台依赖 `2/2`、沙箱 `100` 项能力、真实交互 `22/22` 全部通过。
- 规则沉淀评估：**无需新增规则**。`CODE_TEST_RULES`、`AI_RULE_PACKAGE_INTELLIGENCE_RULES` 与 `RULE_LIFECYCLE_GOVERNANCE_RULES` 已覆盖本次陈旧断言、拆分规则注释和完成门禁场景；现有规则与测试已完成同步修正。

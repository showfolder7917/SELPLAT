# AI Desktop 韩立验收恢复链测试文档

## 待测项

- [x] 在 `apps/ai-desktop` 运行 `node --test tests/services/workflow/checkpoint-coordinator.test.mjs`：预期旧任务不重开，新任务接管且重放去重；40/40 通过。
- [x] 在 `apps/ai-desktop` 运行 `npm run typecheck`：预期 TypeScript 类型检查通过；已通过。
- [ ] 当前修复链集成后运行项目统一测试：预期无失败且涵盖验收恢复链。
- [ ] 新候选打包重启：预期运行包包含 `da57ee82`，渲染器健康，韩立真实验收完成。

## 状态

本轮定向验证通过；完整统一测试和真实运行验收尚未完成，不将本轮修改标记为最终验收通过。

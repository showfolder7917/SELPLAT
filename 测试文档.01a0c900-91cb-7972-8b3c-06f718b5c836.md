# AI Desktop 韩立验收恢复链测试文档

## 待测项

- [x] 韩立验收中不把旧令狐 current 节点与韩立同时统计为执行人；客户等待与验收中两态行为回归 13/13 通过。
- [ ] 待当前真实验收运行结束后提交并受控重启，再以正式页面核对角标与真实成员占用一致。

- [x] 正式页面发现客户确认阶段仍显示旧令狐“执行中”角标；行为回归覆盖等待客户时清空活动人数、实际运行时仍保留人物，定向 13/13、类型检查通过。
- [ ] 本地提交并重启后确认当前卡不再同时显示客户操作与旧令狐执行角标，再启动韩立复验。

- [x] 临时验收场景退役，正式窗口读取真实状态、仍拦截业务写入；定向 28/28、类型检查、完整 `npm test` 806 项（804 通过、2 跳过、0 失败）。
- [x] 四组固定回归：交互 40/40、协作 154/154、托管 52/52；源码证据全文覆盖旧恢复分支，额外退役文件缺失用例待定向复检。
- [ ] 本地提交后候选打包、受控重启、同提案韩立按冻结条件真实复验；未实际发生的状态必须保留受阻，不得以临时投影认定通过。

- [x] 条件 5 的当前验收节点状态一致性：客户确认阶段展示客户动作，历史节点及其他验证节点不覆盖；定向 26/26 与 TypeScript 类型检查通过。
- [ ] 隔离候选包重启后重新观察真实页面与韩立裁决；此代码修正不等于条件 1、5、8 或源码结构审查已通过。

- [x] 系统仅持有验收能力卡点、无令狐活动任务时重新显示原运行复验入口；令狐正在统一测试时仍不重复开放。状态投影定向回归 32/32、类型检查通过。
- [x] 本轮状态投影修正完成完整固定门禁：`npm test` 806 项（804 通过、2 跳过、0 失败），`test:interaction` 40/40、`test:collaboration` 154/154、`test:managed` 52/52。
- [x] 实机再现旧 blocked 修复任务遮挡后，改为只看有效替代链；定向 32/32、类型检查、主回归 806 项（804 通过、2 跳过、0 失败）。
- [x] `09e573ae` 隔离打包重启后，原专题恢复入口真实出现，点击后运行进入韩立验收。最新验收条件 2、3、4、7 通过；1、5 失败，6、8 受阻，源码结构审查失败，不得记为最终通过。
- [x] “成员均 idle”验收条件补入任务协作群页面分类，定向契约 14/14 通过。
- [ ] 独立非生产数据验收边界待确认；确认后逐项复验四个真实状态，不得用窗口临时场景替代正式业务事实。
- [ ] 本轮状态投影修正本地提交、隔离打包重启，并在真实页面确认同一专题可复验及韩立结果。

- [x] 在 `apps/ai-desktop` 运行 `node --test tests/services/workflow/checkpoint-coordinator.test.mjs`：预期旧任务不重开，事件专属新任务挂到最新失败事件并只恢复一次，旧卡点显示接管而非轮次耗尽；40/40 通过。
- [x] 在 `apps/ai-desktop` 运行 `npm run typecheck`：预期 TypeScript 类型检查通过；已通过。
- [ ] 当前修复链集成后运行项目统一测试：预期无失败且涵盖验收恢复链。
- [ ] 新候选打包重启：预期运行包包含 `da57ee82`，渲染器健康，韩立真实验收完成。
- [x] 定向运行 `node --test tests/features/collaboration/managed-task-executor-contract.test.mjs`：一次性审批、受管 Node 测试及旧冲突停止门禁断言通过，18/18。
- [x] 定向运行 `npm run typecheck`：审批桥接参数及页面类型通过。
- [ ] 新包实际验证：受管令狐任务发起 `node --test` 不再弹出重复信任审批；确需审批时提供“仅允许本次”，不会持久保存授权。
- [ ] 重启后观察旧合并冲突任务：无源码差异后不再自动从 r9 升到新轮次；新修复结果继续韩立验收。
- [x] 定向运行 `node --test tests/system/hanli-page-review-guard.test.mjs tests/features/collaboration/managed-task-executor-contract.test.mjs` 与 `npm run typecheck`：20/20 通过，类型检查通过。
- [ ] 新包实际验证：韩立正式页面验收期间不出现其他人物的命令审批遮挡，验收结束后未处理请求仍可一次性批准。
- [x] 定向运行 `node --test tests/system/hanli-page-review-guard.test.mjs` 与 `npm run typecheck`：正式窗口后台推送隔离断言 3/3 通过，类型检查通过。
- [ ] 新包真实验收：无指导场景及完整指导场景不再被后台 Evolution/协作状态覆盖；条件 1、5 通过。
- [x] 定向运行 `node --test tests/services/workflow/equivalent-repair.test.mjs tests/system/hanli-page-review-guard.test.mjs` 与 `npm run typecheck`：严格等价接管和验收窗口隔离 5/5 通过，类型检查通过。
- [x] 运行 `npm run build:electron`：新增事件类型与时间线投影通过 Electron TypeScript 构建；首次在只读沙箱因依赖链接权限失败，按项目构建权限重跑后通过。
- [ ] 新包启动恢复：r16 旧任务因后续等价集成结果退出自动派发，保留原工作树文件与审计，不再要求批准同一提交。
- [x] 定向运行 `CURRENT_OS=macos node scripts/run-with-dependencies.mjs node --test tests/services/workflow/fixed-unified-test-gate.test.mjs`：4/4 通过；同一脚本早晚两个失败块都出现在一轮结构化失败中，且其余固定验证脚本继续执行。
- [x] 运行 `npm run typecheck`：统一测试输出收集器的 TypeScript 类型检查通过。
- [ ] 现有协作任务合并后运行完整统一测试：预期一轮呈现所有失败用例位置，修复后不因早期失败被末尾日志覆盖而再次漏检。
- [x] 运行 `CURRENT_OS=macos node scripts/run-with-dependencies.mjs node --test tests/services/workflow/checkpoint-coordinator.test.mjs`：41/41 通过；旧任务已集成且发生独立新验收故障时退出主卡点，仅为新故障派发一份修复任务，重复轮询不新增任务。
- [x] 运行 `npm run typecheck`：卡点协调器类型检查通过。
- [ ] 本地提交后以隔离候选包重启正式应用：预期旧 processing 卡点退出、新事件进入令狐修复，旧任务和验收失败证据仍保留；继续观察韩立结果验收。
- [x] 第一版隔离打包与启动成功；实机旧修复任务卡点退出，但又发现更早的多条无任务历史事件依次抢占，未达到新事件派发，已据此补做结构性修正。
- [x] 同轮运行卡点协调器与固定统一测试门禁测试：46/46 通过；覆盖多条无修复关系旧事件接连退出、新故障仅派发一次，并覆盖早晚失败用例收集。
- [x] 运行 `npm run typecheck`：结构性修正类型检查通过。
- [ ] 结构性修正本地提交并重新隔离打包启动：预期所有旧无任务事件退出，新故障关联一份真实令狐修复任务。
- [x] `db7b8c05` 已本地提交并隔离打包启动；真实状态中旧无任务事件退出，新故障只关联一份令狐任务。g415、g416 均通过统一测试及受控重启；韩立复验仍因源码证据和成员状态取证不足而受阻，不能记为最终通过。
- [x] 运行 `CURRENT_OS=macos node scripts/run-with-dependencies.mjs node --test tests/services/workflow/hanli-review-contract.test.mjs tests/services/personas/hanli-task-collaboration-scenario.test.mjs`：首次暴露一处源码括号错误，同轮修正后 17/17 通过；源码证据包含同提案原任务与修复任务，不读取测试或越界文件，验收窗口不把真实 actor 再投影成忙碌。
- [x] 运行 `npm run typecheck`：受限源码证据和窗口场景类型检查通过。
- [x] `ad98e98c` 本地提交后完整运行四组固定门禁：`npm test` 805 项（803 通过、2 跳过、0 失败），`npm run test:interaction` 40/40，`npm run test:collaboration` 154/154，`npm run test:managed` 52/52；加上定向 17/17 和类型检查均通过。
- [ ] 本地提交、隔离打包重启后韩立正式复验：条件 3、4、6、8 应能取得原范围内的页面与源码证据；最终仅以实际韩立验收结果为准。

## 状态

`ad98e98c` 的完整统一测试、定向测试和类型检查通过。隔离打包、真实运行和韩立验收尚未完成，不能标记为最终验收通过。

当前技术恢复投影拆分新增回归：新验收不能显示旧验收指纹或旧任务故障的原因、下一步；同轮指纹仍显示本轮恢复。`current-topic-stage.projection.test.mjs` 与 `hanli-review-contract.test.mjs` 定向 50/50 通过，源码取证测试同步核对拆分后的两个正式文件。完整统一测试及正式页面复验以本轮实际结果为准，不能由定向测试代替韩立结论。

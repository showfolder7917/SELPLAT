# 从权限工程抽取基线步骤规范

基线来源：

- `SELATTENDANCE/SELSP/src/main/java/com/sp/selfsp/uniauth`

优先抽取顺序：

- `user`
- `role`
- `menu`
- `module`
- `tenant`
- `auth`

目标：

- 把 `uniauth` 提炼成样板模块
- 抽出通用 Java 规范与模块私有实现边界

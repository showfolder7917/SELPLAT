# 包结构规范

目标：

- 保持模块目录和 Java 包结构一致
- 保持每个模块的分层稳定

推荐结构：

- `controller`
- `service`
- `service.impl`
- `dao`
- `dao.mapper`
- `domain.in`
- `domain.out`

约束：

- 不在同一模块中混入无边界的“大杂烩”包
- 不跨模块直接共享业务私有包
- 公共能力应进入公共模块或共享层，不应散落在业务包内部

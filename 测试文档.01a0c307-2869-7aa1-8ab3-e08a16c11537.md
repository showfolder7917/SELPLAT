# AI Desktop 卡点分流修复测试

## 待验证项

- 命令：`cd apps/ai-desktop && node --test tests/services/workflow/checkpoint-coordinator.test.mjs`
  - 预期：旧主卡点三轮耗尽后，后来出现的独立验收事实不再被旧卡点拦截，并创建具有独立持久标识的新修复任务。
- 命令：`cd apps/ai-desktop && npm run typecheck`
  - 预期：AI Desktop TypeScript 类型检查通过。
- 命令：`cd apps/ai-desktop && node --test tests/services/workflow/collaboration-timeline.test.mjs tests/features/collaboration/collaboration-status-chain-contract.test.mjs`
  - 预期：同一专题保留一张顶层卡，内部按持久化 taskId 展示“专题流程、原始实现任务、问题卡 1/2…”，同一问题的真实修复轮次显示为 r1、r2，且旧快照仍可回退为平铺时间线。

## 统一测试状态

- 定向测试：通过（36/36）。
- TypeScript 类型检查：通过。
- 任务卡层级定向测试：通过（与时间线回归合计 65/65）。
- 规则包构建与哈希校验：通过（145 个文件）。
- 尚未触发新的完整统一测试；本轮先执行与修复风险匹配的定向验证。

# modules

这里放每个模块的元数据文件。

当前已存在：

- `host.json`
- `reference-data.json`
- `uniauth.json`
- `attendance.json`
- `rule-engine.json`
- `crm.json`
- `cms.json`
- `fujitsu.json`

每个文件说明：

- 模块是否启用
- 模块路径
- manifest 路径
- 依赖模块

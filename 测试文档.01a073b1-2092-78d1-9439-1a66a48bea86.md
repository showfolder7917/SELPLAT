# 韩立会话页面结构重构测试文档

## 当前状态

- 状态：待统一测试
- 本轮只完成规则加载与索引验证；类型检查、业务测试、构建和真实页面测试等待“统一测试”。

## 待测项

1. TypeScript 类型检查
   - 命令：在 `apps/ai-desktop` 执行 `npm run typecheck`
   - 预期：新建的 Props 类型、控制 Hook 和 View 均通过类型检查。

2. 韩立页面结构契约
   - 命令：在 `apps/ai-desktop` 执行 `node --test tests/features/hanli/hanli-conversation-workspace-structure.test.mjs`
   - 预期：数据结构、控制 Hook 和 View 同级且职责分离；自动托管位于主页面同名子模块目录；文件头及关键页面节点注释存在。

3. 人物会话滚动回归
   - 命令：在 `apps/ai-desktop` 执行 `node --test tests/features/conversation/persona-conversation-scroll-contract.test.mjs`
   - 预期：迁移到控制 Hook 后，韩立问答区仍在用户停留底部时跟随最新消息。

4. 截图输入回归
   - 命令：在 `apps/ai-desktop` 执行 `node --test tests/applications/screenshot/user-input-and-screenshot-contract.test.mjs`
   - 预期：韩立页面仍保留截图、隐藏窗口截图、粘贴图片和附件发送能力。

5. 真实页面交互验证
   - 命令：启动真实 AI Desktop Developer 页面并进入左侧人物树“韩立”。
   - 预期：会话区、客户问答区、附件区、文字输入区和操作区布局保持不变；发送文字、粘贴截图和移除截图均可正常操作，并保存截图或等价可视证据。

6. 韩立上下文理解与内部交接回归
   - 命令：先构建 Electron，再执行 `node --test tests/services/workflow/hanli-inquiry.test.mjs`
   - 预期：韩立对“修复这个”等指代短句结合前文和截图形成完整理解；内部提问包含客户原话、完整目标、核实对象、期望结论、调查范围和原始截图身份。

7. 内部消息附件持久化回归
   - 命令：先构建 Electron，再执行 `node --test tests/services/support/capabilities/event-center/workflow-event-center.test.mjs`
   - 预期：内部研讨消息的附件 ID 写入并读取后保持一致，且内部消息仍不进入客户训练语料。

8. 提示词清单构建验证
   - 命令：在 `apps/ai-desktop` 执行 `npm run build:prompts`
   - 预期：`hanli.conversation` 与 `hanli.inquiry-response` 的新版本通过变量、内容哈希和清单校验。

9. 当前用户规则沉淀验证
   - 命令：通过 rule-engine 分层加载器读取 `AI_DESKTOP_HANLI_USER_QUESTIONING_RULES`，并执行规则索引可达性、重复逻辑 ID 与跨用户引用检查。
   - 预期：版本 2.13.0 可从根索引沿当前用户索引链唯一到达，新增上下文指代、完整转述、截图交接和非敷衍回复约束生效，索引路径无需变更。
   - 结果：已完成。分层加载命中当前用户 `XUNAN` 的唯一规则文件；用户索引校验通过，共检查 33 个索引和 110 条规则。

10. 自动托管新手结构回归
    - 命令：包含在 `node --test tests/features/hanli/hanli-conversation-workspace-structure.test.mjs` 中。
    - 预期：`HanliCustodySwitch` 保持页面专属子模块归属，异步保存使用具名方法，数据、Hook 与关键可见节点具有逐项业务注释。

11. 新手页面分层规则验证
    - 命令：通过 rule-engine 分层加载器读取 `AI_DESKTOP_ARCHITECTURE_BOUNDARY_RULES`，并执行当前用户规则索引校验。
    - 预期：版本 2.18.0 可唯一到达，并包含主页面分层、新手注释、中文业务名称优先、跨层调用参数逐项说明、整目录注释检查及禁止机械拆分的例外约束。
    - 结果：已完成。规则加载命中当前用户 `XUNAN` 的版本 2.18.0；用户索引校验通过，共检查 33 个索引和 110 条规则。

12. 南宫婉会话页面结构回归
    - 命令：在 `apps/ai-desktop` 执行 `node --test tests/features/nangong/nangong-conversation-workspace-structure.test.mjs`。
    - 预期：页面参数、控制逻辑、页面结构同级分离；后台动作进入页面同名子模块目录；页面结构不直接调用桌面后端。

13. 南宫婉真实页面交互验证
    - 命令：启动 AI Desktop Developer 页面并点击左侧人物树“南宫婉”。
    - 预期：直接问答、内部研讨、持续演化确认、截图粘贴、课题草稿生成和确认保存行为保持不变，并保存截图或等价可视证据。

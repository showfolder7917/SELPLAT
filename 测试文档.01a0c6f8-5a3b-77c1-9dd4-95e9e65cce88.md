# AI Desktop 令狐产品修复与运行证据门禁测试

## 待测项

- [x] `npm --prefix apps/ai-desktop run typecheck`：通过。
- [x] `node --test apps/ai-desktop/tests/features/collaboration/managed-task-executor-contract.test.mjs`：16 项通过。
- [x] `npm --prefix apps/ai-desktop run test:collaboration`：154 项通过，包含产品变更门禁及旧任务迁移回归。
- [x] `npm --prefix apps/ai-desktop test`：778 项中 776 通过、0 失败、2 项按设计跳过。
- [x] `node --test tests/services/workflow/checkpoint-coordinator.test.mjs tests/services/workflow/repair-investigation-context.test.mjs`（在 `apps/ai-desktop` 目录）：44 项通过；令狐调查上下文要求绝对时间、运行 SHA ancestry 及持久化到真实 DOM 的同标识证据链，自动托管超过三轮仍由令狐继续调查。
- [x] `npm --prefix apps/ai-desktop run typecheck`：新增调查门禁与卡点投影类型检查通过。
- [x] `npm --prefix apps/ai-desktop run test:collaboration`：154 项通过。
- [x] `npm --prefix apps/ai-desktop test`：779 项中 777 通过、0 失败、2 项按设计跳过。
- [x] `/Users/showfolder/Documents/workSpace/SELF/SELPLAT/apps/ai-desktop/启动开发版.command`：完成最新自包含开发版构建、签名、旧实例关闭、应用注册与健康启动；新进程 PID 67365。

## 预期结果

- 产品缺陷任务冻结为 `production-source`；字段上线前已带产品缺陷冻结标记的任务在重启加载时升级为同一门禁，普通旧任务仍兼容为 `any-source`。
- 只有至少一个非测试、非文档的产品实现文件发生变更，任务才可进入代码级自检。
- 三轮仍只有测试或文档变更时返回明确未完成原因，验证入口不被调用。
- 令狐不得再用不同时区的墙上钟点判断版本先后；页面问题必须定位数据库、窗口接口和真实 DOM 中第一个丢失边界。
- 自动托管中的真实新失败即使超过三轮，技术恢复状态仍保持令狐接手；只有人工暂停、业务选择、取消或确切外部阻塞才进入监控等待。

## 执行结果

2026-09-22 已完成统一测试：协作回归 154 通过；全量测试 776 通过、0 失败、2 跳过。

2026-09-22 本轮新增门禁统一测试完成：针对性回归 44/44，通过；类型检查通过；协作回归 154/154；全量测试 777 通过、0 失败、2 跳过。首次从工程根直接调用针对性测试因测试相对入口要求应用目录而失败，切换到 `apps/ai-desktop` 后通过，未作为产品失败处理。

开发版已从提交 `7b6389f8fdd265378d02ea9d2e97bace0e51c891` 重新构建并启动。重启后持久状态不再把第 3 轮投影为监控，而是显示系统重试令狐交接；原专题仍处于真实验收失败卡点，等待恢复入口触发下一轮。

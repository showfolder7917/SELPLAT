# AI 工厂旧表清理测试文档

## 修改范围

- 物理删除 22 张小写 `ai_*` 旧表与永久废止的 `AiStageExecution`。
- 退役旧表建表、种子、号段、Java API 与审计过滤器调用链。
- 新版 `AiWorkflowRun.taskId`、`AiWorkflowNodeRun.agentRegistrationId` 保留外部标识，但不再依赖本地旧表外键。
- 保留 `AiRole` 数据；按当前 V2 SQL 重建 `AiProject`、`AiGate`、`AiRule` 和流程表。

## 已授权的数据处理

- 用户明确确认“旧数据不要，直接删除”。
- 清理前备份：`OPTION/temp/ai-factory/backup/aifactory-before-legacy-table-drop-20260821.zip`。

## 待测项

| 状态 | 验证内容 | 命令或入口 | 预期结果 |
|---|---|---|---|
| 待执行 | Java 编译 | `./gradlew :apps:ai-factiory:backend:compileJava` | 编译成功，无旧包或旧表引用 |
| 待执行 | Checkstyle | `./gradlew :apps:ai-factiory:backend:checkstyleMain` | 违规数为 0 |
| 待执行 | V2 定向测试 | `./gradlew :apps:ai-factiory:backend:test --tests com.sp.selplat.aifactory.AiFactoryWorkflowV2Test` | 全部用例通过 |
| 待执行 | 新库初始化 | 使用隔离 H2 内存库启动 V2 测试 | 仅创建当前 `Ai*` 业务表和 `CommonSequenceSegment` |
| 通过 | 正式库结构核对 | 查询 `INFORMATION_SCHEMA.TABLES` | 小写 `ai_*` 数量为 0；业务表为 10 张 V2 `Ai*` 表；旧号段数量为 0 |
| 通过 | 旧引用扫描 | 检索旧表名、旧号段与退役包名 | AI 工厂源码与 SQL 无命中 |
| 通过 | Host 启动冒烟 | `./gradlew :apps:host:backend:run` | 8080 启动成功；页面、管理接口和流程快照均返回 200 |

## 执行结果

- 尚未收到“统一测试”，完整测试项保持待执行。
- 依照运行时规则，首次启动失败后完成针对性修正并重试；仅启动冒烟项已回写，其他完整测试保持待执行。
- 范围外已知依赖：`apps/ai-memory/src/main/python/com/sp/selplat/aimemory/client/AI工厂客户端.py` 仍调用已退役的 AI 工厂旧接口；该跨应用改造未纳入本轮授权范围。
- 首次启动失败：既有 `AiRole.id=100015` 已属于 `CODE_ENGINEER`，新版 `SOFTWARE_ENGINEER` 种子复用该主键触发唯一约束；已将新版角色主键及流程节点引用同步调整为 `100027`，等待重新启动验证。
- 修正后启动通过：Host 正常运行在 8080；`/aifactory/aifactory.html`、`/api/v1/ai-factory/management/dashboard`、`/api/v1/ai-factory/workflows/snapshot?projectId=130001` 均返回 200。

## 页面布局修复（2026-08-21）

### 修改范围

- 把无界整页外壳调整为居中的 `1520px` 有界管理台，保留右侧业务卡片 `1180px` 合理内容上限。
- 将失效的 `--sel-theme-border-subtle` 等令牌替换为当前 SEL 语义令牌，恢复标题、导航、工作区和表格层级。
- 删除应用自行生成的第二个“主题”按钮，只保留公共个性化入口，并让入口与标题栏操作区对齐。
- 使用语义字号、字重和行高令牌，保留窄屏单列回退。

### 已执行验证

| 状态 | 验证内容 | 条件或入口 | 实际结果 |
|---|---|---|---|
| 通过 | Host 启动冒烟 | `./gradlew :apps:host:backend:run` | 进程保持运行，8080 启动成功 |
| 通过 | 页面 HTTP | `/aifactory/aifactory.html` | HTTP 200 |
| 通过 | 公共前端边界 | Host 启动链中的 `verifySelUiSourceBoundary` | 任务通过 |
| 通过 | 宽屏视觉检查 | 内置 Browser，目标视口 `2400×900` | 管理台居中、卡片有界、无横向溢出 |
| 通过 | 主题入口 | 打开并关闭公共个性化面板 | 仅一个可见入口，打开和关闭均成功 |
| 通过 | 标题栏显隐 | 隐藏后再恢复标题栏 | 状态正确，主题入口持续可见 |
| 通过 | 浏览器控制台 | 页面加载与交互后读取错误日志 | 0 条错误 |
| 通过 | 变更格式 | `git diff --check`（AI 工厂静态资源范围） | 无格式错误 |

- 可复核数据：`OPTION/temp/ai-factory/页面布局修复-result.01a0244d-a1f5-7d92-9dff-ce5e6af920f4.json`。
- 已完成真实宽屏截图检查；完整工程测试仍等待用户明确提出“统一测试”。

## 参照 Japanese 的公共布局重构（2026-08-21）

### 修改范围

- 参照 `apps/japanese`，把 AI 工厂页面骨架统一为公共 `selPanel + selTree + selGrid` 五区工作台。
- 左侧树恢复“项目管理 → 当前项目 → 规则、AI 门禁、流程、执行进度”，并保留根级角色管理。
- 项目列表改用公共 Grid；新增、编辑改用公共 Window，删除改用公共确认框。
- 新版公共流程画布嵌入 Panel 中心区，流程页与 Grid 使用同一内容区域切换。
- 保留普通极简浅色默认主题，以及始终与主题入口相邻的标题栏显隐按钮。

### 已执行验证

| 状态 | 验证内容 | 条件或入口 | 实际结果 |
|---|---|---|---|
| 通过 | JavaScript 语法 | `node --check aifactory-japanese-layout.js` | 无语法错误 |
| 通过 | 公共前端边界 | Host 启动链中的 `verifySelUiSourceBoundary` | 首次提示文件头用途不明确，补齐“SEL UI 页面”中文说明后通过 |
| 通过 | Host 启动冒烟 | `./gradlew :apps:host:backend:run` | 8080 启动成功并保持运行 |
| 通过 | Japanese 同尺寸视觉检查 | 内置 Browser，目标视口 `1808×1006` | 公共五区结构完整，页面级无横向溢出，页面高度等于视口高度 |
| 通过 | 项目列表 | `/aifactory/aifactory.html` | 公共 Grid 显示项目数据，状态文本为“启用”，编辑与删除入口存在 |
| 通过 | 流程设计切换 | 点击顶部“流程设计” | Grid 隐藏、公共流程画布显示；3 个角色、3 个节点正常呈现 |
| 通过 | 标题栏显隐 | 点击“隐藏”后再恢复 | 标题区隐藏，按钮保持可见并变为“显示”，恢复成功 |
| 通过 | 项目编辑窗口 | 点击“新增项目”后取消 | 公共 Window 打开，编码、名称、说明字段完整，取消成功 |
| 通过 | 浏览器控制台 | 页面加载及上述交互后读取错误日志 | 0 条错误 |

- 本轮只完成运行时启动与真实页面定向验证；尚未收到“统一测试”，原有完整 Java、Checkstyle 与 V2 定向测试继续保持待执行。

## 角色管理、分页与侧栏边界修复（2026-08-21）

### 修改范围

- 将角色管理数据源与流程画布角色源分离：角色管理展示全部普通启用角色，流程画布仍限定三类快速开发角色。
- 恢复角色 Grid 的编辑、删除操作；项目编辑与角色编辑分别使用独立公共 Window，删除继续使用公共确认框。
- 补齐分页统计、每页条数、页码、上一页、下一页和反馈区域；本地分页默认每页 10 条。
- 公共 Panel 底栏由固定 `104px` 调整为紧凑 `64px`；侧栏 Grid 列改为 `minmax(0, 1fr)`，标题按钮不再越过分隔线。
- 角色类型中文名称继续读取引用数据选项组 `optionSet103006`。

### 已执行验证

| 状态 | 验证内容 | 条件或入口 | 实际结果 |
|---|---|---|---|
| 通过 | JavaScript 语法 | `node --check aifactory-japanese-layout.js` | 无语法错误 |
| 通过 | 公共前端边界 | Host 启动链中的 `verifySelUiSourceBoundary` | 任务通过 |
| 通过 | Host 启动冒烟 | `./gradlew :apps:host:backend:run` | 清理占用 H2 的旧 Host 进程后，8080 启动成功并保持运行 |
| 通过 | 角色完整性 | 浏览器进入“角色管理” | 展示 18 个普通启用角色；固定根和分类节点未进入编辑表格 |
| 通过 | 本地分页 | 默认每页 10 条并点击下一页 | 第 1 页 10 条，第 2 页 8 条；页码、上一页和下一页状态正确 |
| 通过 | 角色操作 | 打开角色编辑和删除确认 | 编辑窗口显示角色编码、名称、类型、经验和专业范围；删除确认显示目标角色，验证后取消，未删除数据 |
| 通过 | 紧凑底栏 | 读取 AI 工厂底栏边界 | 高度为 64px，统计、每页条数和页码完整可见 |
| 通过 | 侧栏按钮边界 | 比较按钮与左栏 `getBoundingClientRect` | 按钮右边界位于左栏右边界以内，不再被裁切 |
| 通过 | Japanese 公共回归 | `/japanese/japanese.html` | 侧栏按钮位于边界内，分页完整，无页面级横向溢出 |
| 通过 | 浏览器控制台 | AI 工厂与 Japanese 页面加载及交互 | 两页均为 0 条错误 |

- 首次启动因旧 PID `18399` 占用 `reference-data.mv.db` 而失败；定位后停止该残留 Host，再次启动成功。
- 尚未收到“统一测试”，原有完整 Java、Checkstyle 与 V2 定向测试继续保持待执行。

## 七角色体系收敛（2026-08-21）

### 修改范围

- 角色管理固定为需求分析师、软件工程师、测试工程师、需求审核员、软件审核员、测试审核员和项目经理。
- 保留 `ROLE_ROOT`、`ENGINEER_ROOT`、`REVIEWER_ROOT` 三个不可编辑结构节点，但不计入七个业务角色。
- 新增四个独立稳定角色编码和种子主键，不复用历史角色主键；集合外普通角色统一逻辑停用。
- 角色管理页面按稳定编码过滤七角色；流程画布仍只允许三个工程角色。
- 当前用户层 AI 工厂规则升级至 `2.8.0`，登记七角色集合及历史角色收敛策略。

### 已执行验证

| 状态 | 验证内容 | 条件或入口 | 实际结果 |
|---|---|---|---|
| 通过 | Host 启动冒烟 | `./gradlew :apps:host:backend:run` | 公共前端门禁通过，8080 启动成功并保持运行 |
| 通过 | 管理接口角色集合 | `/api/v1/ai-factory/management/dashboard` | 返回 3 个结构节点和 7 个启用业务角色；历史普通角色不再返回 |
| 通过 | 角色管理页面 | 浏览器进入“角色管理” | 正确显示 7 行，顺序与用户指定一致，编辑和删除按钮各 7 个 |
| 通过 | 流程角色边界 | 浏览器进入“流程设计” | 角色面板只显示需求分析师、软件工程师、测试工程师 |
| 通过 | 页面布局 | 角色页和流程页 | 无页面级横向溢出，分页显示单页，控制台 0 条错误 |
| 通过 | JavaScript 语法 | `node --check aifactory-japanese-layout.js` | 无语法错误 |
| 通过 | 用户规则索引静态核对 | 从应用规则索引检索逻辑 ID 和七角色 DSL | 逻辑 ID 唯一命中，规则正文包含七角色集合及历史角色逻辑停用约束 |
| 待执行 | 用户规则加载回归 | 统一测试阶段执行规则加载测试 | 从根索引按稳定用户 ID 命中新版 `2.8.0` 规则，无重复逻辑 ID 或跨用户引用 |

- 尚未收到“统一测试”，完整 Java、Checkstyle、V2 定向测试及规则加载回归继续保持待执行。

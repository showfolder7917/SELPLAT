# Java 业务注释与返回示例规则资产目录

## 主入口

- `../RUL_Java业务注释与返回示例规则.md`：SELPLAT Java 业务注释、参数示例和实际返回示例的唯一权威规则。

## 配套资产

- `template/Java业务注释模板.md`：类、字段、方法、参数、返回值、异常和逐行业务注释模板。
- `examples/BaseDaoSupportImpl注释示例.md`：从生产类提取的实际输入、转换和返回结构示例。

模板和样例只负责提供可复用写法，不得覆盖主规则正文。

# CPMAB082 离线依赖配置规则包

## 主入口

- `RUL_CPMAB082离线依赖配置规则.md`：CPMAB082 的只读参考工程、兼容版本和持久层替代验证差异。

## 公共规则

本规则包只维护 CPMAB082 项目差异。离线闭包重建、无网络边界、临时 Gradle init script 和正常测试恢复统一加载 Fujitsu 组织级 `RUL_FujitsuGradle离线依赖闭包恢复规则`。

新增 CPMAB082 专属说明、模板或样例时，应继续放入本同名规则包的标准子目录。

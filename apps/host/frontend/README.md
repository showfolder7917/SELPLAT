# host frontend

本目录预留平台外壳前端，负责统一导航、模块挂载和平台级状态展示。

不负责：

- 复制业务模块页面。
- 实现业务模块私有控件。
- 绕过模块 API 直接访问数据库。

后续模块页面应按 manifest 路由挂载到平台壳，并保持可以独立下线。

这里放宿主前端壳。

建议内容：

- `shell/`
- `router/`
- `layout/`
- `plugin-loader/`
- `app-entry/`

当前状态：空骨架，占位说明。

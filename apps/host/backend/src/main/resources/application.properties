server.port=8080
spring.application.name=selplat-platform-runtime
# DevTools 只在完整构建结束后看到 reload.trigger 更新时重启，避免读取仍在写入的模块 JAR。
spring.devtools.restart.trigger-file=reload.trigger
# 各业务模块自行装配数据库，Host 不再持有或导入任何业务主数据源。

# 晶透管理主题

- 稳定 ID：`glass-admin`
- 主题定义：当前目录的 `manifest.js`、`theme.css` 和 `modes/`
- 独占素材：无；边框与材质由 CSS 实现，不建立空素材目录
- 页面背景：深浅模式分别使用通用纯色背景 ID，不自动引用任何图片背景

以后增加仅属于晶透管理的位图时，再建立 `../../../assets/themes/glass-admin/`，不得放入公共背景或其他主题目录。

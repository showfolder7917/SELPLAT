# AI Desktop 工作区意外横向滚动统一测试文档

## 待测范围

- 确认开发版工作区目录不再显示可拖动的横向滚动条。
- 确认目录数量超过工作区可用高度时，右侧纵向滚动条仍可正常滚动。
- 确认工作区权限、主目录和移除按钮的 Tip 不会扩大列表的横向滚动范围。
- 确认窄窗口下工作区名称、真实路径和目录项仍在侧栏内截断，不会撑宽资源管理器。

## 统一测试命令

1. `cd apps/ai-desktop && npm run typecheck`
   - 预期：TypeScript 类型检查通过。
2. `cd apps/ai-desktop && npm run build:renderer:developer`
   - 预期：开发版渲染构建成功。
3. 启动开发版真实页面，在与问题截图一致的窄窗口下展开包含较多目录项的工作区，并悬停权限、主目录和移除按钮。
   - 预期：工作区列表只出现纵向滚动条，底部没有横向滚动条，也不能把侧栏内容横向拖离原位。
4. `python3 apps/rule-engine/backend/src/main/python/com/sp/selplat/local/code/core/abilities/layered_rule_loader.py '{"action":"load_bundle","logical_ids":["CODE_JS_RULES","CODE_TEST_RULES","AI_DESKTOP_OFFICIAL_HARNESS_RULES"],"active_scope":"selplat","active_user":"XUNAN"}'`
   - 预期：三个逻辑 ID 从根索引唯一命中，加载回执的层、路径和 `override_mode` 正确。

## 当前状态

- 状态：统一测试通过（2026-08-22）。
- 代码验证：已通过。
- 构建验证：开发版正式构建通过，Vite 转换 227 个模块。
- 真实交互：900×700 窄窗口和 1280×900 常规窗口均无工作区横向溢出；纵向内容、截断、按钮和 Tip 约束未造成侧栏撑宽。
- 视觉证据：`temp/interaction/unified/narrow.png` 与 `regular.png`；无 P0、P1、P2 问题。
- 运行状态：正式开发版已受控重启一次并连接官方 Codex app-server。

## 代码验证结果

1. `npm run typecheck`
   - 结果：通过，退出码为 `0`，没有 TypeScript 类型错误。
2. `npm run test:explorer`
   - 结果：通过，资源管理器专项测试 `1/1` 通过，无失败、跳过或取消。
3. 工作区滚动轴 CSS 契约断言
   - 结果：通过；已确认专项样式同时包含 `max-width: 100%`、`overflow-x: hidden` 和 `overflow-y: auto`。
4. 构建、启动、重启及真实页面操作
   - 结果：本阶段禁止执行，未执行。

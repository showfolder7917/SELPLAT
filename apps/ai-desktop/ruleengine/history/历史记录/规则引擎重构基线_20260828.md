# 规则引擎重构基线（2026-08-28）

> 本文件只是可丢失的历史快照，不是规则、索引、程序、测试或构建输入。删除整个 `history` 后，当前规则事实和生产行为必须保持不变。

## 重构前基线

- `ruleengine`：243 个文件，约 4.57 MB。
- `rules`：181 个文件，约 3.84 MB。
- 活跃规则正文：94 个 Markdown 文件，约 3227 条 DSL 声明。
- SELPLAT 通用索引：22 个逻辑规则、124 个触发映射、447 行。
- 跨工程通用索引：24 个逻辑规则、83 个触发映射、331 行。
- 活跃规则正文中的 `upgrade_record*`：约 237 条。
- 旧 `local/XUNAN/archive`：25 个文件，约 189 KB。
- `template` 与旧归档资源合计：54 个文件，约 2.24 MB。

## 已建立的解耦边界

- 旧 `rules/local/XUNAN/archive` 已移入 `history/历史记录/local-XUNAN-archive`。
- `rules/active-user` 中 4 条有效规则已归入 `rules/local/XUNAN` 的真实职责目录，空目录已删除。
- `python/ruleengine/__pycache__` 已删除，生成缓存不得成为源码或规则输入。
- 生产规则清单将 `history` 明确列为排除类别，测试门禁同步禁止打包历史目录。
- 被程序、规则或可重复执行流程引用的模板仍属于正式资源；只有不被生产链依赖的辅助素材才能进入 `history/素材`。

## 索引分片结果

- SELPLAT 通用索引拆为 5 个职责叶子索引，原 22 个逻辑规则和 124 个触发映射保持不变。
- 跨工程通用索引拆为 5 个职责叶子索引，原 24 个逻辑规则和 83 个触发映射保持不变。
- 两个原父索引只保留根路径事实与子索引导航。

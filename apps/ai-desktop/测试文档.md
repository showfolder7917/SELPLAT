# AI Desktop 共享测试文档

## 托管多轮回复逐卡追加修正

### 本轮目标

- 每个真实 Harness `turnId` 向下新增一张独立 `CODEX` 回复卡。
- 下一轮开始前冻结上一轮，后续事件不得覆盖历史文字。
- 完成消息只校准自己的 `segmentId`，最终 IPC 响应只收口最新回复卡。

### 已执行代码级验证

- [x] `npm run typecheck` — 预期：消息路由、React 状态和共享类型无 TypeScript 错误。结果：通过，退出码 0。
- [x] `node --test tests/managed-task-executor-contract.test.mjs` — 预期：逐 `turnId` 路由、旧卡冻结和双回合测试契约通过。结果：12/12 通过，退出码 0。
- [x] `npm run test:interaction` — 预期：后台隔离 Electron 真实发出两个不同 `turnId`，界面出现两张上下排列的回复卡且第一轮文字保持不变。结果：11/11 通过，退出码 0。
- [x] 直接调用 `layered_rule_loader.load_for_current_user("AI_DESKTOP_OFFICIAL_HARNESS_RULES", "selplat")` — 预期：从根索引按稳定用户 XUNAN 命中 AI Desktop 5.31.0 规则。结果：通过；路径与版本均正确，当前用户树为 22 个索引、76 条规则。
- [ ] `python3 -m unittest ...test_layered_rule_loader.py ...test_ai_rule_package_integrator.py` — 预期：规则加载回归全部通过。结果：22 项中 21 项通过、1 项失败；失败原因为冻结 core 测试仍写死旧计数 21/72，而当前生产树实际为 22/76，不是本轮规则路径或内容错误。本轮未越权修改 core 测试。

## 完成门槛

- 应用类型检查、契约测试、真实隔离交互和本轮用户规则直接加载均通过；冻结 core 的旧计数断言需另行授权修正。本轮不执行正式构建、启动或重启。

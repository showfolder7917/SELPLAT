# AI Desktop 当前会话恢复与 Markdown 回答测试文档

## 本轮范围

- 当前活动 Codex 任务在渲染刷新或 Electron 主进程重建后恢复。
- 点击“新建任务”调用官方删除并清空当前本地对话；下一次发送才建立新任务。
- 助手回答支持安全 GitHub 风格 Markdown，并采用自然、结论先行的协作表达。
- 本轮停在任务托管代码级验证，不执行正式构建，不启动或重启用户正在使用的应用。

## 待测项

| 状态 | 验证 | 命令 | 预期结果 |
| --- | --- | --- | --- |
| 已通过 | TypeScript 静态检查 | `npm run typecheck` | 零 TypeScript 错误 |
| 已通过 | 会话与渲染契约测试 | `node --test tests/managed-task-executor-contract.test.mjs tests/user-input-and-screenshot-contract.test.mjs` | 12 项全部通过，确认 `thread/resume`、`thread/delete`、持久线程、安全 Markdown 与自然风格契约存在 |
| 已通过 | 隐藏隔离 Electron 交互测试 | `npm run test:interaction` | 6 项全部通过；Markdown 标题、列表、表格、行内代码可见；刷新后对话恢复；新建任务后旧对话消失；成功结果位于 `temp/interaction/result.json` |

## 执行结果

- `npm run typecheck`：通过，退出码 0。
- 契约测试：12/12 通过，退出码 0。
- 隐藏隔离 Electron 首次受 macOS 沙箱阻止，在应用窗口启动前中止；获准在沙箱外仅启动隔离测试实例后重跑，6/6 通过，退出码 0，无失败截图。
- `git diff --check`：通过，无空白错误。
- 按任务托管边界未执行正式构建、构建后测试，也未启动或重启用户当前应用；这些项目仍待用户进入测试托管后统一执行。

## 测试托管待测项

| 状态 | 验证 | 命令 | 预期结果 |
| --- | --- | --- | --- |
| 待执行 | 全量正式构建 | `npm run build` | TypeScript、Electron、办公版和开发版渲染构建全部成功 |
| 待执行 | 全部 Node 构建后回归 | `npm run test:sites`、`test:screenshot`、`test:stream`、`test:workspace`、`test:audit`、`test:explorer`、`test:managed`、`test:trust` | 所有测试零失败 |
| 待执行 | 构建后隐藏隔离 Electron 回归 | `npm run test:interaction` | 6 项交互全部通过，隔离实例和临时服务结束后关闭 |
| 待执行 | AI Desktop 专项规则加载 | `layered_rule_loader.py load_bundle` | JavaScript、测试和官方 Harness 规则唯一命中，Harness 规则版本为 `5.8.0` |
| 待执行 | 当前用户索引校验 | `layered_rule_loader.py validate_user_index` | 无失效路径、重复逻辑 ID 或跨用户引用 |

## 测试托管结果

尚未执行。若全部测试不依赖当前运行实例，本轮不重启用户正在使用的 AI Desktop。

# AI Desktop 统一测试文档

## 当前任务

- 任务：隔离 AI Desktop 与 Codex App 的官方会话数据域，避免生成 `Registered workspace roots:` 会话记录。
- 登记日期：2026-08-23
- 当前状态：代码验证待执行；本轮禁止正式构建、启动或重启当前 AI Desktop。

## 待执行自动测试

- [!]  `npm run typecheck` — 预期：CodexService 新配置、会话存储 v2、主会话与协同工厂接口均无 TypeScript 错误。 — 结果：失败（退出码 127）；执行者：showfolder@showfolderdeMac-mini.local；开始：2026-08-23T00:25:40.596Z；结束：2026-08-23T00:25:40.714Z
- [!]  `npm run test:interaction` — 预期：后台隔离 Electron 通过 Playwright 语义定位器完成真实程序化交互，且不启动或重启当前 AI Desktop。 — 结果：失败（退出码 1）；执行者：showfolder@showfolderdeMac-mini.local；开始：2026-08-23T00:25:40.714Z；结束：2026-08-23T00:25:40.893Z

## 构建后受控真实验证

- 通过测试托管启动开发版后，专属 `codex-home` 首次无认证时应只要求 AI Desktop 自己完成一次 ChatGPT 登录；不得复制默认 Codex 数据域的认证文件。
- 若存在 version 1 的旧活动线程凭据，应只对该线程执行旧默认数据域的 `thread/delete`；删除失败时保留凭据并阻止新域覆盖。
- 在 AI Desktop 新建并发送主会话、协同执行会话和协同审核会话时，Codex App 左侧列表不得新增对应记录。
- AI Desktop 的 Electron 或渲染器重建后，应能从专属数据域 `thread/resume` 当前持久线程；点击“重新建立一个 Codex 会话”后应先删除旧线程，下一次发送再创建新线程。
- 即使隔离配置未来退化，首条用户消息的预览也应以真实任务开头，不应再以 `Registered workspace roots:` 开头。

# AI Desktop 共享测试文档

## 刷新对话图标与会话重建 Tip：第二轮复测

### 上轮结果与修复

- `npm run typecheck`：失败，当前 worktree 尚未安装依赖，缺少本地 `tsc`。
- `npm run test:explorer`：4/4 通过。
- `npm run test:interaction`：失败，交互运行器将 worktree 路径中的空格保留为 `%20`，创建了错误的证据目录路径。
- 本轮使用 `fileURLToPath(import.meta.url)` 解析交互运行器路径，并在复测前补齐项目锁定依赖。

### 待统一测试

- [x] `npm run typecheck` — 预期：依赖补齐后 DeveloperApp、交互测试及运行器改动通过 TypeScript 静态检查。 — 结果：通过；执行者：showfolder@showfolderdeMac-mini.local；开始：2026-08-22T18:11:17.474Z；结束：2026-08-22T18:11:19.014Z
- [x] `npm run test:explorer` — 预期：刷新图标、指定 Tip、无原生 title、规则版本 5.38.0 与标签入口静态契约继续 4/4 通过。 — 结果：通过；执行者：showfolder@showfolderdeMac-mini.local；开始：2026-08-22T18:11:19.014Z；结束：2026-08-22T18:11:19.269Z
- [!]  `npm run test:interaction` — 预期：交互运行器正确解码含空格的 worktree 路径；后台隔离 Electron 中刷新图标可见，悬停及键盘聚焦 Tip 正确且不被裁切，点击后的会话清理行为保持正常。 — 结果：失败（退出码 1）；执行者：showfolder@showfolderdeMac-mini.local；开始：2026-08-22T18:11:19.270Z；结束：2026-08-22T18:11:20.849Z

### 完成门槛

- 仍只通过无参数 `npm run test:document` 获取独占锁并执行以上项目。
- 禁止正式构建、启动或重启当前 AI Desktop。

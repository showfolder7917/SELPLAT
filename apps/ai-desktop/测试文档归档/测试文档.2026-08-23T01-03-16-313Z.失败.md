# AI Desktop 统一测试文档

## 当前待测：协同执行列表与任务流转归档

- 登记日期：2026-08-23
- 当前阶段：任务托管源码修改
- 源码范围：协同契约、持久化、协调器、Harness 结果协议、业务日志关联、开发版执行列表与详情页
- 本阶段限制：禁止正式构建、启动或重启；完整测试等待用户触发“统一测试”或进入测试托管。

## 本轮统一执行项

- [x] `npm run typecheck` — 预期：渲染进程与共享协同契约通过 TypeScript 静态检查，不生成构建产物 — 结果：通过；执行者：showfolder@showfolderdeMac-mini.local；开始：2026-08-23T01:03:14.131Z；结束：2026-08-23T01:03:15.443Z
- [!]  `npm run test:interaction` — 预期：后台隔离 Electron 中协同执行列表、归档详情、折叠内容和单 Agent 隐藏入口的真实交互全部通过 — 结果：失败（退出码 1）；执行者：showfolder@showfolderdeMac-mini.local；开始：2026-08-23T01:03:15.443Z；结束：2026-08-23T01:03:16.312Z

| 状态 | 验证内容 | 命令 | 预期结果 |
| --- | --- | --- | --- |
| 已通过（任务阶段） | React/共享契约 TypeScript 静态检查 | `./node_modules/.bin/tsc -p tsconfig.json --noEmit` | 无 TypeScript 错误，执行列表和详情组件契约完整 |
| 已通过（任务阶段） | Electron 主进程 TypeScript 只检查不产出 | `./node_modules/.bin/tsc -p tsconfig.electron.json --noEmit` | 协同状态迁移、协调器和日志广播无类型错误，且不生成构建产物 |
| 待统一测试 | 协同持久化、旧状态迁移、结果摘要和调度相邻回归 | `npm run test:collaboration` | 全部协同单测通过；发起人、执行历史、时间语义和结果摘要断言通过 |
| 待统一测试 | 后台隔离 Electron 真实交互与布局 | `npm run test:interaction` | 协同模式显示独立执行列表；单会话不显示；完成记录摘要、多人执行、折叠详情和错误日志可访问；成功不留截图，失败证据进入 `temp` |
| 待统一测试 | 开发版正式构建 | `npm run build:developer` | 类型检查、Electron 与开发版渲染构建全部通过 |
| 待统一测试 | 当前用户规则索引与规则加载回归 | `./gradlew test --tests '*Rule*' -PselplatPython=python3` | 根索引可命中升级后的 `AI_DESKTOP_OFFICIAL_HARNESS_RULES`，无重复逻辑 ID、失效路径或跨用户引用 |

## 人工可视核对

1. 协同模式任务侧栏中，“执行列表”固定显示在人物列表之前；切到单会话后入口消失。
2. 已完成任务不再出现在人物当前任务区域；执行列表摘要直接显示标题、发起人、所有执行人、开始、完成和总耗时。
3. 打开归档后，首屏先显示最终结果、原问题、已解决问题、具体改变和成功/遗留状态。
4. 分析、审核、执行转交、流程日志和错误日志可以分别展开、折叠，且内容来自真实任务参与者快照。
5. 窄窗口下列表、摘要和日志不产生横向溢出。

## 任务阶段静态检查记录

- 结果：两项 `--noEmit` TypeScript 检查均通过。
- 附加检查：`node --check tests/collaboration-mode.test.mjs`、`node --check tests/interaction/isolated-preload.cjs` 与 `git diff --check` 均通过。
- 依赖处理：当前 worktree 没有独立 `node_modules`，检查期间临时复用锁文件一致的主工作区依赖；结束后已移除链接，未安装依赖、未生成构建产物。
- 完整单测、真实 Electron 交互、截图和正式构建仍保持“待统一测试”。

### 2026-08-23 静态检查失败修复

- 原失败一：worktree 内没有 `node_modules`，相对路径命令无法找到 `./node_modules/.bin/tsc`。
- 原失败二：仅从主工作区绝对路径启动 `tsc` 时，TypeScript 仍以 worktree 的配置目录解析类型依赖，因此无法找到 `vite/client`。
- 修复方式：确认 worktree 与主工作区 `package-lock.json` 完全一致后，在检查期间临时链接主工作区 `node_modules`，使配置文件与依赖解析根保持一致；检查结束后已移除该链接。
- 复验结果：用户给出的两条完整命令均原样执行成功（退出码 `0`），包括 `git diff --check`、渲染进程 TypeScript 检查和 Electron 主进程 TypeScript 检查。
- 边界确认：未执行构建、启动或重启，未安装或修改依赖。

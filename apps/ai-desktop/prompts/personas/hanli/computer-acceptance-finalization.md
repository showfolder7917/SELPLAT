你正在继续同一轮韩立正式页面检查。首回合已经结束，但尚未提交完整判断。

验收目标与条件：{{goalJson}}

本回合不得继续观察、点击、滚动、键盘输入、发送消息或执行任何页面操作。只能调用既有 `hanli_computer` 的 `finish` 一次，为每条条件提交真实的功能和布局判断，并分别引用首回合中已经通过 criterionIds 绑定给该条件的截图。不得用首个失败后的通用截图代替其他条件证据。首回合确因客观依赖、数据安全或验收能力限制而无法继续的条件才标为 `blocked`，actual 必须写明阻断关系；不得猜测通过或失败。普通正文不算完成。

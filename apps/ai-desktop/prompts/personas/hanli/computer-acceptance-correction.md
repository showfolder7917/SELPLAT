你正在继续同一轮韩立正式页面检查。上一轮提交 finish 被校验拒绝，原因如下：

{{finishRejection}}

验收目标与条件：{{goalJson}}

你仅有一次受限纠正回合。可以继续使用既有 `hanli_computer` 的 observe 和已授权的安全只读导航，补齐被拒绝条件自身的真实截图与功能、布局判断；不得发送消息、修改设置、恢复任务、写入业务数据、调用外部工具或扩大权限。不得把其他条件的截图作为该条件证据。

补齐后必须在本回合调用 finish，逐条提交全部条件的功能和布局判断及各自绑定截图。若缺少证据或无法安全观察，应如实标为 blocked 并说明原因。第二次 finish 被拒绝不会再开启新的纠正回合。

[令狐故障只读调查]

先调查本次真实失败事实，再决定修复。禁止复述或执行原专题方案，禁止修改文件。

说明失败阶段、直接原因、证据位置、最小修复边界、禁止触碰范围和必须重跑的验证命令。若证据不足，明确指出缺少什么，不得猜测。

先核对相关模块职责、完整调用链和权威状态来源，再判断失败是否来自结构缺陷。重复失败必须比较前轮证据与当前实际候选，说明共同根因和前次为何没有阻止复发；需要重构时明确重构边界、要删除的重复或退役入口，以及防止复发的回归场景。不得只给临时绕过办法，也不得省略权限与完整质量门禁。

若失败来自交互或集成测试，必须核对测试替身实际执行的动作与生产实现是否相同。标记、回执或模拟调用不能当作真实重启、清理、持久化或页面重挂载；先按原验收条件确定观察时机，再判断是产品缺陷还是测试断言不符。前轮结论只能作为待核假设，原始日志、截图和代码证据必须保留。

判断修复是否进入实际运行版本时，必须读取运行应用携带的候选 SHA、发布清单与提交包含关系；时间证据必须先换算为带时区的绝对时间。禁止比较不同时区显示的钟点，也禁止仅凭提交时间、构建时间或源码当前内容推断运行版本。

页面可见性缺陷必须以同一个稳定业务标识逐层核对“持久化事实 → 主进程/窗口 API DTO → 真实页面 DOM”，逐层记录实际值和第一个丢失或变形的边界。只看到数据库记录、源码组件或测试替身均不能宣布页面能力已存在。新候选发布重启后若真实页面仍出现同一症状，前轮根因和修复结论自动视为待证伪假设，必须重新读取运行证据，不得重复发布或重复原补丁。

若韩立对同一提案已有两次不同运行的真实验收失败，必须并列检查原条件、两轮操作和截图或源码证据、验收计划与失败分类。证据证明韩立验收能力误判时，给出原授权范围内的验收能力修复方案；证据证明产品不符合条件时，给出产品修复方案。不得为了通过而改写条件、降低验收门禁或删除失败证据；令狐不能替韩立宣布验收通过。

若一轮统一测试包含多个未通过项，必须先逐项归类，再给出共同根因、受影响调用方与一次性修复清单；不得修完第一项就结束调查。无法证明同根因的失败也要在同一轮修复计划中分别列出，全部处理后才允许交回统一测试。

失败事实：
{{failure}}

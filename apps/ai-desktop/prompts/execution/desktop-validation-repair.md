AI Desktop 已在本任务签发的独立 worktree 中执行固定静态检查与隔离 Playwright，失败事实如下：
{{failure}}

首次实施已经冻结以下文件范围：
{{allowedFiles}}

只允许修改上述已经冻结的文件；不得新增、修改、移动或删除列表外文件。若失败原因位于列表外，必须报告“需要重新分析任务范围”，不能修补构建脚本、测试执行器或其他基础设施。不要自行运行 npm、Playwright、Electron、构建、启动或重启命令。修复完成后直接说明改了什么，AI Desktop 将在同一 worktree 自动复测。

修改前必须先读取失败位置和相关证据，说明预期、实际及支持根因的代码事实。报告缺失、信息被省略或只有汇总时，先按给出的路径补读，不能猜测异步、缓存或测试数据问题。

同一失败再次出现时，先比较上一轮改动与本轮失败位置、期望值和实际值，明确为什么上次无效，再形成新的修复依据。没有新证据不得重复原修改。保留原验收目标，禁止删除测试、跳过断言或缩小覆盖来制造通过；只有证据证明断言与已确认需求不一致时才修正断言，并保留等价覆盖。

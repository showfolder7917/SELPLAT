# AI Desktop 协同模式测试文档

## 本轮目标

- 单会话模式保持原有线程、托管阶段和消息队列行为。
- 协同模式稳定列出韩立及 11 名默认工作人物，并支持非受保护人物增删改查。
- 韩立确认的任务持久排队；空闲执行人取得新 Codex 和独立 Git worktree，异人审核最多三次明确驳回后进入任务执行。
- 执行人与审核员完成临界点先持久化结果，再销毁临时线程和进程；应用重开后显示恢复入口。
- 独立完成任务进入冻结集成代，不等待无依赖的未完成任务；详细耗时和等待归因只进入日志。

## 已执行静态检查

- [x] `npm run typecheck` — 预期：开发版协同人物页面、IPC 合约和状态渲染没有 TypeScript 错误。结果：通过，退出码 0。
- [x] `./node_modules/.bin/tsc -p tsconfig.electron.json --noEmit` — 预期：主进程协同存储、调度、多 Codex 路由、心跳、版本工作区和日志模块没有 TypeScript 错误。结果：通过，退出码 0。
- [x] `node --check tests/collaboration-mode.test.mjs` — 预期：新增协同专项测试文件语法有效。结果：通过，退出码 0。
- [x] `node --check tests/interaction/isolated-preload.cjs` — 预期：补齐协同 API 后的隔离预加载夹具语法有效。结果：通过，退出码 0。
- [x] `git diff --check` — 预期：本轮补丁不存在空白符错误。结果：通过，退出码 0；macOS 开发工具缓存因当前只读临时目录输出警告，但不影响检查结果。

## 待统一测试

- [ ] `npm run test:collaboration` — 预期：默认人物、韩立保护、成员 CRUD、中断恢复、等待归因报告及协同编排源码契约全部通过。
- [ ] `npm run test:managed` — 预期：既有四阶段单会话托管、权限与截图契约没有回归。
- [ ] `npm run test:explorer` — 预期：任务/工作区单活动分区、新建任务入口与 5.37.0 用户规则契约通过。
- [ ] `npm run test:interaction` — 预期：后台隔离 Electron 中单会话历史追加、任务侧栏、模式切换、人物选择和页面边界交互通过。

## 完成门槛

- 用户明确触发统一测试后执行全部待测项；任何失败都回写本文件并修正后重测。
- 本轮不正式构建、不启动或重启当前 AI Desktop；统一测试通过前只可标记为“源码完成、待统一测试”。

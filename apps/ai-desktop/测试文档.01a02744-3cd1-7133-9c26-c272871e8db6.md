# AI Desktop 侧栏折叠与任务入口统一测试文档

## 待测范围

- 确认开发版“资源管理器”标题单击后可折叠和恢复下方全部分区。
- 确认“工作区”标题单击后只折叠和恢复工作区列表，不改变各工作区自身的独立展开状态。
- 确认“新建任务”已移到“任务”标题右侧，不再单独占用一行。
- 确认新建任务仍会调用桌面桥接开启新会话，并清空当前消息。
- 确认折叠按钮具有 `aria-expanded`、可读名称和键盘聚焦能力。
- 确认已有工作区权限、主目录、删除及单根禁用逻辑没有回归。

## 统一测试命令

1. `cd apps/ai-desktop && npm run typecheck`
   - 预期：TypeScript 类型检查通过。
2. `cd apps/ai-desktop && npm run build:renderer:developer`
   - 预期：开发版渲染构建成功。
3. 启动开发版真实页面，在与用户截图一致的窄侧栏视口下执行折叠、恢复和新建任务操作。
   - 预期：标题箭头与内容状态同步，任务加号位于标题右侧，页面无溢出、遮挡或跳动。
4. 保存展开、折叠和恢复三个状态的截图及交互结果日志，并更新 `design-qa.md`。
   - 预期：视觉对照无 P0、P1 或 P2 问题，`final result` 为 `passed`。
5. `python3 apps/rule-engine/backend/src/main/python/com/sp/selplat/local/code/core/abilities/layered_rule_loader.py '{"action":"load_bundle","logical_ids":["CODE_JS_RULES","CODE_TEST_RULES","AI_DESKTOP_OFFICIAL_HARNESS_RULES"],"active_scope":"selplat","active_user":"XUNAN"}'`
   - 预期：三个逻辑 ID 从根索引唯一命中，加载回执的层、路径和 `override_mode` 正确。
6. `python3 apps/rule-engine/backend/src/main/python/com/sp/selplat/local/code/core/abilities/layered_rule_loader.py '{"action":"validate_user_index","active_user":"XUNAN"}'`
   - 预期：当前用户索引链无失效路径、重复逻辑 ID 或跨用户引用。

## 当前状态

- 状态：统一测试通过（2026-08-22）。
- 自动验证：`typecheck`、开发版正式构建、`test:explorer`、`test:managed` 和构建后 `test:interaction` 全部通过。
- 真实交互：隐藏隔离 Electron 使用无障碍名称完成资源管理器整栏、工作区和任务区的折叠恢复；任务入口保持在标题右侧，4 项交互测试全部通过。
- 视觉证据：`temp/interaction/unified/tasks-expanded.png`、`tasks-collapsed.png`、`narrow.png`、`regular.png`；无 P0、P1、P2 问题。
- 运行状态：正式开发版已受控重启一次，主进程与官方 Codex app-server 均正常运行。

# AI Desktop 截图完成与结构化疑问确认测试文档

## 当前状态

- 状态：代码级验证通过；正式构建与其余统一测试项仍待后续显式执行。
- 本轮已完成渲染层和 Electron 主进程静态检查、后台隔离 Electron Playwright 交互测试；未执行正式构建，未启动或重启当前 AI Desktop。

## 源码修复阶段记录

- 首轮规则加载误用未登记逻辑 ID `SELPLAT_COMMON_CONTROL_GOVERNANCE_RULES`；已根据当前用户索引更正为 `SELPLAT_PUBLIC_COMPONENT_GOVERNANCE_GATE_RULES`。
- 更正后的规则加载已完成，四个任务逻辑 ID 均唯一命中，并递归加载源码语言与归属门禁。
- `rg --files` 与后续 `rg -n` 命令均为成功的只读定位，不是源码失败，无需对业务实现做转移性修复。

## 代码级验证结果（2026-08-22）

1. `npm run typecheck`
   - 结果：通过，渲染层 TypeScript 无错误。
2. `npx tsc -p tsconfig.electron.json --noEmit`
   - 结果：通过，Electron 主进程 TypeScript 无错误。
3. `npm run test:interaction`
   - 第 1 轮：沙箱禁止临时服务器监听 `127.0.0.1:4197`；改用获批准的沙箱外隔离测试命令。
   - 第 2 轮：隔离 Electron 继承 `ELECTRON_RUN_AS_NODE=1`，导致 Playwright 调试参数被 Node 模式拒绝；已在隔离启动前移除该环境变量。
   - 第 3 轮：既有侧栏交互 `4/4` 通过；多疑问用例的“其他”定位命中两个独立问题组，已收紧到对应 `fieldset`。
   - 第 4 轮：最终 `5/5` 通过，包括多疑问独立选择、其他文本答案、全部完成后确认、原回合继续、完整意图重述和“就是这意思”按钮显示。
   - 证据：失败轮次的上下文、截图和 trace 保留在 `apps/ai-desktop/temp/interaction/artifacts/`；最终成功轮次不生成截图。
4. 隔离实例生命周期
   - 结果：Playwright 用例结束后隔离 Electron 与临时 Vite 测试服务均已关闭；未影响当前 AI Desktop。

## 待测项

1. `cd apps/ai-desktop && npm run typecheck`
   - 预期：React 渲染层新增截图状态、疑问选项和 Desktop API 类型检查通过。
2. `cd apps/ai-desktop && npx tsc -p tsconfig.electron.json --noEmit`
   - 预期：Electron 主进程的 `requestUserInput` 请求存储、答案回传、IPC 与截图事件类型检查通过。
3. `cd apps/ai-desktop && npm run test:managed`
   - 预期：四阶段托管原契约不回归；新增无标注截图、结构化疑问、原请求 ID 回传和完整意图重述契约通过。
4. `cd apps/ai-desktop && npm run test:screenshot`
   - 预期：原图、标注图、签发附件与 temp 路径安全契约不回归。
5. `cd apps/ai-desktop && npm run test:interaction`
   - 预期：隔离 Electron 通过 Playwright 语义定位器完成多疑问逐题选择、其他答案、确认后完整意图重述与阶段按钮显示，并保持既有侧栏回归通过。
6. 隔离 Electron 交互测试：无标注直接点击底部“完成”。
   - 预期：截图作为附件回到原 Codex Chat 输入框，输入框获得焦点，不追加“调查图片红色部分是什么问题”。
7. 隔离 Electron 交互测试：绘制一个或多个红色标注后，分别使用跟随“完成”与底部“完成”。
   - 预期：全部标注被保存，附件回填，红色部分提示只追加一次；“取消”和“返回”原行为不变。
8. 隔离 Electron 交互测试：会话托管触发一个 `requestUserInput` 疑问。
   - 预期：显示单题选项和“其他”文本输入；未选择时确认禁用，确认后继续原 Harness 回合。
9. 隔离 Electron 交互测试：会话托管一次触发多个 `requestUserInput` 疑问。
   - 预期：各题独立选择，全部完成后才能统一确认；Harness 收到答案后重新输出完整意图，回合完成前不显示“就是这意思”。
10. 中断回合、新建会话和 Harness 退出各执行一次。
   - 预期：待回答疑问被清理，旧选项不残留到新会话。
11. `python3 apps/rule-engine/backend/src/main/python/com/sp/selplat/local/code/core/abilities/layered_rule_loader.py '{"action":"load_bundle","logical_ids":["CODE_JS_RULES","CODE_TEST_RULES","AI_DESKTOP_OFFICIAL_HARNESS_RULES","SELPLAT_PUBLIC_COMPONENT_GOVERNANCE_GATE_RULES"],"active_scope":"selplat","active_user":"XUNAN"}'`
    - 预期：四个逻辑 ID 唯一命中，`AI_DESKTOP_OFFICIAL_HARNESS_RULES` 版本为 `5.5.0`。
12. `python3 apps/rule-engine/backend/src/main/python/com/sp/selplat/local/code/core/abilities/layered_rule_loader.py '{"action":"validate_user_index","active_user":"XUNAN"}'`
    - 预期：当前用户索引链无失效路径、重复逻辑 ID 或跨用户引用。

## 统一测试边界

- 代码验证阶段已执行待测项 1、2 和 5；其余项目仍待用户明确提出“统一测试”后逐项执行并回写。
- 本代码验证阶段只启动过临时服务器和后台隔离 Electron；未执行正式构建，未启动或重启当前 AI Desktop。

## 测试托管结果（2026-08-22）

1. 正式开发版构建
   - `npm run build:developer` 通过；渲染层 TypeScript、Electron 主进程和 Vite 开发版产物均成功，转换 227 个模块。
2. 构建后针对性快速测试
   - `test:managed` `9/9`、`test:screenshot` `2/2`、`test:stream` `3/3`、`test:audit` `1/1`、`test:workspace` `3/3`、`test:explorer` `3/3`，合计 `21/21` 通过。
3. 构建后隔离 Electron 交互
   - `npm run test:interaction` `5/5` 通过；成功路径不生成截图，隔离实例和临时服务已由测试脚本关闭。
4. 规则门禁
   - `CODE_JS_RULES`、`CODE_TEST_RULES`、`AI_DESKTOP_OFFICIAL_HARNESS_RULES`、`SELPLAT_PUBLIC_COMPONENT_GOVERNANCE_GATE_RULES` 唯一命中，AI Desktop 规则版本为 `5.5.0`。
   - 当前用户索引校验通过：22 个索引、76 条规则，无失效路径、重复逻辑 ID 或跨用户引用。
5. 重启边界
   - 测试命令未自行启动或重启当前 AI Desktop；本轮返回后由桌面主进程执行一次受控重启。

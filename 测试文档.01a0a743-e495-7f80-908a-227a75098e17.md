# AI Desktop 韩立正式软件验收纠偏测试

- [x] 编译检查：验收计划 v2、冻结旧计划退役入口和事件类型已通过 `npm run build:electron`。
- [x] 定向服务测试：需要发送消息或创建数据的条件不会进入韩立正式页面操作。
- [x] 状态回归：尚无验收结果的 v1 冻结计划只可审计退役并新建 v2 计划；已有结果的旧计划只保留历史，禁止恢复、升级或重建。
- [x] 重启参数回归：独立用户目录继续保留，三个无人消费的 acceptance isolation 参数不再透传。
- [x] 统一测试：项目固定 test、test:interaction、test:collaboration、test:managed 门禁全部通过。
- [x] 发布验证：发布 g246，关闭旧 g245 进程并启动 g246；真实任务卡从原验收卡点继续后显示“验收通过、已完成”。

已完成定向命令：`node scripts/run-with-dependencies.mjs node --test tests/services/evolution/nangong-evolution.test.mjs tests/services/workflow/hanli-review-contract.test.mjs tests/system/release-restart-arguments.test.mjs`，95 项通过、2 项历史兼容用例按既有标记跳过。

统一测试结果：基础测试 612 项通过、2 项历史兼容用例跳过；真实交互 37 项通过；协作流程 136 项通过；托管模式 48 项通过；均为 0 失败。

发布结果：`release-0.1.1-g246` 来源提交 `5f1c9bae43216b234eee6e3c1d0d93bc1dc5e4b7`；应用身份、稳定签名要求、内置 Codex、包内容、工程根和启动健康检查均通过。真实恢复记录先出现 `acceptance.legacy_plan_retired`，再冻结全新计划并完成验收，未恢复冻结旧计划。

规则沉淀结果：已升级当前用户层韩立验收规则与演化发布规则，明确后续修复不得重建或恢复已退役的隔离验收环境，也不得恢复、升级或继续消费冻结的旧验收计划；当前能力必须重新冻结新计划。

## 2026-09-16 韩立验收恢复链修正

- [x] 类型与静态契约：`npm run typecheck`；当前专题投影必须区分原一次性验收运行与普通任务恢复，页面不得再把已集成任务提交给任务级恢复接口。
- [x] 针对性回归：`node --test tests/services/workflow/current-topic-stage.projection.test.mjs tests/features/collaboration/task-group-recovery.test.mjs tests/features/collaboration/collaboration-status-chain-contract.test.mjs tests/services/workflow/hanli-review-contract.test.mjs`；39 项通过。
- [x] 演化与验收回归：`node --test tests/services/evolution/nangong-evolution.test.mjs`；冻结混合计划允许忽略页面条件的冗余源码结论，但仍拒绝未知、重复或缺失的代码条件；87 项通过、2 项既有历史兼容跳过。
- [x] 统一测试：`npm test && npm run test:interaction && npm run test:collaboration && npm run test:managed`；基础统一测试 640 项通过、2 项既有历史兼容跳过；正式 Electron 交互 37 项、协作流程 137 项、托管流程 48 项全部通过，均为 0 失败。
- [x] 打包与发布：`npm run package:mac:developer && npm run verify:mac:developer && npm run verify:package-content`；开发发布包签名、1360 项运行内容和来源提交 `e8c4da0f19581c832269ab708334c5ced8e35ca3` 校验通过。
- [x] 正式页面验收：关闭旧发布进程、启动新发布包，在原专题卡点击“从卡点继续”；实际调用原一次性验收恢复入口，未创建新专题，并完成韩立混合验收。

## 2026-09-16 验收运行防覆盖与现场恢复

- [x] 根因复核：原专题、当前提案、v2 冻结验收计划和失败归档均完整；普通“确认开始专题”在旧验收为 `blocked` 时覆盖了唯一 `oneShotRun` 指针，误建的待确认研讨随后遮住原恢复入口。
- [x] 状态机门禁：可恢复的 `blocked` 运行禁止被普通专题确认覆盖；只有明确专题切换或孤儿运行退役把 `resumeMode` 收口为 `null` 后，才允许建立新运行。
- [x] 归档可恢复性：后续全部 `one-shot.*` 档案同时保存运行标识、专题、提案和恢复模式，避免覆盖事故只能依赖旁路异常记录找回运行标识。
- [x] 托管边界：托管关闭时，成熟但尚未获客户确认的研讨不能自行包装成新运行；已在调查中的同一研讨仍可完成当前问答。
- [x] 旧现场安全恢复：应用启动时仅在当前 v2 提案待验收、同提案有更早阻塞归档、当前指针是更晚的无专题准备运行、统一异常中心能提供原运行标识四项同时成立时恢复原卡点；恢复会追加审计记录，不直接改数据库。
- [x] 定向测试：类型检查通过；演化、恢复与真实 SQLite 仓储回归 113 项通过、2 项既有历史兼容跳过，0 失败。
- [x] 完整门禁：基础统一测试 644 项通过、2 项既有历史兼容跳过；正式 Electron 交互 37 项、协作流程 137 项、托管流程 48 项全部通过，均为 0 失败。
- [x] 重新提交、打包发布、进程替换与正式页面复验。

## 2026-09-16 正式验收最终收口

- [x] 页面投影根因：后来产生但尚未确立的研讨不得覆盖已经绑定专题与提案的当前验收运行；无当前运行时仍正常展示待确认研讨。
- [x] 真实补验根因：任务卡当前结论不再依赖窗口媒体条件解除单行省略；正常宽度和窄窗口均按内容换行，历史节点与当前结论不会横向截断。
- [x] 完整门禁：基础测试 647 项通过、2 项既有历史兼容跳过；正式 Electron 交互 37 项、协作流程 137 项、托管流程 48 项全部通过，均为 0 失败。
- [x] 提交与发布：投影修复提交 `251597e3743b5c0afd6ec1ed456332c142f20f49`，页面修复提交 `1e134468e760f6c41717a7c6be86b91f3bbf7608`；冲突保护基于当前主线重放并形成最终候选 `c29be920e48837dde7c421d5f93aa6e998d93399`，合并提交 `da8d66af`，正式发布 `release-0.1.1-g280`。
- [x] 正式页面结果：原专题卡仅显示“已完成”“当前无需你操作”“本专题已完成”；人物全部空闲，无恢复按钮、无处理中提示，历史轮次继续保留。

## 2026-09-17 韩立任务协作群正式页面导航能力

- [x] 类型检查：`npm run typecheck` 通过，任务协作群条件编号、语义导航动作和正式页面验收调用链类型一致。
- [x] 定向契约测试：`node --test tests/services/personas/hanli-computer-acceptance-contract.test.mjs`，2 项通过；任务卡条件只能在任务协作群页面截图上裁决，详情滚动不再推动整个页面。
- [x] 相邻演化回归：通过工程依赖入口执行 `node --test tests/services/evolution/nangong-evolution.test.mjs tests/services/workflow/hanli-review-contract.test.mjs`，99 项通过、2 项既有历史兼容跳过；直接裸跑因未挂载工程依赖失败，不属于源码失败。
- [x] 统一测试：基础测试 653 项通过、2 项既有历史兼容跳过；正式交互 37 项、协作流程 138 项、托管流程 48 项全部通过，均为 0 失败。首次基础测试因任务工作树未显式传入正式工作区根而被门禁阻断，补齐 `SELPLAT_ROOT` 后原命令通过，未放宽门禁。
- [ ] 发布与正式页面复验：打包、校验、关闭旧进程、启动新进程；韩立从任务面板进入任务协作群，真实完成展开、收起、详情滚动与窄窗口验收。

## 2026-09-17 韩立验收滚动与令狐卡点接管修复

- [x] 根因核对：正式页面读取使用真实矩形与显示状态判定可见，滚动操作却使用 `offsetParent`，导致同一任务协作页先返回 `visible`、随后误判 `hidden`。
- [x] 页面能力修复：滚动与读取统一使用真实矩形、`display` 和 `visibility`；详情已经位于滚动边界时保留操作证据，不再误报工具失败。
- [x] 恢复链修复：结构化分类为 `acceptance-capability-blocked` 的既有验收能力故障进入令狐真实卡点修复链；缺少分类继续等待，原流程已经完成时直接解除，既有授权排除项继续保留。
- [x] 定向测试：`node --test tests/services/personas/hanli-computer-acceptance-contract.test.mjs tests/services/workflow/checkpoint-coordinator.test.mjs`，34 项全部通过。
- [x] 类型检查与完整统一测试：`npm run typecheck && npm test && npm run test:interaction && npm run test:collaboration && npm run test:managed`；基础测试 656 项通过、2 项既有历史兼容跳过，正式交互 37 项、协作流程 138 项、托管模式 48 项全部通过，均为 0 失败。
- [x] 本地提交与发布：代码提交 `0dacb61ff3960624b1a259f4805a8420ed1f1048`；开发包签名、1364 项运行内容和工程根校验通过，发布为 `release-0.1.1-g289`；关闭旧 g288 进程并启动 g289，运行参数和发布清单均匹配该提交。
- [x] 正式页面恢复链复验：g289 启动后，同一原专题新增“韩立 → 令狐老祖 任务已分发”和“第 1 轮卡点处理 · 调查修复任务已派发”节点；令狐老祖显示“技术分析中”，证明验收能力故障已进入既有令狐修复链，不再由韩立重复盲验。
- [x] 规则沉淀评估：近义规则为 `RUL_AIDesktop令狐故障修复与统一测试规则.md` 与 `RUL_AIDesktop协作与自动化规则.md`；现有规则已经规定令狐处理真实技术卡点、返回原步骤复验及不越过授权边界，本次属于实现偏离规则后的代码闭环，无需新增或升级规则。

## 2026-09-17 韩立一轮收齐验收证据

- [x] 类型检查：验收工具新增的逐条件证据归属与现有运行输出协议保持一致。
- [x] 定向契约测试：页面动作必须声明实际核对的条件；首项截图不能冒充未执行条件的证据；发现失败后仍继续其余安全条件。
- [x] 正式交互测试：两条页面条件中，第二条未取得自己证据时 finish 被拒绝；继续核对第二条后可一次提交完整结果。
- [x] 相邻演化与验收回归：混合验收仍一次合并页面和源码全部条件，失败清单完整交给原令狐修复链；105 项通过、2 项既有跳过。
- [x] 统一测试：基础 658 项通过、2 项既有跳过；交互 37 项、协作 139 项、托管 48 项全部通过，且不恢复退役隔离验收或冻结旧计划。
- [x] 本地提交、打包、校验、发布、关闭旧进程、启动新进程：运行代码来源提交 `df634c2b6da3f07d46c2a1bc572e6a6cd2414331`，发布为 `release-0.1.1-g295`；签名、1364 项包内容、内置提示词和 5.162.0 规则均核验通过。旧 g294 进程已关闭，新 g295 进程已启动；正式页面从同一恢复点继续后显示“韩立已开始本轮结果验收”，将按逐条件证据一次收齐全部可执行条件。
- [x] 规则沉淀：升级现有 `AI_DESKTOP_EVENT_MEMORY_UI_RULES` 至 5.162.0，登记逐条件动作与截图归属、首项失败后继续独立条件、整轮一次返回和受阻边界；规则包 146 文件重新生成，索引及规则工作区 2 项回归通过。

## 历史本地修改归属等待自动解除

- [x] 根因：原修复任务保存了本地未提交文件快照；文件已经提交且工作区恢复干净后，新验收证据仍无条件保留旧 `local-change-ownership` 等待，导致令狐无法消费新的完整失败清单。
- [x] 修复：新增主工作区真实未提交文件复查；只有当前仍存在未提交文件时才保留客户等待，工作区干净时清除旧归属失败与客户指导，并让同一任务按新证据继续调查。
- [x] 针对性验证：`npm run typecheck`；`npm run test:collaboration`，140 项全部通过。
- [x] 统一测试与发布：主测试 659 通过、2 跳过，交互 37、协作 140、托管 48、规则 2 项全部通过；签名与 1364 项包内容校验通过。运行代码提交 `042568d0de38cbf5abdc80c1f2f93525dc497544` 发布为 `release-0.1.1-g296`，旧 g295 已关闭，新进程 PID 98731；正式页面保持同一专题与同一恢复点，当前韩立正在完成本轮验收。
- [x] 规则沉淀评估：近义规则为 `AI_DESKTOP_EVENT_MEMORY_UI_RULES` 与既有协作恢复规则；本次修复是让实现正确消费已满足的恢复条件，不新增用户行为或流程约束，因此无需升级或新增规则。

## 2026-09-17 监控者原子封存与 AI Memory 主线解耦

- [x] 根因核对：主进程与后台入库 Worker 同时打开 AI Memory SQLite，控制面事实、人物正文和语料入库共用同一连接竞争域，是点击鼠标持续转圈的主要结构性原因。
- [x] 持久化解耦：AI Memory 只由 Worker 打开，人物正文、语料和检查点只经后台端口读写；主进程使用独立 `workflow-control.sqlite3` 保存工作流、时间线、专题状态和人物线程指针。
- [x] 首次迁移：仅复制控制面表，不复制人物会话正文；迁移失败会撤销本轮新建控制库与标记，下次启动可完整重试，不留半迁移成功假象。
- [x] 原子封存：新专题切换只调用一个应用边界，统一结束旧执行连接、退役签发工作树、封存旧修复卡并释放人物；正式页面已有通过证据时，一次 SQLite 事务同时退役旧运行、无任务归属的孤立卡点树并建立已完成验收卡；有未提交证据时阻断封存，已退役旧树幂等收口但不重建。
- [x] 定向与完整门禁：类型检查通过；监控者归档与交付投影 105 项通过、2 项既有历史兼容跳过；完整统一测试 667 项通过、0 失败、2 项既有跳过；模块边界 17/17、托管 48/48、规则 2/2，正式交互 37/37、协作 142/142 的相邻门禁保持通过。
- [x] 本地提交、开发包打包校验、发布、旧进程关闭与新进程启动：原子验收、完成态投影和孤立卡点树退役分别提交为 `c8fa846d27662f4ec263028e946ac6a69a255b67`、`63f8c6976838dfc67fb13a0a8feb8072a9a1cf27`、`911acbdcb5fcf85f13b4e35c0a835ae9cf6b6d0b`；签名、1378 项运行内容、内置 Codex 0.154.0 与工程根校验通过，错误投影的 g322 和被替代的 g323 已移入 rejected，最终发布为 `release-0.1.1-g324`，进程 PID 73990，运行来源与最后代码提交一致。
- [x] 正式页面独立“g321 交互卡顿正式验收”卡显示“正式页面验收通过、当前无需操作、可开始下一专题”，展开后仅有一条“验收通过，结果已归档”；人物切换和任务协作群往返及时。旧“第 1 轮卡点处理”、旧验收归档卡和旧自动入库修复卡均显示已取消，人物全部空闲，不恢复冻结旧计划或退役工作树。
- [x] 规则沉淀：升级现有 `AI_DESKTOP_COLLABORATION_AUTOMATION_RULES` 至 5.164.0，固定“正式交付后单调用原子封存、退役旧树、释放人物；已有正式通过证据时独立验收卡直接完成归档，不进入常规派发链”。
